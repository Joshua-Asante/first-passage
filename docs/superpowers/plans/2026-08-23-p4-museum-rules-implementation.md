# P4 museum rules — Implementation Plan

> **For agentic workers:** Landed same session as GO. REQUIRED SUB-SKILL was not needed (pointer-only).

**AUTHORIZATION:** **GO 2026-08-23.** Operator: close remaining pain-point packets. Charter §P4.

**Goal:** Rule 1 origin no longer reads as live Guardian book; Rule 7 lock paths match `_archive/<family>/` + CARD stubs.

**Shipped:** [`docs/operational_rules.md`](../../operational_rules.md) Rule 1 HISTORICAL origin + Rule 7 lock/lineage rows; edit-log entry.

**Falsifier:** Rule 1 still reads Guardian as live, or Rule 7 still cites `core/strategies/<strat>/LOCK.md` — **cleared**.
