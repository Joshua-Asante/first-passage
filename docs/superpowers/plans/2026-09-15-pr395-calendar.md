# PR 395 Calendar Implementation Plan

> **For agentic workers:** Execute with superpowers:executing-plans; use superpowers:subagent-driven-development when bounded delegation is useful and authorized. Preserve the behavioral contract and integration owner. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extract an independently reviewable calendar and enforce ratification time before admission.

**Architecture:** Keep validated schedules readable independently of permission. Retain a nullable ratification instant on the immutable calendar; only the ratified loader supplies it. Admission checks this instant before producing a sizing session.

**Tech Stack:** Python dataclasses, zoneinfo, JSON/SHA-256 artifacts, pytest.

**Spec:** `docs/superpowers/specs/2026-09-15-pr395-component-split.md`

## Global Constraints

- Preserve the pinned calendar, overlay and evidence bytes and policy constants.
- Keep the existing legacy evidence warning visible; extraction is not fresh source qualification.
- No settlement database, signing key, Step 5 acceptance or Step 6 admission is part of the calendar PR.

## Task 1: Calendar lookup and timed admission

Integration owner: coordinating agent. Existing `BookSession` and sizing consumer
on main consume admission results. The operator's existing RATIFIED.json is the
producer of permission authority; raw artifact validation is insufficient.

Files: `ops/c1_rail/book_session_calendar.py`,
`scripts/author_book_session_calendar.py`, `ops/calendars/`, `.gitattributes`,
`tests/ops/test_book_session_calendar.py`, and the Step 4 calendar note.

- [x] Extract calendar-only files from `bd41cb6` into `codex/pr395-calendar` based on `242992b`.
- [ ] Add regression cases using the actual ratified loader, with a synthetic second-precision ratification record and unchanged pinned artifacts:

```python
assert not raw.session_for(at).permitted
assert cal.session_for(at - timedelta(microseconds=1)).refusal == "calendar_not_yet_ratified"
assert cal.session_for(at).permitted
assert cal.session_for(at + timedelta(microseconds=1)).permitted
assert cal.schedule_for("tradeify-account-day:2026-09-03") is not None
```

- [ ] Run `py -3.13 -m pytest tests/ops/test_book_session_calendar.py -q` with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`; confirm new tests fail for the missing admission boundary.
- [ ] Import `replace`, add `ratified_at: datetime | None = None`, return `replace(calendar, ratified_at=_utc_event(row["ratified_utc"], "ratified_utc"))` from the ratified loader, and refuse raw/not-yet-ratified admission before constructing `BookSession`.
- [ ] Update admission fixtures to use the ratified loader. Tests needing synthetic dates must write explicit synthetic ratifications, never imply historical production permission.
- [ ] Run the calendar and sizing integration tests, then the complete ops suite. Verify pinned-byte tests and regression boundaries pass.
- [ ] Independently review the component and resolve actionable findings; record exact validation results here.
- [ ] Commit, publish the calendar PR against main, and monitor required checks. Link the split and remaining dependencies from PR 395.

## Verification and review

The five new ratification regression cases failed on the extracted code before
the fix. Calendar and sizing integration then passed (241 tests). The complete
ops suite passed: 1,733 passed, 15 skipped, two upstream seaborn warnings, using
Python 3.13 and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` on this working tree.

Independent review found no blocking code defects. Its documentation finding
was corrected: explicit holiday CME trade-date input is an outstanding authoring
capability, not a feature supplied by this extraction. Pinned September bytes
remain unchanged. This limitation must be resolved before a future holiday
extension is qualified.

## Remaining split work

Evidence ingestion, pure calculation, durable owner and qualification are still
owed under the approved design. Each receives its own execution plan and review;
this calendar PR does not claim their findings are resolved.
