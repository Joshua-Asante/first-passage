# Phase 3 completion handoff

> **For agentic workers:** Execute with superpowers:executing-plans when this handoff is dispatched. Preserve one integration owner and the behavioral contract below. This document is a draft assignment, not an instruction to restart work in the drafting session.

**Goal:** Qualify the fixed portfolio through accepted tooling, F1 freeze, separate exact-depth approval, prescribed E1 execution and, only on PASS, policy admission and separate ORB approval.

**Architecture:** One Phase 3 integration owner accepts the complete source → frozen contract → durable dispatch → replay/Part A → authenticated result → seal chain. Existing validators, controller and journal retain authority across restarts; component test results do not establish whole-flow acceptance.

**Tech stack:** Existing Python qualification package, SQLite attempt journal, detached approval signatures, pytest and repository gates. Pin the actual interpreter and dependencies before freezing.

**Spec:** Read the governing documents listed in `docs/briefs/phase3-preparation/2026-09-15/requirements.md`, especially the final-validation preregistration, protection-instance admission ADR, synchronized replay specification, current ratifications and regime robustness gate. Resolve discrepancies against current code and approved requirements before execution.

## Mandate and limits

When dispatched, resume Phase 3 engineering and prepare every concrete approval packet. Continue through qualification only after the required approvals and consuming prerequisites are actually satisfied. A generic instruction to complete Phase 3 is not an exact-depth approval or ORB GO.

- Preserve the fixed portfolio, historical admitted inputs, corrected port generation and predecessor chains. Do not tune after seeing outcomes, change thresholds to obtain PASS, silently change settings, or redraw a failed/ambiguous attempt.
- Read production risk-control code before changing or describing its behavior. Preserve replay/runtime rule equivalence and repository dependency boundaries.
- Keep private inputs, account evidence, executable private ports and qualification outcomes in verified ignored storage. Public evidence is limited to permitted identities, verdicts and counts.
- Do not execute B7, n3, deployment, live activation or orders. These belong to later phases. Do not invent a fresh-B7 prerequisite for pristine E1.
- No merge is authorized by this handoff. Prepare a reviewable PR when engineering is ready; obtain any required merge authorization before the accepted merged revision is frozen.

## Starting state and ownership

- Phase 2 PR: https://github.com/Joshua-Asante/first-passage/pull/409. Original offline acceptance is at `5a2098b5be2c106ee385313700f1f958e22ebe6f`; PR follow-up `928ece4c01fa3e053361aac1f7e1466a564228d0` removes a duplicate image-validation entry. Inspect current remote head, checks and merge status; do not assume it has merged.
- Phase 3 branch: `codex/phase3-qualification-tooling`.
- Worktree: `C:\Users\joshu\multi_firm_operations\.worktrees\phase3-f1-preparation`.
- Preserved HEAD: `35fae36169f056600286081c44edcc53bf618880`; last engineering checkpoint: `8f09a6266be671890cf4e0693664612eeded7a8a`.
- Read `docs/briefs/phase3-preparation/2026-09-15/paused-checkpoint.md`, `README.md`, `production-readiness.md`, `execution.md`, `tooling-review.md` and `compute-depth.md` first. Earlier progress notes are historical and must not override the pause checkpoint.
- Inspect recovery commit `f9ff974` and its `preflight-g5-unreviewed-checkpoint.md`. Its preflight/seal work is incomplete and unreviewed; assess it before selective integration. Do not label it accepted or cherry-pick unrelated work.
- The Phase 3 integration owner now owns unfinished qualification control as well as replay composition. Do not leave preflight/G5 waiting indefinitely for the completed Phase 2 task. Coordinate any genuinely shared runtime repair with that owner.
- Preserve the primary checkout and all unrelated worktrees. Record shared-file ownership before concurrent edits. This draft does not dispatch additional agents.

## Phase 3A — Finish and accept the engineering

- [ ] Inspect current branches, dirty files and remote state. Reconcile the preserved qualification branch with the accepted Phase 2 successor; account explicitly for schedule, import, input-binding and native-exit changes. Re-run affected acceptance after reconciliation.
- [ ] Complete domain-aware preflight and G5 integration. Starting files: `ops/c1_rail/qualification/{trust_domain,contract,preflight,attempt,production_source,production,orchestration,result_adjudication,seal}.py` and their current consumers. Inspect actual interfaces before editing; the old plans contain proposed interfaces and outdated ownership.
- [ ] Prove the genuine signed TEST_ONLY route: validator-issued G1 → retained source/runtime bytes → G2 controller and durable attempt → continuous replay/Part A → authenticated G5 result → seal. Use the existing `tests/ops/qualification/test_composition_route.py` as a starting point. Public production entrypoints must reject TEST_ONLY authority.
- [ ] Execute the full negative/recovery route: failed receipt persistence, reopen without redraw, changed retained bytes or derived state, substituted keys using enrolled IDs, actual import aliases, independently signed but cross-wired domains, missing verifier/authority, partial outcomes and runtime identity drift. Retain failures and attempt state rather than manufacturing a fresh attempt.
- [ ] Prove that the actual selected runtime, source, settings, calendar and replay rules are the ones captured and consumed. Retain historical input identities separately from reviewed runtime successors.
- [ ] Run the combined qualification suite, Phase 1 provenance/loader regressions, affected runtime/settlement/sizing tests, required repository gates and full operations suite as appropriate to the final diff. Capture commands, interpreter, dependency pins, exact head, counts, skips and exit codes.
- [ ] Obtain independent whole-flow review at the final integrated revision and close findings. Existing component passes at `8f09a62` are useful evidence, not final pipeline acceptance.

**Exit:** One exact, reviewed integration revision with executable positive and rejection/restart evidence. No production qualification outcome has been generated.

## Phase 3B — Resolve F1 inputs and prepare the decisions

- [ ] Turn the F01–F50 requirements matrix into an evidence ledger: satisfied, missing, or inapplicable with a reason and named consuming gate. Obtain accepted source populations, source/settings bindings, calendar coverage, exclusions and affected parity evidence.
- [ ] Resolve source-bound exact-instant price/interval chronology where exposed or pending legs require it, or obtain the explicitly approved timing/fill model. Missing observations are not proof of flatness or coverage; synthetic split fixtures are not historical evidence.
- [ ] Keep actual-account evidence separate. Operator-provided semantics are TradingView `America/New_York`, Tradovate `CDT`, inclusive date endpoints. Source-backed mapping, September 14 closing equity or contemporaneous flatness, and close acceptance/correction evidence remain unresolved unless new retained evidence establishes them. Apply those facts only at their actual consuming gates.
- [ ] Refresh representative single-process measurements on the integrated pipeline. Measure source verification, replay, Part A proofs, controller, runtime capture, durable result handling and sealing separately and together. Do not reuse bare-replay timings as the total budget.
- [ ] Prepare a concrete F1 packet binding exact code, source/runtime/dependency closure, portfolio/settings, clocks/calendars, startup, coverage/model assumptions, monitoring indices, RNG allocation, stage sizes and budget. Obtain the actual required freeze approval; do not self-issue operator signatures.
- [ ] After F1, obtain the separate dated exact-depth approval bound to that frozen identity, actual workload, attempt and measured budget. Draft values `n1=200`, `n2=n3=970` per population, Part A depth 200 and horizon 500 remain proposals until approved. n3 is reserved for later, not executed here.

**Operator packet:** Present the exact decision, recommended selection with evidence, identity digest, compute/time cost and consequences. Complete all independent preparation before asking; do not repeatedly ask for already granted authorization.

**Exit:** Accepted frozen contract and independently authenticated exact-depth approval, with all E1-consuming prerequisites satisfied. If evidence or approval is missing, report that precise gate and continue independent work; do not call Phase 3 complete.

## Phase 3C — Execute the prescribed E1 once

- [ ] Verify current production entrypoints against the accepted code, authenticated approvals, complete observed inventory, pre-admission registry state and a new ignored private output root. Do not execute illustrative commands from old Markdown as though they were approved interfaces.
- [ ] Reserve and retain the durable attempt. Execute the prescribed legality screen, n1, n2 with its coupled Part B populations, and Part A with only its prescribed expansion. Use the frozen sizes, namespaces, stopping rules and thresholds.
- [ ] Persist stage receipts and output identities. On interruption, inspect retained state and follow the approved recovery semantics; no automatic restart/redraw of a started outcome-bearing stage.
- [ ] Adjudicate the complete evidence through G5. Only authenticated complete PASS permits the E1 seal/FBR. Preserve FAIL, ambiguity, invalidity and partial evidence accurately; do not tune or substitute draws.

**Exit:** A revision- and contract-bound terminal E1 disposition. A failed or unresolved E1 is a reported qualification outcome, not permission to admit the policy.

## Phase 3D — Admission and separate ORB decision

- [ ] On E1 PASS only, prepare and validate the exact D0 policy admission using the governing ADR, matching policy/cell/shared identities and independent provenance. Complete the authorized admission change through required review and approval.
- [ ] Prepare the separate D1 ORB decision with its required book-level PASS and ORB-specific supersession evidence. Obtain and retain explicit operator ORB GO; do not infer it from E1 PASS or remove ORB to bypass it.
- [ ] Publish a final handoff listing exact commits, frozen identities, review/test evidence, approved depth/budget, durable attempt disposition, E1/FBR, D0 admission and D1 decision. Clearly identify anything still missing.

**Phase 3 completion:** Accepted engineering + F1 + separate exact-depth approval + E1 PASS/FBR + accepted D0 admission + separate D1 ORB approval. Stop there. Feed/broker qualification, attended operations, release review, fresh B7, sole n3, deployment GO/reseal and initial-session activation remain later work.
