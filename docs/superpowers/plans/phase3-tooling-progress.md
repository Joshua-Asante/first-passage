Plan: docs/superpowers/plans/2026-09-15-phase3-qualification-tooling.md

Base d107ebdfaec194ac4b4448d1d121331ff7128e1a; branch codex/phase3-qualification-tooling.

Latest integration: source/loader8e5c6ec, independent Phase1 regressiond7c2f4c,
runtime provenance and provisional repeated synthetic timing6a953d8, G5 authority
repair2e28b5a and real-store controller reconciliationb8f929d. The combined
qualification and unchanged Phase1 test files passed289tests in23.13s on
CPython3.13.2. Original four signature/receipt mutation regressions now pass;
Phase1 independent closure confirmation and final full-route acceptance remain
pending. Counts include the evolving synthetic composition fixture tests.

Coordinator additionally authorized a single signed TEST_ONLY trust context for
true positive full G1→retained source/ports→G2controller→replay/PartA→G5 tests.
Phase2 owns context/G1/G5 binding; Phase3 owns fixture, source/loader/executor and
controller consumers. Production pins and authority remain fixed. Test context
must bind every signed/durable boundary and be rejected by production consumers.
No actual qualification is authorized. This composition work is required before
the final combined review, not an optional replacement for component testing.

Trust-domain module checkpoint72d9df7 passed32tests and independent review after
fixing compiled-policy same-object mutation and mutable input-buffer aliasing.
The following consumer checkpoint deliberately precedes Phase2's matching
contract/preflight/journal/G5 APIs. Public production entrypoints reject TEST_ONLY;
private composition wrappers share mechanics with an issued domain. Controller
seed/plan/receipt/path/result bindings carry its digest. The last broad consumer
probe reported81passed/9failed: missing AttemptStore trust-domain keyword and
canonical import aliases were expected integration dependencies; the fixture's
loaded-package closure discovery was subsequently extended before signing. A
new executor-domain reassignment regression passed after a per-dispatch recheck.
This checkpoint is reviewable integration work, not combined acceptance. The
real retained-source/controller/G5 composition test must pass after Phase2 lands
its owned consumers; no skip or waiver closes that requirement.

- Model/coverage/clock and runner: integrator; initial tests passing, controller integration pending.
- Blocks/path/PartA: /root/bootstrap_tooling; implemented and reviewed corrections for seed purpose, adjacency and interval boundaries. Explicit nearest_rank supported alongside linear/nearest. Source coverage never inferred from missing observations.
- Replay: /root/continuous_replay; continuous shared-policy/emulator implementation, now58 focused tests including seven startup/state and exact-risk vectors. Earlier reviewed fixes committed58a341b andbb21d77; proposed startup does not inherit arbitrary-start native parity.
- G1/G2/G5: Phase2, exclusive files agreed, separate worktree.
- Independent provenance: Phase1 commits 6a8b5a0 and 1c5b54a integrated as 2c595c2/b919afb; loader fixes independently reproduced passing and runtime precursor 72e3242 integrated as 4abc30a.
- Local checkpoints:58a341b G3/G4;9a1a91c shared schedule;bb21d77 provider/native exits;7599b01 result adjudication. G1/G2/G5 integrated as2d464c0,01cd253,6dc2224,13ee23d. Further producer-code and journal hardening remains in progress with Phase2.
- Concrete production source factory, once-only orchestration and driver: implemented, combined review in progress. Driver consumes exact fresh G2 dispatch capability, rechecks budgets on deadline exceptions; PartA rejects nonterminal failure prefixes. Controller receipts now match G5 after an actual SQLite-to-G5 regression exposed the prior mismatch.
- Runtime-origin collector and evidence-bound expected source grid: implementation/review in progress. No final combined acceptance or final compute estimate yet.

Open consuming-gate dependencies: approved frozen settings and transitive identities; source/schedule/coverage evidence; exact depths and budgets; no outcome execution authorized. S2 proposed lab placement reclassified to ops by mutual implementation-owner agreement to preserve repository boundaries.

Verification checkpoints: combined G3/G4 suite 85 passed before subsequent partial-fill extension; model deep-immutability vectors 21 passed independently; import boundary gate passed after relocating executable preparation script to ops. Preparation source-free component timing refreshed with that relocation, recorded in compute-observation.json. Exploratory 500-session replay timing 7.624s is diagnostic only (not final reviewed integrated benchmark), seed791946219, 46,000 path bars/184,000 adapter bars.

Findings fixed under review: outer vs inner seed collision; lost outer adjacency/tail gap; shallow immutable source/edge/outcome collections; rounded or activated stop timing mismatch; adverse exit-slippage undercount; partial deadline lost excursion; native same-calculation bracket/OCA batching; controller cancels causing extra close calculations. No known fixed finding substitutes for combined review.

Execution note: PowerShell subprocess startup stalled during work; explicit cmd.exe/login:false restored read/test execution. Stalled outputs are not test evidence. Tests use CPython3.12.14, no bytecode writes, pytest plugin autoload disabled, isolated temp directories.

Combined controller tests also use existing CPython3.13.2 with the Phase2 pytest-dependency directory because the3.12 environment lacks cryptography required only for test signing. Production verification uses the repository Ed25519 verifier. Stable qualification subset179tests passed before the latest startup/controller fixes; independent111focused tests verified those fixes and synthetic startup invariants. Independent25controller/G5/loader tests then verified corrected receipt compatibility. Counts refer to their bounded checkpoints, not final acceptance.
