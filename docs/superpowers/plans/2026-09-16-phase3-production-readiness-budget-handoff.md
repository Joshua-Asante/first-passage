# Phase 3 Production Inputs, Workload Budget and Freeze Definitions Implementation Plan

> **For agentic workers:** Execute with superpowers:executing-plans; use superpowers:subagent-driven-development when bounded delegation is useful and authorized. Preserve the behavioral contract and integration owner. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce an evidence-complete, decision-ready F1 candidate with accepted production-input bindings, a defensible full-workload compute estimate and every required freeze definition resolved or explicitly blocked.

**Architecture:** Extend the existing F01–F52 preparation inventory and signed qualification tooling after its integrated acceptance. Separate source verification, bounded parity evidence, non-decision-bearing workload measurement and operator model decisions. Assemble one review packet; do not create another qualification runner or turn preparation into an outcome-bearing sample.

**Tech Stack:** Accepted Python qualification/source/fingerprint libraries, private retained ports/panels/settings, existing parity and calculator tools, post-413 operations launcher/evidence recorder and Markdown preparation records.

**Spec:** [Phase 3 plan](2026-09-16-phase3-simplified-qualification.md), work package 2; [post-413 integration handoff](2026-09-16-phase3-post413-integration-handoff.md); accepted final-validation preregistration, TB-P2, TB-S2 and F01–F52 requirements. Read the current integrated preparation records rather than treating historical candidate tables as approved configuration.

## Global Constraints

- **DRAFT HANDOFF — 2026-09-16.** This request authorizes drafting only. Later execution covers preparation and its expressly permitted checks, not F1 approval or qualification execution.
- Require accepted signed integration on the selected baseline before claiming production readiness. PR 415 is the integration reference; verify its final revision, CI, review and merge disposition at execution, including resolution of the previously observed missing-signing-dependency CI failure.
- No F1 signature, exact-depth ratification, production legality screen, n1/n2/Part A/n3 draw, E1 seal, D0 admission, ORB GO, B7, deployment or arm in this slice.
- Use the accepted fixed book and production risk code. Read `book_policy.py`, sizing/capacity consumers and their accepted configuration before any risk-related proposal; do not copy stale numerical claims from preparation documents.
- Historical admission is scoped evidence, not automatic acceptance of changed runtime ports/settings/startup/schedule. Preserve original artifacts and historical receipts.
- No performance-based choice of startup, source window, exclusion, depth, monitoring definition or model amendment. Bounded required parity checks are not permission for exploratory portfolio runs.
- Keep private sources, panels, settings, numerical outputs and evidence under approved ignored primary-checkout roots. Verification records can capture command/output bytes; do not print private data or secrets into public reports.
- Use the tested checkout's launcher and validated operations environment; keep research environment requirements separate. No bare-system-Python workaround or parallel simulation fan-out.
- Actual feed/broker qualification remains Phase 4. Do not import live-only evidence gates into pristine E1 unless a governing F1 requirement explicitly consumes them.

---

## Ownership and deliverables

The coordinator owns the combined packet. Existing source/parity/calendar owners supply scoped acceptance. Existing validators establish byte identity and valid structure. Joshua owns unresolved material model choices and later approvals. A digest is not acceptance, and a candidate validator cannot grant authority.

Update the existing preparation files under `docs/briefs/phase3-preparation/2026-09-15/`: `requirements.md`, `identity-ledger.md`, `production-readiness.md`, `compute-depth.md`, `representative-workloads.md`, `freeze-candidate.md`, `execution.md` and their retained measurement artifacts. Preserve historical observations as dated history; mark superseded claims explicitly.

Create or extend one `decision-packet.md` there. Each F01–F52 row reports original requirement/owner, consuming gate, actual evidence/revision/digest, disposition and next action. Use `SATISFIED`, `BLOCKED`, `DECISION_REQUIRED` or `LATER_GATE`; the last requires a cited gate and cannot waive an F1 prerequisite. Keep candidate artifacts visibly **DRAFT — NOT FROZEN**.

## Task 1: Pin accepted integration and audit the readiness delta

**Outcome:** One current requirement inventory replaces scattered historical claims without repeating accepted engineering work.

- [ ] Select an isolated post-415 integration baseline with recorded accepted revision, environment and clean starting state. Preserve old worktrees and unrelated primary-checkout files.
- [ ] Read the post413 acceptance record and final CI/review evidence; verify decisive signed cases executed. Do not use a failed hosted test or a skip as acceptance.
- [ ] Reconcile every F01–F52 row against current code, contracts and evidence. Close only the engineering items actually established by integration; carry source/settings/calendar/budget obligations forward.
- [ ] Inventory private artifact availability and original admission scope by read-only hashes. Report exact missing bodies or receipts; do not request all seven bundles again by default.
- [ ] Identify the authority for every proposed parity/diagnostic run before execution. Where existing permission does not cover the actual data/run, prepare the exact procedure and obtain the required bounded authorization while independent work proceeds.

**Acceptance:** Every open item has a named producer and consuming gate. The packet distinguishes seven evidence bundles from the four live legs, historical full-origin parity from successor qualification, and implementation acceptance from external-source facts.

## Task 2: Complete source, settings, startup and schedule readiness

**Outcome:** Actual candidate inputs have traceable provenance and scoped acceptance through the same source/loader behavior the qualification will consume.

**Existing boundaries:** `ProductionSource.build(contract, artifact_root=...)`, qualification adapter loader, typed calendar/population construction, retained runtime inventory and shared replay/sizing consumers. These may require validated frozen authority: do not forge that authority to execute a pre-F1 preview. Use existing non-authorizing intake/parity validators; any missing preparation interface must be a narrowly reviewed separation of pure validation from authority, not a production bypass.

- [ ] Bind exact private Pine/port/panel/export/settings bytes and historical receipts. Identify each successor change and its specific required parity/review evidence; preserve historical references unchanged.
- [ ] Resolve fixed-book successor settings, including ORB's accepted base behavior versus its historical export settings. Verify loader uses the reviewed successor, with no historical fallback. Review changed ports/settings through the existing bounded acceptance procedure.
- [ ] Specify startup exactly: constructor and configured paper state, pristine E1 account, readiness behavior, retained source prefix role, scoring denominator and continuous state across sampled splices. The existing cold-once-per-assembled-path proposal remains a proposal until accepted; historical full-origin parity is not arbitrary-start equivalence.
- [ ] Bind source date membership, active-window rules, provider-slot provenance, population partitions and exclusions. Distinguish consumption loss, cross-leg miss, provider-wide absence, policy denial and exchange closure. Do not invent missing bars or strengthen RC7 into an unapproved full-tape requirement.
- [ ] Accept exact schedule/product/calendar/overlay inputs for all consumed historical dates. Future deployment coverage cannot certify historical schedules. Preserve source/path-clock mapping and explicit missing-date dispositions.
- [ ] Resolve between-bar actions using source-bound finer chronology and reviewed interval mapping, or present an exact permitted timing/fill model amendment for decision. No interpolation, next-bar price substitution or synthetic chronology masquerading as market evidence.
- [ ] Run only the required, authorized successor parity/source-consumer checks. Retain input/output digests, scope, comparison verdict and limitations; do not use economic results to choose a replacement configuration.

**Acceptance cases:** changed port/settings under old receipt; missing required prefix; wrong source generation; schedule/date mismatch; fabricated closure for missing data; unsupported intrabar chronology; state reset at a splice. Each rejects or remains blocked under its owning requirement. Valid retained sources traverse accepted preparation consumers without authorizing G1 or a production attempt.

If chronology or startup genuinely requires a model decision, consolidate it into one operator packet with exact proposed semantics, alternative dispositions and affected evidence. Missing source facts cannot be supplied by a signature.

## Task 3: Measure full workload cost without consuming qualification

**Outcome:** A reproducible engineering measurement and accounting model covers the complete planned workload, clearly separating measured values, projections and unknowns.

The old invented-adapter timings exclude material work and are historical illustrations. Current signed test-suite durations are also not production budgets. Do not reuse either as the final estimate.

- [ ] Before measuring, define and retain a benchmark protocol: accepted code/runtime/dependencies, input class, fixed non-qualification seeds, repetitions, machine conditions, timed boundaries, allowed outputs and maximum bounded work. It must have no production authority or qualification namespace.
- [ ] Build an exact workload inventory from candidate source metadata and frozen-design proposals: legality/chronological checks, FULL/H1/H2 population proof, n1/n2, shared Part B, ordinary and maximum prescribed Part A construction/depth, later sole n3, and monitoring at its own gate. Do not double-count speed or Part B as independent draws.
- [ ] Measure synthetic full-shape workloads through real retained-source verification, runtime capture, source/panel proof, initialization/warmup, continuous replay/shared kernel, Part A reconstruction, serialization, durable dispatch/checkpoint I/O, adjudication, authentication/commit and seal. Synthetic signing must exercise the real boundaries in a segregated TEST_ONLY domain.
- [ ] Include the actual frequency of source rehash/proof/closure checks: per process, per panel, per stage or per path as implemented. Do not estimate one startup check if the executor repeats it on every dispatch.
- [ ] Scale source length, row counts, indicator/event complexity and alternate-panel shapes to the accepted production inventory. Measure wall/CPU, memory, retained bytes/disk, cold/warm effects and stage costs across the predeclared sequential repeats; retain stable input and source identities.
- [ ] For private-port cost that synthetic adapters cannot establish, define a bounded non-decision-bearing measurement using a permitted data class and exact allowed observables. If it executes actual private panels or could reveal portfolio outcomes, confirm its specific authority before use. No exploratory real-data sample is authorized merely by calling it a benchmark.
- [ ] Keep production attempt journals, namespaces and authority entirely inaccessible to measurement. Synthetic output/receipts must fail production admission; a pre-F1 timing tool cannot mint a frozen contract.
- [ ] Produce ordinary/max-case totals with each measured unit multiplied by its actual count/frequency, plus startup/finalization, storage and explained uncertainty margin. Distinguish measured full-shape cost from extrapolation; identify every residual unbounded component.

**Budget output:** For each stage: exact workload count/source dimensions, measurement identity, repetitions/minimum/median/maximum, scaling assumption, memory/disk needs, conservative total and limitation. Report E1, later n3 and later monitoring separately. The n3 estimate must expose any conflict with the eventual B7 launch window; solve that before launch rather than extend expiry.

**Stop rule:** If runtime is impractical, return the bottleneck and permitted options. Do not reduce depth/horizon, expand hardware/spend, parallelize samples, weaken repeated integrity checks or change statistical criteria without their required review/authority. Performance repairs require behavior-preserving tests and a new measured identity; no budget acceptance on obsolete code.

**Acceptance:** Every cost is measured or transparently projected from a representative measured unit; unknown production costs prevent a complete-budget claim. A whole production Monte Carlo batch is not required or permitted for preparation. If representativeness cannot be established within authorized non-decision-bearing work, return that precise measurement decision as a blocker.

## Task 4: Finish every freeze definition

**Outcome:** No required field is left implicit or chosen after results; unresolved decisions are surfaced before F1.

- [ ] Bind actual code/tool/runtime/dependency bytes, canonical fingerprint recipe/vectors, complete source/settings/calendar closure and candidate config. Verify permitted exclusions and independently authenticated admission/provenance requirements.
- [ ] Finalize initial state/startup, source populations/date partitions, costs/fill/tie/schedule rules, exact fixed-book settings and per-leg request/allocation semantics from accepted owners. Do not copy stale candidate tables over current code or silently change policy.
- [ ] Use the existing certification calculator under declared design assumptions to prepare n1/n2/n3 proposals and exact cutoffs. Record assumptions and governing approval status; historical numerical proposals are not automatically ratified.
- [ ] Specify Part A outer construction, venue-session length/truncation, continuous replay/proof, rebuilt inner blocks, panel count and prescribed close-call expansion, exact positive depth, percentile/sanity definitions, RNG children and compute envelope. Preserve the separate post-F1 exact-depth approval.
- [ ] Materialize every monitoring variant definition: year/partial-year partitions, dependence, cost/fill/order perturbations, outages/missed entries, removals/ties, downside/co-loss, seam windows/counts, replica counts/budget, stream namespaces, missing-output treatment, statistics, cutoffs, severities and down-only or descriptive disposition.
- [ ] Resolve seam-index provenance explicitly; unavailable contract-month mapping is an unresolved definition, not permission to invent dates or omit the row. Any proposed scope amendment is separately reviewed before results.
- [ ] Complete predictive-interval method, infinity handling, effective-activation session clock and trigger/action definitions. Distinguish definitions due at F1 from quantities produced only by later n3.
- [ ] Confirm terminal-failure, ambiguous/void, budget exhaustion, privacy and one-attempt dispositions. Record exact future invocation interfaces/signing subjects without invoking mutating preflight or reserving an attempt.

Monitoring definitions are due before results; this slice does not execute the monitoring battery or add its diagnostics as qualification acceptance tests. Preserve existing scope unless an explicit amendment is approved.

## Task 5: Independent review and one decision-ready packet

- [ ] Trace each F01–F52 item through its actual producer, retained artifact, validator/consumer and required decision. Audit cross-document agreement against the preregistration and current implementation.
- [ ] Review the full workload accounting for omitted proof/verification/I/O costs, duplicated populations, nonrepresentative fixtures and hidden outcome-bearing measurements.
- [ ] Obtain independent combined review of production-input scope, non-decision-bearing measurement design/results and definition completeness. Fix material findings and recheck affected evidence/identities.
- [ ] Put unresolved operator choices first, with exact subjects and consequences of withholding. Keep administrative assembly agent-owned; no repeated request for already scoped approvals.
- [ ] Deliver a public-safe summary and private packet with source identities, accepted-input evidence, budget report, complete candidate definitions, signing/invocation preparation and remaining blockers. Mark whether the packet is ready for F1 review, never “frozen” or “qualified.”

## Environment, evidence and exit

Before project Python in the isolated checkout:

```powershell
.\fp.ps1 doctor
```

Run selected tests through `.\fp.ps1 python -m pytest`, standard operations checks through `.\fp.ps1 test-ops`, and required gates through `.\fp.ps1 check`. Resolve exact selections and benchmark invocations from the accepted code/help before execution; old direct-interpreter recipes are historical only. If PowerShell 7.3+ is unavailable, use the documented `python -I scripts/fp.py` bootstrap. Child Python uses `sys.executable`.

Doctor and arbitrary benchmark commands do not automatically create PR 413 verification records. Retain explicit benchmark reports with source/input/runtime identities, commands, interpreter, working-tree state, raw measurements and failures. For tests/gates, inspect automatic records/JUnit for completed capture, stable source and actual signing-case execution; disclose skipped inputs and pre-existing failures. Keep source stable during each measured or recorded run.

Successful exit means accepted production-input bindings, defensible complete workload accounting, every required definition concrete and the packet ready for the operator's F1 review. If evidence, model choices or measurement authority remain missing, deliver their exact blockers without calling readiness complete.

The next sequence is separate: actual F1 approval/validation → exact-depth subject from the frozen contract → separate ratification → authorized E1. This handoff neither performs nor preapproves it. Commit/PR publication follows the execution instruction's actual scope.

Drafting used existing preparation, integrated source and acceptance records. No private input acceptance, benchmark, parity run, qualification sample or approval occurred while drafting.
