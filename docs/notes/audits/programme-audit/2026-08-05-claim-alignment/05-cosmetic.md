# 05 — COSMETIC tier (both rounds)

**What this file is.** Every COSMETIC finding from the two-round post-de-scope claim-alignment audit that is **not** agent-facing — 45 rows over 71 findings, grouped so that remediation is **one pass per target file**. Round 1 contributes 48 findings / 31 rows (ported from the round-1 artifact's §5.6 and §5.7); round 2 contributes 23 findings / 14 rows. Index and combined counts: [`README.md`](README.md).

**COSMETIC means low consequence, not low precision.** Nothing in this file can cause a wrong deployment, a wrong clearance, or a real-money action — that is the whole content of the label, and it is the entire reason this tier is **deferrable to the 2026-11-08 checkpoint**. Every row is still specified to the exact edit, because an unspecified cosmetic item is what becomes a MISLEADING one at the next sweep.

**Evidential standing.** Round-1 rows (C1–C31) survived a refutation pass that killed 21.4% of what was raised and narrowed 53.6% of the survivors. Round-2 rows (C32–C45) come from surfaces never swept before and survived a pass that refuted 9.7% — **round-2 findings therefore carry slightly lower evidential standing**, for the reason stated once and plainly in [`README.md`](README.md). No row in this file is hedged on that ground individually; verify a round-2 quote at HEAD before editing.

**Nothing here is applied.** Every row is a **recommendation pending operator ruling**, except where a row is marked as already landed by one of the four FIXED/RULED commits below.

---

## Accounting, exact

**Round 1 (12 domains, anchored `e031225`).** §2's COSMETIC total is **64**. Of those, **62 carry an Algorithm adjudication** and **2 do not** — the latter are the unadjudicated pair (U1/U2 in [`06-operator-judgement.md`](06-operator-judgement.md) §3 — there is no section-09 (never authored)). Of the 62: **12 are already absorbed** into a MISLEADING row whose Action specifies the same edit, and are *not* re-rowed here (list below); **2 returned `KEEP-AS-IS`** and are routed to the operator-judgement section; the remaining **48 are rowed** — 35 findings / 20 rows in group A, 13 findings / 11 rows in group B. Verdict tally over all 62, countable rather than hand-tallied: **23 KEEP-CORRECTED · 20 SIMPLIFY · 17 DELETE · 2 KEEP-AS-IS.** The ±2 uncertainty §2 attaches to the Algorithm distribution therefore lives in the MISLEADING tier, not this one.

**Round 2 (7 previously-unswept domains).** 60 COSMETIC confirmed. **37 are agent-facing** and are carried in the agent-facing section, not here — they are consumed by an agent or operator mid-task, where a stale instruction causes an *action*, which is a different remediation priority even at identical severity. The **23 non-agent-facing** are rowed here as **C32–C45**. Verdict tally over the 23: **8 KEEP-CORRECTED · 7 SIMPLIFY · 8 DELETE.** Nine of the 23 are **load-bearing** — the text does real work (a dependency justification, a scope rule, a gate's stated coverage) — which is the sharpest available demonstration that COSMETIC is a consequence rating, not a precision one.

**Four of the round-1 items are the residue of the send-vs-arm cluster** (refutation ground 4) and are COSMETIC precisely because the executable gate `ops/c1_rail/c1_rail_arm.py::m1_acceptance_reason` already carries the amended bar.

---

## Status changes since the round-1 artifact — where they land in this tier

Three of the four fixes and the new proposed ADR change rows below. They are recorded here once, and the affected rows carry a bracketed marker rather than restating the commit.

- **B3 FIXED (`d84c5e4`)** — `cap_firm` → `cap_alloc[leg]` in `docs/spec/c1_watch_realization_multiplier_layer.md` §2 and `docs/spec/c1_nt8_sizing_host_impl.md` §2.2, plus the normative HALT property, the constants gloss, and the worked check (**9/67 → 8/60**, pre-split pair retained for provenance). Production was already correct and was **not** touched. Consequence in this tier: **C20** no longer has to say the illustration's base "no longer exists" — the spec now carries a live worked base, and `floor(8 × 0.50)/8 = 0.500` is **exact**, which is the concrete anchor that row was missing. **C21(a)**'s recomputation at 60 lots is the same post-split pair and is now consistent with the spec rather than ahead of it.
- **FU-1 RULED (`551d5c5`)** — operator: the venue will not be allowed to lapse; primary path is a deployed strategy, fallback is **ONE operator-submitted manual token trade by 2026-08-07**, recorded as a deliberate override of the book-composition §5 forbidden move. The rail does not move; no agent may place it. Consequence in this tier: **C2**'s closing instruction ("propose no account action … see O-C") is **superseded** — the choice it declined to make has been made. C2's Action is updated below and marked.
- **B2 FIXED (`a818b3f`)** — STATE.md's under-reaching 08-08 rider-enumeration command retired; the hard-core P-gate count is **five (P1–P5)**, not the four the round-1 artifact stated. No COSMETIC row restates that figure, so nothing here inherits the error — recorded so the next sweep does not re-derive it from this file's silence.
- **NEW ADR PROPOSED (`0af62ec`)** — [`docs/adr/2026-08-05-strategy-venue-binding-axis.md`](../../../../adr/2026-08-05-strategy-venue-binding-axis.md) adds a third orthogonal axis (BOOK → VENUE EDITION → DEPLOYMENT). It is the structural owner of the `venue_tradable` / `venue_note` fields that **C5(c)** and **C7** propose to reshape. If it is ratified, land those two rows in its shape rather than inventing a per-ledger tri-state convention; if it is not, the rows stand as written.

---

## Absorbed — do not open a second edit at these sites

Re-patching them individually is the accretion the M34 consolidation exists to reverse.

- `ops/prop_envelope_default.md` E6 → **M43**.
- `core/mc/preflight.py`'s measured idle bracket → **M41**, which says so explicitly (*"the bracket finding is the same continuous sentence — do not double-patch"*).
- `docs/briefs/2026-07-23-tradeify-book-composition.md` §4 H1 → **M34** item (2); §3's Tuesday-cap clause → **M34** item (4).
- `docs/notes/rail_build/RUNBOOK.md`'s hand-maintained *"2 of 6"* skew count → **M20**.
- `docs/spec/2026-07-27-third-leg-target-spec.md` S4's hedging bar → **M9**'s Status banner.
- `docs/adr/2026-07-23-c1-rung-selection-ev-objective.md` — the live-rung assertion **and** revert triggers 2/3 → **M28**.
- `docs/adr/2026-07-29-third-leg-symbol-occupancy-limb.md` §4 dormancy → **M29**.
- `docs/briefs/pre-registration/2026-08-03-c1-cadence-leg-preregistration.md` — the reader-intercept **and** C5's cap arithmetic → **M39**.
- `docs/briefs/2026-07-17-0808-packet-delta-and-sequence.md` §2 step 5 → **M30**.

## Grouping rule

**One row per target file, so remediation is one pass per file.** Where several findings share a file the row says how many and gives a **single consolidated action**; where a row is the residue of an M-row at the same file, it names the M-row that owns the rest and instructs a shared commit. Round-2 rows follow the same rule and continue the round-1 numbering (C32 onward) so cross-references from the round-1 artifact do not break.

---

## Group A (round 1) — root docs, instrument ledgers, and the constants / rules / gate estate

**35 findings · 20 rows.** Ported from the round-1 artifact §5.6.

| # | File · anchor | Class | Verdict | Action |
|---|---|---|---|---|
| C1 | `CLAUDE.md` — L5, L14, L23, §Protection L80 | premise-dead · dead-pointer | **DELETE ×3 · KEEP-CORRECTED ×1** | **Four findings, one always-loaded charter — and M1–M4 own four further sites in the same file, so land all eight in one commit.** (a) **L5** — the `§Multiplier System` pointer is dangling (no such section exists); replace with the charter's declared pointer form, `([substrate Phase 2](../../../../adr/2026-07-22-challenge-era-substrate-retirement.md) §2-D)`. (b) **L14** — delete the clause and its leading em-dash: *"— c1 GO **not** overturned — the deployed WATCH-1 0.50× rung is fully measured and passing (open B7 input CLOSED 2026-07-24; 0.50× bootstrap-95th 1.20% vs 3.0% ceiling)"*. The bullet then ends at *"US Treasuries untradable."* with its three ADR/RESULTS links retained. **Substitute no replacement sentence** — a rung is not "passing" against a venue that receives no deployment. (c) **L23** — delete both per-directory inventories (`portfolio_mc dd_protection … strategies` and `analysis codification research_utils discovery`); `REPO_MAP.md` is the declared owner and `codification` is retired. Leave the `ops/`, root-governance, `REPO_MAP` and `check_boundaries.py` clauses intact. (d) **§Protection L80** — *"**live: consumed by the c1 rail's sizing path.**"* → *"**wired: consumed by the c1 rail's sizing path**"* plus ⚠ the rail is built and **disarmed**, pointed at a de-scoped venue pending F2, so the law is unchanged and **unused** (§Purpose). Change-control is unaffected. **Touch no `dd_protection` literal.** |
| C2 | `STATE.md` — fork **F2** row (queue item 1) | gate-unreachable | KEEP-CORRECTED | **[Action updated 2026-08-05 — FU-1 RULED, `551d5c5`.]** Append to the F2 row, changing nothing already there and **not moving the 2026-08-08 date**: the account still carries `inactivity_max_idle_days: 5`; enforcement is a warning email followed by **irreversible deletion** (art. 12268494 — no pause facility, no reactivation, non-refundable); the last covered Mon–Fri week was **2026-07-27→31**, so **2026-08-03→07 is the first uncovered one and closes the day before this fork is due**; no warning had arrived as of 2026-08-02, so foreclosure is **possible, not scheduled**. Link `docs/superpowers/specs/2026-08-02-idle-rule-disposition-options.md` as an *input* (suspended per M36; its exposure statistics carry) so F2 and that spec are not double-booked. ⚠ **The round-1 instruction "propose no account action … see §5.8 O-C" is superseded and must not be carried forward.** The operator ruled on 2026-08-05: the venue will not be allowed to lapse — primary path is a deployed strategy, fallback is **one operator-submitted manual token trade by 2026-08-07**, recorded as a deliberate override of the book-composition §5 forbidden move. Record that ruling in the F2 row as the *branch-preserving* action, and record with it that **the rail does not move and no agent may place the trade**. O-C is discharged, not open. |
| C3 | `REPO_MAP.md` §2.2 L121 | premise-dead | **DELETE** | Delete L121 in full — the table row whose cells read `Codification emit` and `PYTHONPATH=lab python -m codification.emit …`, a runnable invocation of a module deleted 2026-08-02. This is one of the two unpropagated meta-layer removals §3.2 counts. Verify with `rg -n 'codification\.emit'` that only §1 L57 and §4 L151 remain, and **leave both** — they carry the dated RETIRED record and are the tombstone; no new file is owed. If house style requires the in-table strike form used at L119/L120, substitute a struck row reading `~~Codification emit~~` / `**RETIRED 2026-08-02** (operator ruling #3 / prune Packet D) — see §1 and §4` instead of deleting. |
| C4 | `ops/instruments/MNQ.md` — L105, N1 (~L67), N10 banner, L104 | premise-dead · stale-figure | **SIMPLIFY ×3 · DELETE ×1** | **Four findings, one ledger, one commit.** (a) **L105** — the c1 MNQ leg bullet sits under `## ACTIVE / OPEN`, which is false on its face. Move it to a new `## RECORD — c1 leg (withdrawn)` section immediately below, and reopen it: *"**c1 MNQ leg — WITHDRAWN from deployment 2026-08-04** (see Status above); retained here for the sizing/cap record … per-leg contract cap **11** (account-aggregate cap allocated per leg 2026-07-22; `LEG_MAP` untouched by the de-scope — this is code truth, **not** a live reservation against a third leg)."* Rest byte-unchanged. Apply the identical two-step to `MYM.md` (C5). (b) **N1** — strike *"**Tradeify — the live account**"* and read *"**Tradeify** ($0.91, `rt_pt` 1.41) — former deployment venue, de-scoped 2026-08-04, no successor registered (fork F3 open; see session log 2026-08-04d) — FAILS it on the Stage-6 limb"*. **Add no new session-log entry** — 2026-08-04d already records the void, and a second is the duplicate-record failure. (c) **N10 banner** — *"The bank of 2 is still real …"* re-states the same mutable counter M2 deletes from the charter; replace the figure with `~~2~~ **(figure removed — re-derive from `discovery_manifests/`; it has moved since this banner was written)**` and **substitute no new count** — the DEAD-list rows below already log every increment with its date and manifest. (d) **L104 `ORB-MNQ-1` bullet** — rewrite to current lineage: `CANDIDATE @ 1.00×`, PARKED (unparked 2026-07-31, re-PARKED 2026-08-03); payable-standalone-Tradeify-leg target **FALSIFIED** (§4 T2 FIRED), scoped to **one target at one firm** — lifecycle not demoted, K not spent, mechanism not rejected; manifest closed 2026-08-04 operator-stopped, `executed_k=0` (a manifest fact, not a lifecycle one); rail integration needs F2 ruled and a successor venue under F3 before any operator GO. The `NQ.md` twin of this bullet is **M21**'s. **Do not touch the preserved "Original text" block in N10.** |
| C5 | `ops/instruments/MYM.md` — L94, M6, PROFILE | premise-dead | **SIMPLIFY ×1 · KEEP-CORRECTED ×2** | **Three findings, one ledger, one commit; pairs with C4.** (a) **L94** — move the c1 MYM leg bullet out of `ACTIVE / OPEN` into the durable-record section and relabel the lead so header and label agree: *"**c1 MYM leg — venue edition, WITHDRAWN from deployment 2026-08-04 (see Status, L4). Record only; nothing here is open.**"* — WATCH-1 0.50×, per-leg cap **69**, expected sizing 8 base / 60 add, hedging rule clears by construction (long-only), disposition fork **F2**. If `ACTIVE / OPEN` then empties, write `None open — see Status.` rather than deleting the header. (b) **M6** — the 4× Tradeify cost hurdle (**≈ 6.57 bp/event** on MYM) stays as the working screen but must state its basis at admission: append ⚠ 2026-08-04, Tradeify is no longer the programme's binding venue (successor is fork **F3**), so re-price against the elected venue once F3 rules — friendly-firm per-side costs span **$0.50–$0.95**, so the hurdle moves by well under 2×. Leave the mirrored PROFILE `basis` field alone; it is already cohort-labelled. (c) **PROFILE** — add a `venue_note` recording that the locked Striker DJ30 MYM edition occupies the cell as an **`AUTHORIZED`-but-venue-less incumbent**, with no live book to correlate new work against, and set `venue_tradable: false` to match the `XAUUSD`/`NAS100` convention; in the PROFILES generator change the LIVE consult text to *"an authorized leg occupies this cell; if a live book exists, new work must clear the book-correlation gate — check `venue_note` first, as an authorized leg may have no venue"*; regenerate `PROFILES.md` and `profiles.json` in the same commit. **Do not change `verdict: LIVE` and do not touch lifecycle state** — the axis is deliberately unmoved. |
| C6 | `ops/instruments/*.md` — the `**Last updated:**` header field (20 ledgers) | stale-figure | **DELETE** | Four headers are stale against their own newest session entry (`MYM` → 2026-08-04, `M2K` → 2026-08-04, `NQ` → 2026-08-04, `NAS100` → its `venue_note` date or later). **Delete the field rather than re-syncing it**: it is a hand-maintained mirror of a value the session log already owns, and it has drifted at four of twenty. Two ledgers carry extra payload on the same physical line and must be split first — `ops/instruments/USOIL.md` (canonical panel + retired-seed note) and `ops/instruments/XAUUSD.md` (canonical Guardian baseline panel) — moving that payload to its own `**Canonical panel:**` line before the date is removed; `EURGBP.md` and `USOIL.md` also carry parenthetical prose that moves to the session log. Add one line to the ledger shape under Rule 10: *"Ledger currency is read from the newest SESSION LOG entry; do not restate it in the header."* **If the operator wants a header date kept, GENERATE it** — `scripts/instrument_profiles.py --check` already runs in CI, so derive `last_updated` from the newest session-log date there and it cannot drift. Hand-maintaining it without a gate reproduces this finding at the next sweep. |
| C7 | `ops/instruments/ZF.md` · `ZB.md` · `ZN.md` — Status line, W3, PROFILE | overstated-modality | **KEEP-CORRECTED ×1 · DELETE ×2** | **Three Treasury ledgers, one commit, one substitution.** Each states Treasury untradability as a standing bar; it is a **Tradeify product-set fact at a de-scoped venue**, so programme-level tradability is **UNDECIDED, not barred** — availability at Bulenox / MFFU / BluSky has never been checked in these ledgers. Move the fact out of the Status line into `W3` / `venue_note`, retained verbatim as the dated record with *"⚠ 2026-08-04 — Tradeify de-scoped; no successor venue elected (fork F3). This remains an accurate fact about Tradeify, not a programme-level bar."* appended. Status becomes *"**Research venue only — no strategy, no allocation, no live exposure.** (Venue tradability: see W3 and `venue_note`.)"*. Machine-read PROFILE: `venue_tradable: false` → `undecided` (or the ledger's nearest tri-state), with the attribution carried in `venue_note`. **Record each Status-line deletion in the file's amendment log** — deletions are recorded, never silent. `ZB`'s gate on the Treasury-complex tail-exhaustion finding (`ZN.md` R1) is independent of all of this and is unaffected. ⚠ **Check the binding-axis ADR first** (`0af62ec`, Proposed): if ratified, venue tradability is a VENUE-EDITION-layer property with an owning schema, and the tri-state lands there rather than as a per-ledger invention. |
| C8 | `ops/instruments/M2K.md` — M2 `basis:` field | stale-figure | SIMPLIFY | **M24 owns this file's Status line and `venue_note`; this is the separable `basis:` field** — the machine-adjacent cost row an M2K feasibility screen actually reads. Replace with the recomputed panel-era figure plus a pending flag: *"panel-era median 1,897.20 ⇒ **11.89 bp/RT** (recomputed 2026-07-28; supersedes the illustrative index-2300 figure and the withdrawn 10–30× margin claim). ⚠ Multiplier basis **PENDING** — the 4× hurdle was a Tradeify figure and Tradeify was de-scoped 2026-08-04 with no successor elected (fork F3, 08-08). Friendly-firm per-side costs span $0.50–$0.95; re-price against the **elected** venue before this row gates anything."* Land inside M24's commit so `PROFILES.md` / `profiles.json` regenerate exactly once. |
| C9 | `ops/instruments/NAS100.md` — Status line | contradiction | SIMPLIFY | M25 instructs *"align the `LIVE` Status word to `XAUUSD.md`'s `DORMANT — no live venue` wording in the same pass"*; this supplies the exact wording so the instruction is not left to the reader — **one edit, not two.** Status → *"**AUTHORIZED · MECHANISM @ 1.00× — no live venue.** Striker NAS100 v1 LOCKED 2026-05-05 (long-only; Mon/Tue 13:00–17:00 UTC; 'pyramid IS the strategy' — base-only PF 0.31). Venue state is owned by the PROFILE block below (`venue_tradable: false`); the FXIFY/DXTrade CFD venue closed 2026-07-10."* Keep it word-for-word parallel to `ops/instruments/XAUUSD.md` so the two dormant-incumbent ledgers read the same. **Do not touch `core/lifecycle.py` or the 1.00× tier** — the lifecycle axis is unmoved by design. The superseding event here is the 2026-07-10 CFD closure, **not** the de-scope; the PROFILE eleven lines below already reconciles it, which is why this is COSMETIC. |
| C10 | `ops/instruments/NQ.md` — N6 row title | premise-dead | KEEP-CORRECTED | M21 owns this file's Status, ACTIVE/OPEN, N1, N7 and ORB row, and explicitly rules **do not strike "account-multiplier layer"** — it is the standing ratified layer name. This is the residue: the row **title**. Retitle *"**c1 leg operational facts:**"* → *"**MNQ venue-edition facts (leg withdrawn from deployment 2026-08-04; findings below are venue-independent):**"* and leave the body byte-unchanged. If a concrete anchor is wanted, append *"(realized in `ops/c1_rail/c1_sizing_host_reference.py`)"*. Land in M21's commit. |
| C11 | `docs/operational_rules.md` — Rule 6 trigger + checklist; Rule 11 L425–426 | gate-unbinding | KEEP-CORRECTED | **Two meta-layer rules, one commit — this is the rule-text half of §3.6's "the staleness class has no owner".** (a) **Rule 6** — extend the trigger beyond version and constant changes to *"**or when a decision changes DEPLOYMENT SCOPE — venue targeting, what is deployed where, or whether an execution surface exists (a scope event)**"*, and add a checklist item 5 naming the surfaces the 08-04 hand-authored §6 list missed: `docs/spec/` (absent from the rule entirely, and the home of screening specs that orphan silently), `docs/briefs/INDEX.md`, `ops/instruments/*.md` **including parent ledgers**, `STATE.md`, `docs/notes/rail_build/`, and `core/**/*.py` **module docstrings** (values are governed; provenance prose is not). Specify it as a **grep, not a hand-authored list** — the 08-04 §10 hook 3 is the right shape and was scoped to four files. Trigger stays event-based, never calendar. (b) **Rule 11 L425–426** — widen *"each affected prior ADR"* to *"each affected prior **artifact, whatever its class**"* (ADR, `docs/spec/` standard, brief, closure, `ops/instruments/` ledger, desk card, `STATE.md` board row), **placed at its reader-intercept position per Rule 14**, and record the standing gap plainly: `check_falsifier_reachability.py` and `check_adr_graph.py` scan `docs/adr/` only, so **non-ADR intercepts are an author obligation with no gate behind them.** Cite the 2026-08-04 de-scope as the second origin incident alongside the 2026-07-01 audit. The Rule 7 owner-table row this pairs with is a governance choice — operator-judgement row **O-F**. |
| C12 | `core/firm_rules.py` — module docstring PROVENANCE block; BluSky Premium rows; cadence-study path comment | premise-dead · stale-figure | KEEP-CORRECTED | **Three findings, comments only. No encoded value changes anywhere in this row** — a firm-rules value change needs an ADR plus the engine pre-flight (`CLAUDE.md` §Firm Expansion). (a) **PROVENANCE block** — *"Live c1 uses the explicit tier key `Tradeify_Select_100K`"* → the rail *config names* that key (there is no `ACTIVE_FIRM`; callers pass a tier key explicitly), followed by `NOTE 2026-08-04:` that venue is de-scoped as a deployment target **evaluation included**, both Striker legs withdrawn, rail disarmed, disposition fork F2 — **the key is retained because no code changed; it is not a live deployment tier.** Widen the 08-04 ADR's §10 hook 3 to include `core/firm_rules.py` and `ops/instruments/*.md` in the same pass. (b) **BluSky Premium rows** — insert a `# PHASE SCOPE (re-verified 2026-08-05)` block recording that `micro_contract_cap` encodes the **Evaluation/Buffer** cap (100 micro / 10 mini at 100K; 50/5 at 50K) while Sim Funded / Live is roughly **half** (Propel Premium $100K = 5 mini / 50 micro; Orbit funded 3/30), so any funded-capacity or composition-fit read of this field is **overstated ~2×**. Confirmed, **not re-encoded** — whether funded rows are onboarded is operator-judgement row **O-H**. (c) **Cadence-study path comment** — `lab/analysis/c1_cadence_inactivity_2026-08-02` → `lab/analysis/c1/c1_cadence_inactivity_2026-08-02`, broken by the 2026-08-03 theme nest. The durable fix is widening `scripts/check_path_liveness.py` to scan path literals inside `core/**/*.py` and `ops/**/*.py` comments — the nest broke at least two pointers in this finding set and nothing mechanical caught either. The `cost_per_side_usd` value question is operator-judgement row **O-G**. |
| C13 | `core/mc/preflight.py` — L139 comment + `ValueError` message body | dead-pointer | SIMPLIFY | M41 and M42 own the `inactivity_rule_sourced` docstring and the token-trade sentinel; this is the separable **dead pointer**, present twice. Both occurrences of the stale `OPEN DEFECT` text must read: *"See the BluSky INACTIVITY RULE — SOURCED block in `firm_rules.py` and ADR `2026-08-05b-blusky-inactivity-rule-sourced` (which supersedes the 2026-08-05 containment ADR)."* **Change no condition, no threshold, no control flow, and no `firm_rules.py` value.** Verify: `rg -n 'OPEN DEFECT' core/` returns zero and `python -m pytest tests/core/ -q` is green. Land inside M41's paragraph edit so the file is opened once. |
| C14 | `core/dd_geometry.py` + four mirror sites | premise-dead | KEEP-CORRECTED | **One commit, five sites, one substitution — the other unpropagated meta-layer removal §3.2 counts, and the same dead script M6, M7 and M25 each hit independently.** Use `core/csv_parser.py:12` as the house phrasing template: *"(`ast.Constant` RHS; `scripts/verify_lock_anchors.py` requires that shape — the `validate_params` hub retired 2026-08-03 with `params.toml`, ADR `2026-08-03-params-toml-gate-retirement`)"*. Sites: (1) `core/dd_geometry.py`; (2) `PIPELINES.md:155`; (3) `README.md:61`; (4) `ops/instruments/NAS100.md:6`; (5) `docs/operational_rules.md` Rule 7 owner table — **delete** the row whose cells read `Derived parameter mirror (non-canonical)` and `core/config/params.toml (labeled)` (verified: `core/config/` does not exist). **Touch nothing in `dd_protection.py`, no constant, and no `POLICY_REGISTRY` entry.** ⚠ `PIPELINES.md:155` is also named by **C32** item (e) — land it once, in whichever commit opens `PIPELINES.md` first. |
| C15 | `ops/c1_rail/c1_rail_listener.py` — `INSTRUMENT_SYMBOLS` docstring (~L25–28) | overstated-modality | SIMPLIFY | Replace with the cohort-bound form: TV continuous-contract notation (`MYM1!`/`MNQ1!`) matching `tests/rail_crosstrade/fixtures.py`, **VERIFIED on live orders at the B4a dry-fire 2026-07-20** against CrossTrade → Tradovate on the Tradeify Select 100K eval — both symbols accepted and auto-translated to `MYMU6`/`MNQU6`, no code change needed (RUNBOOK §B6 items (3) and B4a) — with two standing consequences stated: dated-contract translation implies a **quarterly roll obligation** on any future armed session (§B4 note (e)), and the verification is **bound to that broker pair, so re-verify the ticket format against any successor broker elected under fork F3.** The cohort binding is mandatory, not optional: an unqualified "VERIFIED" over-generalizes a Tradovate-specific result. |
| C16 | `scripts/check_supersession_placement.py` SCOPE docstring · `tests/governance/test_supersession_placement.py` | gate-unbinding | KEEP-CORRECTED | **The COSMETIC residue of §3.1's 6.8% finding — the docstring and the self-test, not the glob repair itself.** Widen `in_scope()` from `lab/analysis/*/RESULTS*.md` to `lab/analysis/**/RESULTS*.md` and, in the same edit, correct the SCOPE line to say so and why (`**` reaches the 2026-08-03 theme nest `lab/analysis/<theme>/<slug>/`; `lab/archive/` stays excluded as LTM). **If the single level is deliberate after all, declare that instead** — an accurate narrow scope is acceptable; an overstated one is not. Append to the existing *"What this deliberately does NOT catch"* paragraph: *"a file with NO addendum heading at all — `scan_file` returns early, so a document that simply never corrects itself (e.g. a mooted desk card) scans clean. Missing-intercept-entirely is a human obligation under Rules 11/14, not a gated one."* — the exact hole **M17** and **M11** both fall through. Then add, in the same commit, a selector test the corpus assertion at `:96` cannot pass by covering almost nothing: assert `len(in_scope(REPO)) >= 60`; assert some path contains `/analysis/orb/` (theme nest reached); assert some path contains `DESK_CARD`; assert none contains `/lab/archive/`. Tune the floor to the post-widening count minus a margin. |
| C17 | `scripts/check_falsifier_reachability.py` — numeric census block in the docstring | stale-figure | SIMPLIFY | Delete the hand-maintained census and replace with a direction statement plus an instruction to run the tool: *"COVERAGE IS A MINORITY OF FALSIFIERS, AND ERODING. Run `--stats` for the current census — do **not** hand-maintain figures here."* Record both readings inline as the evidence for "eroding": 2026-08-02 read **69 sections / 19 runnable = 28%**; 2026-08-05 read **83 / 21 = 25%** — the corpus added ~14 falsifier sections while anchored coverage rose by 2. Keep the closing caveat exact: a green run means *"no NAMED input is missing"*, never *"all falsifiers are in force"*. **Leave the BLIND-TO-RETIREMENT and WARN-TIER paragraphs byte-unchanged.** |
| C18 | `scripts/check_root_doc_liveness.py` — `DEFAULT_DOCS` scope vs `docs/spec/` | gate-unbinding | SIMPLIFY | **No edit to the script.** `DEFAULT_DOCS` states that gate's scope accurately, and §3.6's separate complaint (it resolves markdown links only, so dead paths inside backtick spans pass green) is a different defect. The finding here is that **`docs/spec/` has zero gate coverage of any kind**: 10 of its 28 files carry no `**Status:**` field at all, which is why M9 and M35 were discoverable only by hand. Remedy without a new checker: (a) add a dated `**Status:**` to those 10 using the `docs/briefs` vocabulary (`OPEN` / `LOCKED` / `SUPERSEDED` / `DORMANT`); (b) run a one-time triage dispositioning all 28, beginning with the two this audit retires (`2026-07-27-third-leg-target-spec.md` → DORMANT per M9; `2026-08-02-tradeify-activity-rule-disposition-spec.md` → CLOSED/MOOT per M35) **and the two round 2 adds at C41** (`pine_baseline_csv_format.md`, `2026-06-13-codifier-breakout-longshort-trailing-extension.md`). **Defer any new checker explicitly**, re-evaluating at the 2026-11-08 checkpoint only if (a)+(b) have drifted — and **record the deferral as a decision, not a backlog row.** Adding a gate here would be the ninth in a window §3.2 already scores net **+9**. |
| C19 | `scripts/validate_c1_monitoring_acceptance.py` — the M1 NOTE string | contradiction | SIMPLIFY | Fourth surface in the send-vs-arm cluster. *"NOTE: M1 is not RESOLVED — next `dry_run=false` entry/add **send** remains gated."* → *"NOTE: M1 is not RESOLVED — **arming** is gated. The trigger is the ARM, not the send (ADR Addendum 2026-07-31b); enforcement lives in `ops/c1_rail/c1_rail_arm.py::m1_acceptance_reason`, not here."* **Do not restate the invariant's text again in this file** — the duplication is why four surfaces drifted together. Land with **M16** and the `M1_MONITORING_ACCEPTANCE.json` note (**C22**). |
| C20 | `docs/methodology/strategy_lifecycle.md` — L113 parenthetical; MVD spec-pin anchor (3 sites) | stale-figure | **SIMPLIFY ×1 · KEEP-CORRECTED ×1** | **Two findings; M26 owns the two dangling sizing-host links at this file and this is the residue — one commit.** (a) **L113** — beyond M26's path fix, demote *"What realizes it today is …"* to *"The layer that **would** realize it is …"* (nothing realizes it today), and correct the parenthetical: the flooring error is **base-dependent** — exact at even reserve bases, lossy at odd ones — so it must be evaluated at the leg's actual `cap_alloc`-derived base. **[Updated — B3 FIXED, `d84c5e4`.]** The round-1 instruction "not against the fixed `floor(9 × 0.5)/9 = 0.444` illustration, whose base no longer exists" now has a live substitute: `docs/spec/c1_watch_realization_multiplier_layer.md` §2 carries the post-split worked check **8/60**, at which `floor(8 × 0.50)/8 = **0.500` — exact**. Cite that pair, and state that the 9/67 figures are retained in the spec **for provenance only** and must not be quoted as current. (b) **MVD spec-pin anchor** — §3.7 flagged `dd_protection.py:176/181` as a **stale citation, not drift**; those lines now hold `_validate_state`'s multiplier guard and the pin itself passes. Three sites, three mechanisms per house rules: in this living doc, cite by **literal** (`if DD_TRIGGER != 0.015:` / `if DD_SCALE != 0.40:`, currently ~L292/L297) so it cannot re-stale; correct the §10 hook comment in `docs/adr/2026-07-10-strategies-never-locked-lifecycle-governance.md` **in place with a recorded note** (ADR 2026-08-04's own §10 set that precedent); and for the same ADR's L235 **body prose, append a one-line addendum rather than editing it.** **Make no edit to `core/dd_protection.py` or `core/lifecycle.py`.** |

### Late placement — two MISLEADING rows lived here and are NOT in this file

The round-1 artifact placed **M44** (`.claude/skills/brief-authoring/references/adr.md` — §7 Phase 2 + the L329–330 acceptance bar) inside §5.6 and **M45** (`lab/analysis/c1/c1_thirdleg_instrument_map_2026-07-27/RESULTS.md` — the K columns and the E-K / E-KCAP eliminations) inside §5.7, after both failed the artifact's file-string placement match and were nearly lost. **Both are MISLEADING-tier and are carried in the MISLEADING section, not here.** They are named at their exact anchors so that a reader working the COSMETIC tier from the round-1 §5.6/§5.7 line numbers cannot lose them a second time. **M45 shares a study directory with C31** — land them in one commit.

---

## Group B (round 1) — briefs, specs, closures, pre-registrations, rail-build artifacts and research bodies

**13 findings · 11 rows.** Ported from the round-1 artifact §5.7.

**This group is thinner than group A, and the reason is structural rather than a coverage gap.** This estate was swept hardest at MISLEADING — ten rows over it there and four more in the rail/monitoring group — and those rows consolidate by document, so most of the COSMETIC residue was already absorbed (the absorbed list above names nine of the twelve). What survives is two rail-build artifacts, one dated note, six brief / closure / pre-registration surfaces, one ADR trigger line, one design spec and one research body.

| # | File · anchor | Class | Verdict | Action |
|---|---|---|---|---|
| C21 | `docs/notes/rail_build/RUNBOOK.md` — L318 "Why"; Stage 2b heading | stale-figure · premise-dead | SIMPLIFY | **Two findings at §Stage 2b. M13–M16 own the rest of this file and M20 owns the skew comment — land all six in one commit.** (a) **L318** — the Q-COSTGEO-3 add-liquidity figures predate the cap split. Recompute at the post-split size of **60 lots** (the 2026-07-22 `cap_alloc` split moved entry 9→8 and add 67→60): **~12.0×** median displayed depth, still clearing the inside on **0 of 61** historical add-moments — the verdict is unchanged *a fortiori*, since 60 already exceeds the maximum inside depth of 30. State that the original 67-lot / 13.4× figures **must not be re-quoted as current**, and add `⚠ **STRANDED 2026-08-04**` — Stage 2b needs a real strategy-signal add fill, and ADR §6 names it among the five threads with no live source anywhere in the estate; the landed harness and the panel baselines transfer unchanged to an F3 venue. **Leave the baseline table, the cohort-split mandate, the market-order rule and the mbp-10 decline byte-unchanged.** *(The 8/60 pair is now the spec's own worked check per B3, `d84c5e4` — this row is consistent with it, not ahead of it.)* (b) **Stage 2b heading** — the queue-status word belongs to `STATE.md`, not the RUNBOOK. Retitle to *"built 2026-07-24; Q-COSTGEO-3 follow-on — queue status owned by `STATE.md`, currently **STRANDED**: its input, a live strategy-signal add fill, has had no route since the 2026-08-04 venue de-scope"*. Keep `built 2026-07-24` — that is a rail fact the RUNBOOK owns; only the forward-disposition word moves. Then sweep the file's other stage headings for `QUEUED` / `OWED` / `PENDING` and replace each with a `STATE.md` pointer in the same commit. |
| C22 | `docs/notes/rail_build/M1_MONITORING_ACCEPTANCE.json` — `notes[2]` | contradiction | **DELETE** | *"Next `dry_run=false` entry/add **send** remains gated on RESOLVED."* names the **send** as the gate's object; Addendum 2026-07-31b moved the trigger send → **arm**, so setting `dry_run=false` is itself barred while `status != RESOLVED`. **Delete the note; do not correct it in place** — a corrected send-shaped sentence is still a fifth copy of an invariant that has one owner. Append a dated note recording the deletion, the owning ADR, and that enforcement lives in `ops/c1_rail/c1_rail_arm.py::m1_acceptance_reason`, which reads only this artifact's `status` field. Add one clause that item 5 is undischargeable **while** no strategy is deployed to emit a signal — **contingent, not a permanent property** (the calibration exemplar named in the header, not repeated). `notes[1]`'s bar on advancing status is unchanged. **Do not change `status` (`CODE_LANDED`) or any event id.** Land with **M20** (same file's `fixture_hashes` note), **M16** and **C19**. |
| C23 | `docs/notes/2026-08-04-databento-entitlement-inventory.md` — L73, L91 headings | premise-dead | **DELETE** | Two section headings read `### MNQ (live c1 leg)` and `### MYM (live c1 leg)`. Delete the four-word labels and append one dated correction line at the foot recording that both were removed under ADR 2026-08-04 and that **no cost figure, window or implied rate changes — every measurement in the note is venue-independent.** **Touch no table cell.** Listed despite being the cheapest item in the audit because the note is itself dated **2026-08-04**: it was authored the same day the labels became false, which is the clearest single instance of §3.2's finding that the prune was ruled and did not reach the writing hand. |
| C24 | `docs/briefs/pre-registration/2026-08-04-ict-1m-execution-mnq-preregistration.md` — §3 L182–183 + §0 L38 | premise-dead | SIMPLIFY | The binding-basis note justifies the Tradeify $0.91/side basis as *"the live account" / "the account that actually trades"*. **The basis survives; the justification does not.** Retain $0.91 because it is the **most adverse index-micro basis on file** — the conservative choice, and venue-independent — and record ⚠ 2026-08-04 that Tradeify is de-scoped in both phases, no account trades anywhere, and the successor venue is fork **F3**. Add the forward bar: **if option B is elected, the binding basis, F1's 4.0× gate and FM-5 must be re-keyed to the elected venue BEFORE this document freezes.** ⚠ **Flag, not a ruling:** §2.3's screen is stated as failing at both bases (+0.890 Bulenox / +0.835 Tradeify) against a **0.980** floor — but 0.980 is a family DSR floor from the K-ladder ADR 2026-08-04 retired, and **M10** replaces that table with `floor_at_k(K_intrinsic)` = **0.650**. Whether §8's NO-GO survives re-pointing to the current floor is **not adjudicated here**; it must be checked against `lab/research_utils/axis_screen.py::floor_at_k` before this pre-registration is quoted as settled. **Do not freeze, sign, or bind K until it is.** |
| C25 | `docs/briefs/rnd-pipeline/Q-FUNNEL-1-contract-funnel-ev-scoping.md` · `docs/briefs/closures/Q-FUNNEL-1-closure-resolved.md` | contradiction · premise-dead | **SIMPLIFY + DELETE** | **Both ends of one question, one commit.** (a) **Scoping brief** — Status reads `OPEN` and a `Pre-Lock Checklist (DRAFT — remove once locked)` block is still present, against a closure dated 2026-07-22. Set `**Status:** \`CLOSED — RESOLVED\`` with authored/closed dates, the closure link, and the one-line finding (funnel EV varies materially; half the evidence horizon-fragile); **delete the checklist block in full** — it names its own removal condition and that condition is met; and either populate §8's pre-registration hash (`b430a59`) and date **or** delete the placeholder lines. **Leave no empty placeholder.** Verify with `python scripts/check_closure_disposition.py`. (b) **Closure** — append one dated reader-intercept at the head, body unedited: the **2026-08-08 D1 rung-selection routing is VOID, not deferred** (there is no live rung to select, and the funded-payout upside that makes funnel-EV prefer 1.00× sits at the de-scoped funded phase), so **no decision item is owed at the 08-08 checkpoint** — and note it would not have surfaced regardless, since 08-08 riders are enumerated from `docs/adr/` and a closure is not an ADR. The surviving finding (funnel-EV and a bust-denominated floor are different objectives) is reusable F3 framing. The *"WATCH-1 0.50× remains the ratified configuration"* line refers to the GO ADR whose deployment limb is superseded per **B1** — **now FIXED (`ae5ffe7`): the GO ADR carries reciprocal supersession edges and a dated Addendum 2026-08-04, so the intercept may cite it rather than assert it.** |
| C26 | `docs/briefs/closures/Q-COMPOSE-1-closure-falsified.md` — §c1-disposition bullet | premise-dead | KEEP-CORRECTED | Append **one sentence to the existing reader-intercept at L3–5** — do not add a second banner, do not touch the body. The bullet's *"is now the **sole** deployable path for the c1 book"* describes the state at closure date; both Striker legs were withdrawn 2026-08-04, the book has **no deployment at any venue**, and the rail is retained and disarmed pending F2. The load-bearing half of the edit is the disclaimer: **the composition verdict and "Nothing re-opens" are unaffected** — variance dominance is a property of two return series against a fixed barrier, not of the venue they were pointed at. Without that clause the banner reads as reopening a falsified question, which is the over-reach refutation ground 5 catalogues. |
| C27 | `docs/briefs/closures/Q-CAPALLOC-2-closure-resolved-fragile.md` — §4 reopen precondition (b) | gate-unreachable | KEEP-CORRECTED | Insert **above §4** — Rule 14 placement, not a bottom addendum — with the body byte-intact: precondition (b) is **unsatisfiable**, because B7-REFIRE was mooted with the venue de-scope (ADR §6) and there is no live fill source anywhere in the estate; §4 governs a `LEG_MAP` allocation at an account no longer deployed to, and **does not bar a fresh cap-allocation derivation at a successor venue under fork F3**, which would carry that tier's own contract cap and need its own forward check rather than post-B7 Tradeify fills. **Do not restate the 51/29 or 69/11 figures inside the banner** — restating a superseded split within a correction is exactly how M2 and M3 came about. **Do not touch `LEG_MAP`** in `ops/c1_rail/c1_sizing_host_reference.py`; it is a governed surface the ADR deliberately left alone. |
| C28 | `docs/briefs/closures/Q-BOOKFIT-1-closure-resolved.md` — fork-priority table | premise-dead | **DELETE** | Append **one** dated reader-intercept at the head; make no in-body edit. The fork-priority table is **SPENT — nothing rides the 2026-08-08 packet from this closure.** All three forks closed `SCREEN-FAIL` on 2026-07-20 on the edge axis the projection explicitly did not test: **H-ZNAUC-1** (cost wall, δ 1.01 bp against a 6–10 bp hurdle), **H-FCCARRY-1** (effect absent, annSR 0.092), **H-FBEIA-1** (informed flow, δ −1.16 bp). State that the closure's risk-geometry verdict is **unaffected and not withdrawn** — geometry passed, edge failed, exactly as its own scope caveats anticipated. **Do not add a second banner about the $273.28 σ basis**: that premise is dead too, but M9 owns the σ-derived estate and a second banner here duplicates it. Note the timing honestly in the banner — recorded **2026-08-05** about events of **2026-07-20**. |
| C29 | `docs/adr/2026-08-04-firm-rules-eval-lock-fix-applied.md` — T2 trigger line | overstated-modality | SIMPLIFY | The T2 line reads as a scheduled cadence and is booked on **no board row**, so it convenes nothing and will be silently missed. Rewrite it as what it is — a **use-gated staleness bound**: the Tradeify eval-lock rows were verified 2026-07-22 and go stale **2026-10-20** under `ops/prop_envelope_default.md`'s *"a row older than 90 days is stale and must be re-verified before use"*; **this is a bound on use, not a calendar cadence — no board row convenes it**; the convening hook is the quarterly programme audit. Optionally fold the ~10-20 bound into `STATE.md`'s existing 2026-10-11 overlay-re-verify row so the two per-row clocks share one board entry. This is a trigger-schedule line, **not §2 decision text**, so an in-place amendment is the lighter option; if the operator treats the ADR's schedule as frozen, land the identical text as a dated addendum instead. |
| C30 | `docs/superpowers/specs/2026-07-30-tradeify-native-fade-program-design.md` — §0 anchor row | contradiction | KEEP-CORRECTED | The §0 anchor records the `dd_lock_offset_usd` defect as FOUND 2026-07-22 and open; it was **APPLIED 2026-08-04** (all six tiers now encode `1_000_000.0`, i.e. unreachable ⇒ pure fixed-$ eval trail) — and this spec's §3 numbers **already assume the corrected geometry** (see the Stage-2 gate's *"Run-2, corrected geometry"* and the 4.74% vs 3.0% citation), so the anchor row contradicts the body it anchors. Correct the row to `APPLIED 2026-08-04` and append one dated line below the §0 table: the recorded upstream-drift check was performed 2026-07-30 and `core/firm_rules.py` has changed **twice** since — 2026-08-04 (eval-lock fix applied) and 2026-08-05 (BluSky inactivity rule sourced + unit-corrected) — so **re-run the drift check before any Stage-2 execution.** **Touch nothing in §3 or the Stage-2 gate.** |
| C31 | `lab/analysis/c1/c1_thirdleg_instrument_map_2026-07-27/RESULTS_stage2.md` — Slot-1 primary surface | orphaned-scope | KEEP-CORRECTED | Insert a banner under the Status line, **editing no measurement cell**: the Slot-1 (`wed_thu`) restriction is derived from incumbent occupancy of a **deployed** two-leg book; both legs were withdrawn 2026-08-04, and per `ops/instruments/MYM.md` (2026-08-04) the symbols are **not thereby released** — that is fork **F2** (2026-08-08), so **until F2 is ruled the primary surface still governs.** Name what re-derives *if* F2 releases them: the wider `all_days` surface already measured below (N=450), the 0.0924 operational Clause-N floor, and the M6A COST→POWER flip. **The banner resolves nothing; it names what re-derives** — which is the whole of operator-judgement row **O-B** applied at one file. Land in the same commit as **M45** — same study, same missing-intercept class. |

---

## Group C (round 2) — root docs and build, CI workflows, scripts, specs, and the RESULTS corpus

**23 findings · 14 rows.** The seven round-2 domains contributed 60 COSMETIC findings; the 37 agent-facing ones are in the agent-facing section. These 23 are the reader-facing residue.

**Two boundary notes, stated once.** (1) Two rows below sit under `.claude/` — **C43** and **C44** — and are here rather than in the agent-facing section because the surveyor classified them non-agent-facing on inspection: C44's permission entry has granted nothing since 2026-06-05 and so instructs no agent, and C43 is teaching prose in a worked example, not a directive. If the operator disagrees with either classification, move the row; nothing else changes. (2) **`PIPELINES.md` alone carries five findings.** It gets **one row and one commit** (**C32**) — the M34 pattern. Patching those five individually is the accretion this pass exists to reverse, and one of them (the L3 self-exemption) is *false until the other four land*, so a split commit ships a document that contradicts its own header.

| # | File · anchor | Class | Verdict | Action |
|---|---|---|---|---|
| C32 | `PIPELINES.md` — L3, L14, L15, L72, L121, L155, L160 | contradiction · premise-dead · stale-figure | **KEEP-CORRECTED ×3 · SIMPLIFY ×2** | **Five findings, one file, ONE commit — and the ordering matters: L3 is FALSE until the body edits land.** (a) **L3** — the absolute *"**Posture is not restated here**"* is falsified by the file's own Status column (L11–17) and by L121 / L127 / L143, and because it reads as an exemption it is the structural reason the posture sweep skipped this file. Replace the clause with a **precise** scope, not a stronger absolute: *"**Posture lives in [`CLAUDE.md`](../../../../../CLAUDE.md) §Live-execution posture (canonical) and [`STATE.md`](../../../../../STATE.md) (dated obligations).** This file may carry **at most a one-line disposition per pipeline row**, each stating its own disqualifier and its owning fork/ADR. Anything longer — arming gates, spend ceilings, rung numbers, fill history, park reasons — belongs in CLAUDE.md and is a defect here."* (b) **L15 / L14 Status cells** — P5 → *"**BUILT · DISARMED** — venue de-scoped 2026-08-04; nothing deployed; disposition = fork **F2** (2026-08-08)"*; P4 → delete the parenthetical *"(c1 eval runs on P5)"* and read *"**IDLE** — legacy book has no venue; the c1 eval deployment was withdrawn 2026-08-04"*. Every other non-active row already states its disqualifier, so P5's bare *"BUILT · currently DISARMED"* reads as deliberate contrast and makes P5 look like the one live pipeline. (c) **L121** — *"live spend still gated"* states no falsehood but carries pending-release modality; replace with *"the deployment limb was **superseded 2026-08-04** and no live-spend path exists — $208 of the $700 ceiling is sunk against a venue de-scoped as a deployment target, and whether the programme regains an execution surface is fork **F3** (08-08)."* **Leave the bullet's closing 11-08 demotion clause byte-unchanged — it is still live.** Same pass, L127: re-tense *"**Active path** (GO 2026-07-17)"* to *"was the active path until 2026-08-04"* and drop the rung and $700 figures to a `CLAUDE.md` pointer per the corrected L3 scope; L143: re-point the Q-NAS-ECR-1 line at `Q-MONSURF-1`. (d) **L72** — the quoted *"AMBIGUOUS-HOLD with 5/5 screened axes FAIL"* is a superseded interim branch **and it blocks a licensed route**: Q-KBUDGET-1 closed `RESOLVED` (2026-07-15) at 6 FAIL / 1 PASS / 0 UNSCREENABLE, and its §6 states that **D5 (NQ/MNQ intraday-momentum footprint) may proceed to campaign scoping**. Replace from *"its fork"* through *"not fundable today"* with the RESOLVED verdict, D5 named, the standing HARD gates (HARV §R reachability attestation before `register_search open`; net-of-cost Sharpe vs the Clause-K floor at K_eff ≤ 3), and *"Every OTHER axis remains unfunded for the 2026-11-08 falsifier. Screen PASS never blesses a candidate and never authorizes a Databento pull."* **Do not restate per-axis counts here** — the closure owns them, and restating is how this decayed. (e) **L160 / L155** — the `make check` membership list is wrong in **both** directions (names a `validate-params` target that has had no rule and no `.PHONY` entry since 2026-08-03; omits `skills-no-constants`, which the target does run at `Makefile:6`). Delete the membership list rather than re-pinning it: *"| Build entry points | `make check` — the aggregate gate; read the `check:` target in `Makefile` for its current member list, which is authoritative and is deliberately not mirrored here. Also: `make test` / `make test-ops`; `make sentinel` |"*. L155 cites the deleted `scripts/validate_params.py` under Locked-constant integrity → `scripts/verify_lock_anchors.py` (plus `scripts/check_pine_manifest.py` for Pine) — **that is the C14 site; land it once.** Record in the commit message that this closes the round-1 §5.10 G19 root-cause row. ⚠ **Generalizable, flag do not act:** any document declaring "posture is not restated here" is a candidate hiding place; that corpus sweep has not been run. |
| C33 | `.github/workflows/` — `skills-check.yml` L9 · `tests.yml` L27–62 · `tests.yml` L36–40 · `pylint.yml` · `manifest-check.yml` L82–88 | premise-dead · gate-unbinding | **SIMPLIFY ×2 · DELETE ×1** | **Three findings across one directory, one commit — and the fix is to give a single dated fact one owner instead of four copies.** (a) **Create `.github/workflows/README.md`** as that owner: *"# CI status — INERT AS SHIPPED — **GitHub Actions is disabled repo-wide** (verified 2026-08-05, `gh api repos/Joshua-Asante/multi_firm_operations/actions/permissions` → `{\"enabled\":false}`; no workflow run since 2026-07-16). **No workflow in this directory executes.** Re-enabling Actions resumes all of them at once. Live substitutes while inert: `scripts/githooks/pre-commit` (blocking, local) and `scripts/githooks/post-merge` (detection-on-arrival, after the bytes are already gone); `make check` is the manual equivalent. ⚠ No workflow here may describe itself as a \"backstop\" without pointing at this file."* Then add a one-line pointer to `skills-check.yml`, `tests.yml` and `pylint.yml`, and replace only the **first sentence** of `manifest-check.yml` L82–88 with a pointer — **keep its regression rationale (commit `66c2a14` / PR #574, bytes lost) in place.** (b) **`skills-check.yml` L9** — delete the word *"backstop"*: it overstates coverage **even when Actions is enabled**, because three hook items have no CI coverage of the real corpus. New comment: `# ⚠ INERT AS SHIPPED — see .github/workflows/README.md; this job does not run.` above `# Gate scripts once in CI; test execution lives in tests.yml — operator ruling 2026-07-24 (Algorithm review #5).` (c) **`tests.yml` — retire the `validation-controls` job** (L47–62 plus its L27–45 comment block) with a tombstone comment, not a silent deletion: it has no consumer (Actions disabled) **and** its selector decoupled from its corpus in the 2026-08-03 nest — running its exact selector set at HEAD aborts with *"Interrupted: 3 errors during collection"*. Tombstone carries the repair recipe verbatim so a re-enable is a paste, not a re-derivation: `--ignore=lab/archive/gbpusd_rank_cert` (body moved; `lab/analysis/gbpusd_rank_cert/` now holds `CARD.md` + `README.md` only), `--ignore=lab/archive/q_funnel_1_2026-07`, `--ignore=lab/analysis/regime/decompound_remc_2026-06-07` (**permanently unsatisfiable** — its Pepperstone input retired 2026-08-02), plus the existing `legacy/guardian_parity_2026-06-23` and `legacy/usoil_regime_capture` ignores; *"Verify with `pytest lab/ --collect-only` BEFORE re-enabling."* (d) **`tests.yml` L36–40 COVERAGE BOUNDARY block** — it names pre-theme-nest paths and claims coverage that is permanently unreachable (`test_regime_check.py` imports `regime_gate` at module scope, which loads a retired Pepperstone input at import time: 29 collected, 1 collection error, on every clone and every runner). Re-point both paths, state the unreachability with its ADR, and then **ENFORCE it** — add `--ignore=lab/analysis/regime/decompound_remc_2026-06-07` to the job's pytest invocation so the boundary is a property of the command rather than a claim in a comment. **Preserve** the "intentional, not a gap" framing, the note that the quarterly regime-trigger script is not run, and the retired-quarterly-duty and 08-08 Decompound-HOLD A5 lines. |
| C34 | `pyproject.toml` — L8 `description`, L17 `pyyaml` justification | premise-dead | **SIMPLIFY ×1 · KEEP-CORRECTED ×1** | **Two findings, one file, one commit — and the second is the one that matters, because its failure mode is deleting a live dependency.** (a) **L8** — the description names **one** retired capability, not two: `multipliers` is dead (per-account multiplier lookup retired in substrate Phase 2; `ops/cli.py:62` exposes only `tearsheet`), `Monte Carlo validation` is **narrowed, not retired** (`core/mc/` and `core/portfolio_mc.py` are present and regression-tested vendor-free; only the runnable broker-panel anchor went in Phase 3), and `DD protection` is live. Replace verbatim with: `description = "First Passage futures prop-trading research + operational layer (DD protection, Monte Carlo engine, governance gates)."` (b) **L17** — the justification comment is dead (`lab/codification/` retired 2026-08-02; `concept_schema` now survives only in this comment and the Gen-1 retirement ADR) but **the dependency is live and must not be pruned**. Replace with: `    "pyyaml",  # scripts/instrument_profiles.py:70 — the only importer; wired into make check (Makefile:40) and scripts/githooks/pre-commit:74. Do not prune.` **Correct the scope while doing it:** exactly **one** file imports yaml — the claim that five governance gates break is false; it is one gate wired into two entry points. Verify before landing with `grep -rn "import yaml" scripts/ core/ ops/ tests/` (expect exactly one hit). |
| C35 | `REPO_MAP.md` — L103 roster note | stale-figure · gate-unbinding | SIMPLIFY | The **warning** has a consumer (whoever adds a script); the hand-maintained **count** and 14-name enumeration do not — nothing regenerates or gates them, and they have decayed in the under-reporting direction: `git ls-files 'scripts/*.py'` returns **37** at HEAD, not 35, and **18** are unlisted rather than 14 — the extra five being `archive_strategy`, `check_closure_disposition`, `check_falsifier_reachability`, `check_md_relative_links`, `check_supersession_placement`, three of which are wired into pre-commit gates 13/14 or the Makefile. So the warning understates its own hazard, and the *"All 14 are stdlib-only today"* assurance is scoped to a set that no longer covers the corpus. Replace the *"⚠ **This list is incomplete:** …"* sentence through *"could be flagged an illegal edge."* with the **regeneration one-liner plus the invariant**: `comm -13 <(rg -o '"[a-z0-9_]+"' scripts/check_boundaries.py | tr -d '"' | sort -u) <(git ls-files 'scripts/*.py' | xargs -n1 basename | sed 's/\.py$//' | sort)`, then *"**Invariant:** any script absent from `check_boundaries.py`'s `SCRIPTS_LAYER` is classified `governance` by the fallback (`check_boundaries.py:109`), so the moment it gains a first-party import it can be flagged an illegal edge. As of 2026-08-05 the corpus is 37 files with 18 unlisted — treat that as a snapshot, not a pin."* **Preserve** the invariant and the fact that the two mappings are not gated against each other. A frozen number with no gate behind it is guaranteed to decay; replacing it with the command that regenerates it makes the claim self-verifying. |
| C36 | `.gitignore` — L168–171 US500 bar-cache block | dead-pointer | SIMPLIFY | Class-E break: the selector silently stopped reaching its corpus while its intent never changed. The path was vacated by the 2026-08-03 theme nest (the study is at `lab/analysis/legacy/us500_discovery_2026-06-22/`) and `git check-ignore` returns rc=1 there. Adopt the **proven sibling form** at L176–179 rather than inventing one, and correct the now-false regeneration comment in the same edit so the block stops re-teaching a dead producer path. Replace L168–171 with: *"# US500 discovery bar cache — serialized Pepperstone 15m bars (vendor-derived, redistribution not permitted). Producer RETIRED: harness.py reads `core/data/tv_exports/pepperstone/bar_export/US500_M15_pep.csv`, deleted with the Pepperstone feed 2026-08-02, so this cache is no longer regenerable in-tree. Paths use `**/` so the theme-nest location (`legacy/`) and any flat remnant both match."* followed by `lab/analysis/**/us500_discovery_2026-06-22/us500_15m.pkl`. Delete is defensible (the cache cannot be regenerated from anything in the tree) but a gitignore line is nearly free and fail-closed, and the guard must survive an offline-copy restore. |
| C37 | `scripts/githooks/pre-commit` — header block L2–9 | stale-figure | **DELETE** | The header enumerates **4** checks (numbered 1, 2, 4, 5) while the body runs **15** invocations across 14 distinct scripts — an undercount of **eleven**. It is a mirror of documentation that already exists in a better place: every gate carries its own numbered inline comment immediately above its invocation, several longer and more informative than the header entries. Regenerating the header recreates a hand-maintained mirror in a 112-line file where the canonical record is already co-located. Replace L2–9 with a pointer that **records what was struck and why**: *"# Pre-commit gate. Every check is documented INLINE, immediately above its own invocation below. This header deliberately does NOT mirror the list: the mirror rotted (it enumerated 4 of the 15 invocations, numbered 1/2/4/5, from 2026-06 until struck 2026-08-05). Read the body — it is under 100 lines. # ADR 2026-06-04 methodology-skills-under-vc §2.4 wires checks 1 (skills) / 2. # (Notion data-skill mirror gates removed 2026-06-25 with the Notion-subsystem retirement.)"* **Leave every inline comment block and every invocation byte-identical.** Held at COSMETIC because each unlisted gate is individually documented inline and the skipping numbering already signals incompleteness. |
| C38 | `scripts/check_brief.py` L35, L180 · `scripts/check_skill_refs.py` L88 · `scripts/check_skills_no_constants.py` L8–10 | dead-pointer · gate-unbinding | **DELETE ×2** | **Three gate-script docstrings, one commit — and the three take DIFFERENT dispositions on purpose; treating them identically deletes a rationale.** (a) **`check_brief.py`** — both cites of the deleted `validate_params.py` are pure ceremony: the HARD/WARN contract is stated inline in the two lines directly beneath. L35 *"Severity model (mirrors validate_params.py's HARD/WARN tiers):"* → *"Severity model:"*; L180 *"""One well-formedness finding. Mirrors validate_params.py's Violation."""* → *"""One well-formedness finding."""*. This is live code documentation, not a dated body — Trap #12 offers no protection. (b) **`check_skill_refs.py:88`** — that cite carries **reasoning** (why absent vendor Pine is WARN, not HARD), which survives its referent's death, so **KEEP-CORRECT** it: append *"(gate retired 2026-08-03, `docs/adr/2026-08-03-params-toml-gate-retirement.md`; the precedent stands)"*, mirroring what `check_skills_no_constants.py:4` already does correctly. (c) **`check_skills_no_constants.py` L8–10** — delete the CARVE-OUT's exemption enumeration: it names 3 skills where **14 of the 18** in `.claude/skills/` are unguarded, so it reads as a closed carve-out list when it is a sample. Replace with: *"CARVE-OUT: scans ONLY the skills named in GUARDED_SKILLS below — the four *methodology* skills, where a restated operational constant is pure drift. EVERY other skill in `.claude/skills/` is out of scope, including operational skills that do restate live constants. Whether that scope is right is an open operator question (**FU-13**, due 2026-08-08); do not widen `GUARDED_SKILLS` without that ruling."* ⚠ **Do NOT widen `GUARDED_SKILLS` in this pass** — that is a coverage decision with its own owner and its own date, and pre-empting it in a docstring edit is exactly the belt-grows-without-adjudication pattern. **Preserve** the inline HARD/WARN definitions and the exit-code contract in `check_brief`, and the vendor-data WARN rationale in `check_skill_refs`. |
| C39 | `scripts/pine_lint.py` — L2, L4, L11, L61, L631+ | premise-dead | **DELETE** | (a) Delete `_self_test()` (L631 onward) with its `--self-test` argparse flag and dispatch: it lints `lab/codification/scaffold.pine.tmpl`, deleted 2026-08-02 with the codification tree (`ffc7aff`), and has exited 2 ever since — the linter's own regression harness can never pass. Insert the tombstone below the module docstring: *"# 2026-08-05: the --self-test branch was removed. It linted lab/codification/scaffold.pine.tmpl, deleted 2026-08-02 with the codification tree (ffc7aff; PIPELINES P2 RETIRED), and had exited 2 since. If this linter regains a consumer, vendor a minimal scaffold under tests/fixtures/ and re-add a real regression rather than re-pointing at a research tree."* (b) Same commit, re-anchor the orphaned framing — this is live code asserting a wiring that grep disproves: L2 *"static linter for codification-stage candidate .pine"* → *"static linter for candidate .pine (repaint / lookahead bias)"*; **L4 delete** *"THE LOAD-BEARING CHECK of the R&D pipeline."* — it is wired into no Makefile target, no `scripts/githooks/`, no workflow; L11 replace `validation/concept_intake/check_concept.py` (retired 2026-07-11) with `scripts/check_brief.py`; L61 comment `CANDIDATE_DIR` noting `core/strategies/candidates/` now holds only `candidates_CARD.md`. (c) ⚠ **FLAG FOR OPERATOR, do not act:** the linter is invoked by nothing. Either it earns a `make` target or it is the next deletion candidate and the 2026-08-02 scout's `KEEP` ruling (`docs/notes/cleanup/2026-08-02-deletion-candidates-scout.md:111`) should be re-examined at the 2026-08-08 review. **PRESERVE `lint_text()` and all 13 checks** — repaint/lookahead detection is the substantive asset and the only contamination class invisible downstream. |
| C40 | `scripts/pine_check.README.md` — §Re-checking L46–51, "Two boundaries" L38–42 | overstated-modality | KEEP-CORRECTED | §Re-checking enumerates three re-check limbs, but the **oracle-regression limb has been vacuous in every tree since the 2026-08-04 coldstore move** while still contributing to the printed `== audit PASS ==` — and the L38–42 block compounds it by attributing the skip solely to worktrees, so four `SKIP (absent)` lines in the MAIN tree read as normal rather than as corpus loss. Append to the boundary-1 bullet at L38–40: *"Note (2026-08-04): the locked `.pine` also moved to coldstore, so a `SKIP (absent)` in the MAIN tree is NOT the worktree case — it means the oracle corpus is unreachable and the limb contributed nothing to `== audit PASS ==`. Verify the printed SKIP count is 0 before treating the audit as a re-validation."* **Land in the SAME commit as the SE-02/SE-03 path repair** in `scripts/pine_check_audit.sh` / `.ps1` (agent-facing section) — once those land the enumeration is true again and only the attribution clause needs the extra sentence; ship it apart and the next reader re-derives the wrong conclusion from this file. If that repair slips, add to L48–50 instead: *"(as of 2026-08-04 the oracle-regression limb is vacuous in every tree — see SE-02/SE-03)"*. |
| C41 | `docs/spec/pine_baseline_csv_format.md` L3–5 · `docs/spec/2026-06-13-codifier-breakout-longshort-trailing-extension.md` H1 + L4 | premise-dead | **DELETE ×2** | **Two orphaned specs, one banner sweep — and the pattern is the finding.** With SP-18 and the already-bannered `PREREG-NAS-ECR-1-live-edge-capture.md`, these are the **third and fourth `docs/spec/` artifacts still addressing deleted trees**; the 2026-07-11 CFD-estate retirement sweep reached one of three, which is the `docs/spec/` gate-coverage gap **C18** names. (a) **`pine_baseline_csv_format.md`** — replace L3–5 with *"**Status:** ⛔ **RETIRED 2026-08-05** — no consumer exists. Both named consumers were deleted with the CFD/ECR estate on 2026-07-11 ([ADR](../../../../adr/2026-07-11-ops-cfd-estate-retirement.md)): there is no `live_journal/` tree at HEAD, so `live_journal/ingest/pine_loader.py::load_pine_csv` and `live_journal/counterfactuals/schema.py` are gone. Not revivable as-is — Q-NAS-ECR-1's dormancy flag rules that a re-point to a futures venue is **not type-preserving** and needs a fresh Pre-Q, so a venue-native monitor requires a new format, not this one. Kept below as the historical column-format record."* (b) **`2026-06-13-codifier-breakout-longshort-trailing-extension.md`** — insert below the H1: *"> **RETIRED 2026-08-02 / banner 2026-08-05 — historical design record only, do not implement.** Both trees this spec extends are deleted: `lab/codification/` (the Python→Pine bridge, retired 2026-08-02 — `REPO_MAP.md:57`, `:151`) and `lab/validation/sweep/` (retired with the Gen-1 pipeline). The §6 \"revisit 2026-08-08\" booking belongs to the `(C) PARK` branch of an operator fork already resolved in favour of `(A) BUILD` (WI-1/2/3 landed 2026-06-13, §8) — it is a road not travelled, not a live obligation. The L4 Status field is a dated landing record of what was built on 2026-06-13 and stays as written."* Append to the L4 Status field: *" — **tree RETIRED 2026-08-02; see banner above.**"* **No body edit to either file.** ⚠ The "gate can never fire" framing that generated (b) over-reaches and must not be carried into the banner: the file's own §Scope guard at L9 already states the Python pre-filter *"has no gate authority … can only mis-rank a shortlist, never deploy"*. **Sweep the remaining `live_journal` references (SP-18) in the same pass** and close the 2026-07-11 gap once. |
| C42 | `docs/spec/PREREG-C1-DEDUPE-1-intent-key-functional-property.md` — one banner below L4 | gate-unreachable | KEEP-CORRECTED | §6 precondition 1 (M1 `RESOLVED`) has **no route to satisfaction today** — item 5 requires a real TV strategy signal at non-zero size and both Striker legs were withdrawn 2026-08-04, so nothing can emit one — and the file says nothing about it. Insert **one** banner immediately below the Status line, **no body edit anywhere**: *"2026-08-05 — §6 precondition 1 has no route to satisfaction today. M1 item 5 requires a REAL TV strategy signal at non-zero size, and both Striker legs were withdrawn from the c1 eval deployment on 2026-08-04 (`docs/adr/2026-08-04-tradeify-venue-descope-eval-included.md`), so nothing can emit one. This is CONTINGENT on operator forks F2 and F3 (both 2026-08-08), not permanent — M1 is undischargeable WHILE no strategy is deployed. The gate is unchanged and still binds: `M1_MONITORING_ACCEPTANCE.json` status reads `CODE_LANDED`, so the answer to precondition 1 is still NO. Note §6 states THREE preconditions; the two-condition summary in the Status line above is the weaker statement. Body frozen (Trap #12)."* **Land jointly with SP-14 so the file carries one dated banner, not two.** Three narrowings hold this at COSMETIC and must not be re-inflated: the document self-describes as a frozen change pre-registration (Trap #12 permits a header banner only); the gate **fails closed and correctly**, so nobody is misled into acting, only into a wrong expectation about *when* it opens; and L4 already carries the restraint *"Not scheduled; not in the OPERATOR QUEUE"*. **Preserve all three preconditions verbatim and do not compress the L4 summary into the gate.** |
| C43 | `.claude/skills/inqhiori/SKILL.md` — §9 heading L204, L206 | overstated-modality | KEEP-CORRECTED | The §9 worked example is titled *"(in flight)"*, which makes three questions closed on **2026-04-25** read as open work; §8's heading at L186 already carries the correct form, *"(2026-04-25, retrospective)"*, so the file demonstrably knows the convention. One-word class of fix: L204 → *"## 9. First conscious test — Inquire-phase work (2026-04-25, retrospective)"*; L206 → past tense (*"The 2026-04-25 Claude Code brief **set up** three Inquire-phase questions. Each **was** an opportunity to …"*); append one sentence: *"All three have since closed; Q8a (doc/code skew) was promoted to a standing operational rule with its own slash command, `.claude/commands/skew-audit.md`."* **Leave the three numbered gate applications byte-unchanged** — the teaching content survives Delete (a second worked example on a different phase earns its place) and the Notion page-IDs it cites resolve via `docs/governance/notion-redirect-map.md`, which is not a defect. |
| C44 | `.claude/settings.json` — L7 `permissions.allow` entry | dead-pointer | **DELETE** | Delete `"Bash(python dd_protection.py:*)",`. The path has been wrong since the 2026-06-05 monorepo split (the module is `core/dd_protection.py`), so the rule has **granted nothing for two months and nobody hit a prompt** — that absence is the measurement. A corrected entry would also be redundant: the actual lock-check entry point `scripts/verify_lock_anchors.py` is separately granted on the very next line with its full path, which is also the file's convention. The file is live config with no comment syntax, so the **tombstone lands in the commit message**, verbatim: *"settings: drop dead pre-approval Bash(python dd_protection.py:*). Path stale since the 2026-06-05 monorepo layer split (module is core/dd_protection.py); the rule has matched nothing for two months and granted nothing. Lock-checking is already pre-approved via Bash(python scripts/verify_lock_anchors.py:*)."* ⚠ **This is not a narrowing of an operator grant** — a rule that matches no command cannot be narrowed. **OPERATOR ALTERNATIVE, not an audit repair:** replacing it with `"Bash(python core/dd_protection.py:*)"` **creates** a pre-approval nobody has needed, which is a larger change than deletion and an operator call. |
| C45 | `lab/analysis/orb/orb_universe_2026-06-22/RESULTS.md` — Status line · `lab/CATALOG.md:59` | orphaned-scope | KEEP-CORRECTED | The file's bare *"ACTIVE — which FXIFY CFD best suits Opening Range Breakout"* ranks instruments in a universe that closed 2026-07-10/11, and it contains **zero** occurrences of MNQ, superseded, retired, any 2026-07 date, or any warning marker; `lab/CATALOG.md:59` mirrors the bare status. Replace the Status line with: *"**Status:** SUPERSEDED-VENUE (2026-08-05) — findings stand; the universe does not. The FXIFY CFD estate closed 2026-07-10/11 ([ADR](../../../../adr/2026-07-11-ops-cfd-estate-retirement.md)), so this instrument ranking has no venue. The native-futures successor **ORB-MNQ-1** is re-PARKED with its payability target FALSIFIED 2026-08-03 ([ADR](../../../../adr/2026-08-03-orb-mnq-repark-payability-falsified.md)). **§Next steps below is a CFD-era plan and is void** — every item assumes DXTrade `contractValue`, a 5-strategy MC, or an 08-08 slate that no longer exists; the file's own later operator-ruling section already supersedes item 1's \"only path to a lockable NAS100 ORB\"."* Mirror the same status token at `lab/CATALOG.md:59` **by hand** (regenerating from a worktree clobbers the heavy column). ⚠ **Do not edit the body** — the ranking, the placebo p-values (ORB-30 within-day p=0.012) and the operator ruling are correct history at their date (Trap #12), and the +0.058R / 0.9pt slip anchor is cited across the ORB lineage. The overstated-modality limb of this finding is **refuted**: the file's own *"Operator ruling + approval (2026-06-22, post-research)"* section already retires the regime-conditional caveat and sets out a different four-item route, so the "only path" sentence is superseded **in-file** and needs no edit. ⚠ **FLAG FOR OPERATOR, do not act:** archival to `lab/archive/` is status-gated and operator-fired under the 2026-07-11 STM/LTM design; this study is now a candidate. |

---

## Commit clusters — rows that must not be landed alone

Nine rows in this file are the residue of a MISLEADING row at the same file, or are false until a sibling lands. Landing them alone either opens a file twice or ships a document that contradicts itself.

| Cluster | Rows | Why one commit |
|---|---|---|
| `CLAUDE.md` | **C1** + M1–M4 | Eight sites in one always-loaded charter. |
| `ops/instruments/MNQ.md` · `MYM.md` | **C4**, **C5**, **C8**, **C10** + M21, M24 | `PROFILES.md` / `profiles.json` regenerate exactly once. |
| `docs/notes/rail_build/RUNBOOK.md` | **C21** + M13–M16, M20 | Six findings, one file. |
| M1 send-vs-arm cluster | **C19**, **C22** + M16 | Four surfaces drifted together because the invariant is copied; the fix is to stop copying it. |
| `core/mc/preflight.py` | **C13** + M41, M42 | One file, one open. |
| `docs/methodology/strategy_lifecycle.md` | **C20** + M26 | Path fix and modality fix are the same paragraph. |
| `PIPELINES.md` | **C32** (+ **C14** site 2) | L3 is **false until** the four body edits land. |
| `scripts/pine_check.README.md` | **C40** + SE-02 / SE-03 | The enumeration is only true after the path repair; shipped apart, the next reader re-derives the wrong conclusion. |
| `lab/analysis/c1/c1_thirdleg_instrument_map_2026-07-27/` | **C31** + M45 | Same study, same missing-intercept class. |

## Why this tier is deferrable — and what "deferrable" does not mean

**Deferrable to the 2026-11-08 checkpoint.** No row in this file can cause a wrong deployment, a wrong clearance, or a real-money action: nothing here sizes a position, sets an arming state, encodes a firm rule, or authorizes a spend. That is the entire justification for the deferral, and it is a claim about *consequence*, not about how well-specified the rows are — every one names the exact line and the exact replacement text, because an unspecified cosmetic item is what becomes a MISLEADING one at the next sweep.

**Two things the deferral does not license.**

1. **Do not defer the rows that share a commit with a MISLEADING or BLOCKER row.** The clusters above are the cheapest possible remediation ordering: the file is being opened anyway. Deferring the COSMETIC half means opening it twice and re-deriving the context, which is how the twelve absorbed items became duplicates in the first place.
2. **Do not treat deferral as adjudication.** These are recommendations pending operator ruling. If the operator rules a row DECLINED, that ruling belongs in the row, so the claim is not silently re-raised at the next sweep — the same treatment the two `KEEP-AS-IS` adjudications received.

## What this file does not license

- **No edit to the frozen set.** Nothing here authorizes a change to locked strategy parameters, Pine, `dd_protection` constants, `core/lifecycle.py` tier multipliers, allocations, or frozen ADR §2 decision text. Rows C1(d), C9, C12, C13, C14 and C20 each say so at their own site because each sits adjacent to one of those surfaces.
- **No in-place edit of a dated body.** Session logs, closure bodies, RESULTS bodies, frozen pre-registrations and ADR §2 text are correct history (Trap #12). Where a dated body is wrong about the present, the remedy in this file is always a **banner, an addendum, or an amendment-log entry** — C21, C22, C25(b), C26, C27, C28, C31, C41, C42 and C45 are all of that shape, and each says "no body edit" explicitly.
- **DELETE means retire with a tombstone.** Every `DELETE` verdict in this file carries its own record of what was struck and why: an in-file dated line (C3, C6, C23), a replacement comment (C33(c), C37, C39), a commit-message tombstone where the format has no comment syntax (C44), or a banner over a retained body (C41). **Silent byte removal is never the instruction.**
- **The lifecycle axis is unmoved.** Both Striker legs remain `AUTHORIZED · MECHANISM @ 1.00×`. Venue-fit is not decay; no row here implies otherwise, and C5(c), C7 and C9 say so at the sites most likely to be misread.

---

*Sections: [`README.md`](README.md) — index, combined counts, and the round-1 vs round-2 evidential-standing statement.*
