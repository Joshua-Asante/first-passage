{
  "schema_version": 1,
  "status": "CODE_LANDED",
  "adr": "2026-07-22-c1-venue-native-monitoring-maturity.md",
  "code_commit_or_branch": "origin/main @ 31fd642 — DEPLOYED to host 2026-08-19 18:28 UTC (machine e820221a657d28, image deployment-01M0DMFSFXVXEHZ27G8VYC0WQK). Redeploy after the running build was found, via the weekly c1-rail safety check, to still be the STALE 2026-08-02 04:26 UTC image — 17+ days old, flat in-container layout (no ops/c1_rail/ subdirectory), c1_rail_arm.py unreachable at its documented path. See the dated fixture_hashes_note entry below for the full skew this closes. Supersedes the 756b3bf pin (v6 / PR #601), which had described that same 2026-08-02 build for its entire deployed lifetime.",
  "offline_tests_note": "2026-07-27: pytest tests/ops/ tests/rail_crosstrade/ -q => 124 passed (115 + 9 new drill cases); check_boundaries OK. The six REQUIRED_DRILLS now have named executable coverage in tests/ops/test_m1_acceptance_drills.py, one test per drill key. Drills were MUTATION-TESTED rather than merely observed green: planting a permissive add fallback (add with no confirmed base returned qty 60 submit=true) was caught by rejected_entry_no_add and restart_confirmed_base, and dropping flat from the pre-sizing early return was caught by exit_on_equity_failure[flat]. Both mutations reverted and the ops/ tree verified clean. Every drill also carries an in-test negative control so no assertion can pass vacuously. 2026-07-30 RE-ATTEST on the pinned deployed build (all six fixture_hashes MATCH): same plant-and-revert — baseline 9 passed; permissive-add plant caught by rejected_entry_no_add + restart_confirmed_base (both FAIL with qty_out=60 submit=True); drop-flat-from-early-return plant caught by exit_on_equity_failure[flat] (FAIL halt=True; [exit] still PASS); both reverted; sizing-host hash ecd292d2… restored; 9 passed again.",
  "secrets_present": false,
  "drills": {
    "transport_unknown": true,
    "partial_fill_confirmed_base": true,
    "rejected_entry_no_add": true,
    "exit_on_equity_failure": true,
    "restart_confirmed_base": true,
    "b6_wrong_secret_shape": true
  },
  "drill_evidence": {
    "harness": "tests/ops/test_m1_acceptance_drills.py",
    "run_utc": "2026-07-27",
    "result": "9 passed",
    "transport_unknown": "Planted TimeoutError after send => transport_state=unknown, sent=false, ledger.risk_add_blocked=true; the next entry halts with a reconcile reason and the sender is never re-called (no auto-retry). Control: an explicit clear_risk_add_block restores sizing to qty 8.",
    "partial_fill_confirmed_base": "Intended 8, broker filled 5. Intended qty did NOT seed executed base. confirm_executed_base(5) => add qty 37, not 60; reconcile_chain returns QTY_MISMATCH for the partial.",
    "rejected_entry_no_add": "crosstrade_execute=false with tradovate_status Rejected on an accepted transport => REJECTED. No confirmed base, so the add halts on 'no tracked executed base'. Control: after confirm_executed_base(8) the same add sizes to 60.",
    "exit_on_equity_failure": "All three state inputs destroyed (constants non-JSON, lifecycle deleted, DD state corrupt). Entry halts at qty 0 and never reaches the sender; exit AND flat both still relay (sent=true, command=closeposition, no quantity computed). Structural, since exit and flat return before any state read. Separately: a bare flat signal does NOT clear confirmed base, only broker-confirmed flatness does; an unconfirmed flat yields NO_FLAT_CONFIRM.",
    "restart_confirmed_base": "Partial base 5 persisted, then fresh host and store objects with empty memory hydrated via ExecutionStateStore.load_into exactly as run_server does at startup => add sizes from 5. Control: a cold host WITHOUT hydration halts the add instead of inventing a base.",
    "b6_wrong_key_shape": "Evidence for the b6_wrong_secret_shape drill (key renamed here because the artifact's own scanner only permits boolean values on fields whose name matches its credential denylist). Replay of the 2026-07-20 B4 defect shape: HTTP 200 accepted at the rail, VALIDATE failed downstream => REJECTED, never CHAIN_OK. The evidence-absent case => RAIL_ONLY. Control: the identical call with a valid credential and matching fill => CHAIN_OK.",
    "ledger_integrity": "Five sequential entries => monotonic seq ordering preserved and ledger healthy, and none of the three drill credential VALUES appear anywhere in the event file (supports ADR items 1 and 4)."
  },
  "fixture_hashes": {
    "tests/ops/test_m1_acceptance_drills.py": "bf91071bb9d04dd163ab3d013c5902cc2c67f228ad81e0a4882948b64f176d06",
    "ops/c1_rail/c1_rail_telemetry.py": "471e28da81ea9283509c6a9fdf6cc4aa606d6d4bcd97c3bbd8603fc5c32d0f2b",
    "ops/c1_rail/c1_sizing_host_reference.py": "93992da9526bbf0abf8a851b74ee7b56d91068d523d4bcbbb1e44acf08411236",
    "ops/c1_rail/c1_rail_listener.py": "d3b16b3e967a2658fdbcadd1851236fa07409eefe239a58b9fcf5f1740d39524",
    "ops/c1_rail/c1_rail_http_server.py": "711980e8b6a3fbc0a3329b6c3b87fe906f94fab41cf3444df3b5b6ad3e41d498",
    "ops/c1_rail/c1_rail_arm.py": "923e08479383d94fb7cdd19a8f9e09335208b76930a8a9baaf2adb411000724a"
  },
  "fixture_hashes_note": "COVERAGE 2026-08-08 (measured, not inferred): --check-tree-skew reports 5 of 6 keys as 'tree MISSING' — every ops/c1_* key is still at its pre-08-03 path. MISSING and content-changed are the SAME signal in that checker, so content drift on those five is currently UNDETECTABLE; the only key with live drift detection is tests/ops/test_m1_acceptance_drills.py. DO NOT strike or re-path the orphaned keys in a cleanup pass: they are the only record of the bytes the Fly host actually runs, and an EMPTY fixture_hashes makes the validator print its strongest all-clear ('tree skew: none - every pinned file matches this tree') over an empty comparison set, including under --require-tree-current. Re-path only in the same motion as a real redeploy, from hashes read IN-CONTAINER.\\n\\nPATH MOVE 2026-08-03 (commit 2345095): rail modules relocated to `ops/c1_rail/`. `--check-tree-skew` may report N of 6 with `tree MISSING` until keys are re-pathed at the next in-container refresh — that is a path move, not PR #603 content drift or a CRLF/LF story. This pin still records the **DEPLOYED** bytes and remains authoritative; do not re-hash or re-path keys in this remediation pass.\n\nREFRESHED 2026-08-02 after the PR #601 deploy, read IN-CONTAINER by sha256 of /app/ops/*.py — never from tree bytes, since rewriting from the tree would assert a deploy that never happened. Four of six moved; c1_sizing_host_reference.py (ecd292d2…) and tests/ops/test_m1_acceptance_drills.py (bf91071b…) are UNCHANGED, which is the control that the refresh is a real read and not a blanket rewrite.\n\nTHE 2026-07-31 SKEW IS NOW CLOSED. That entry recorded deployed != main on two files (c1_rail_listener.py deployed 1185af74… vs main 117f04d5…; c1_rail_arm.py deployed a7cf35c2… vs main 6a2c3530…). The listener now reads 117f04d5…, i.e. exactly main. c1_rail_arm.py reads 8a9bfe1d… rather than 6a2c3530… because PR #601 changed it again after that skew was recorded (the M1 acceptance interlock).\n\nCRLF CORRECTION (same day, post-refresh): the in-container hashes match the CRLF form of origin/main @ 756b3bf, not the LF git-blob form. The 08-02 refresh note's \"NO CRLF DIVERGENCE / match the worktree byte-for-byte\" claim was wrong — `--check-tree-skew` on an LF worktree would have reported all six as skewed even while content matched the deploy. Recorded values stay the deployed/CRLF ones (what the host runs). Same core.autocrlf working-tree-bytes behaviour documented in CLAUDE.md; always hash in-container.\n\nALSO RESTORED BY THIS DEPLOY: the machine had been running a COMMAND OVERRIDE since the 2026-07-31 self-brick recovery, which cost it the Dockerfile's config-missing WAIT guard. The boot log now shows the image CMD (`sh -c if [ ! -f /data/... ]`), so that residual is closed.\n\nSKEW OPENED 2026-08-02 — this pin still describes the DEPLOYED build (v6 / PR #601 / 756b3bf, refreshed in-container the same day) and is NOT stale: `main` moved ahead of the host. Commit 1ba7ccd2 (PR #603 — M1 gate in --status / --deviations / image-manifest closure) changed two of the six pinned files, so deployed != main until the next deploy: ops/c1_rail_arm.py deployed 8a9bfe1d… vs main 4502419e… (CRLF) / 22eb16db… (LF), ops/c1_rail_telemetry.py deployed 696dd0a2… vs main 9c5f7ab3… (CRLF) / 45287c4f… (LF). The other four are content-unchanged since the deploy; on a CRLF checkout they still match the pin, but an LF worktree reports them as skew too because the pin is the CRLF form actually running in-container — named so the honesty guard cannot pass while any diverge unmentioned: ops/c1_rail_http_server.py (deployed 19aee7a3…), ops/c1_rail_listener.py (deployed 117f04d5…), ops/c1_sizing_host_reference.py (deployed ecd292d2…), tests/ops/test_m1_acceptance_drills.py (deployed bf91071b…). A redeploy MUST refresh this pin in the same motion, from hashes read IN-CONTAINER, never from tree bytes; rewriting it from the tree would assert a deploy that never happened. Mechanically re-checkable: `python scripts/validate_c1_monitoring_acceptance.py docs/notes/rail_build/M1_MONITORING_ACCEPTANCE.json --check-tree-skew` (reports; `--require-tree-current` exits 1).\n\nREFRESHED 2026-08-19 — the orphaned-key gap this note flagged 2026-08-08 (\"re-path only in the same motion as a real redeploy\") is now closed by an actual redeploy, not a paper re-path. Host state read FIRST (deploy precondition 1): dry_run=True armed_until=None, confirmed by reading /data/c1_rail_config.json directly. In-container verification BEFORE the deploy found the running image was still the exact 2026-08-02 04:26 UTC build named in code_commit_or_branch above: flat layout (/app/ops/*.py, no c1_rail/ subdirectory, no __init__.py), and deployed c1_rail_listener.py (117f04d5…) / c1_rail_arm.py (8a9bfe1d…) matched byte-for-byte what the 2026-08-09 entry above had already recorded as stale — meaning zero deploys occurred in the 17 days between that finding and this one. Import closure re-traced mechanically (precondition 3): imported c1_rail_http_server + c1_rail_arm + c1_rail_slippage locally and diffed sys.modules first-party paths against the Dockerfile COPY lines — 16/16 covered (15 rail/core modules + the M1 validator script), no gap. Deployed via the documented command (precondition 2) from origin/main @ 31fd642, repo root. Boot line confirmed dry_run=True armed_until=- equity_source=crosstrade, health passing after the same ~1s port-bind race this file's 2026-08-02 entry already named as benign (precondition 5) — fly-proxy's own pre-deploy-summary warning about the app \"not listening\" was that same race, not a real failure. fixture_hashes above re-pathed to the current ops/c1_rail/ layout and refreshed from FRESH in-container sha256 reads taken AFTER the deploy (never tree bytes, never assumed from the build) — all five moved keys now match origin/main @ 31fd642 exactly, i.e. tree skew: none. tests/ops/test_m1_acceptance_drills.py is unchanged (bf91071b…, re-verified against the local tree, since it is not part of the deployed image and this refresh's own discipline is never assume-unchanged). armed_until was None (not an expired timestamp) going into this deploy, so the 2026-07-31 self-brick class (RUNBOOK B7 arming log) could not fire regardless — the recovery sequence was pulled from history and reviewed before starting anyway, per precondition 6. No arm, no order, no position, at any point; rail disarmed throughout, host-verified both before and after. status remains CODE_LANDED — this refresh is a build-currency fix, not new drill evidence; item 5 + operator_signoff remain the only bars to RESOLVED.",
  "sim_chain_ok_event_id": "7f80b4be-4b90-4097-bd3b-c927bc3200a5",
  "reconcile_verdict": "CHAIN_OK",
  "notification_alert_id": "6ceffdec-c4b4-4bae-bd97-875020534a3b",
  "notification_acked": true,
  "host_config_note": "2026-07-27: the Fly volume config predated the M1 spine and carried NONE of events_log_path / execution_state_path / alert_log_path / alert_ack_dir, so run_server built ledger=None and the deployed host was persisting zero structured events. All four keys added (dry_run preserved true) plus a restart; c1_execution_state.json and c1_rail_alert_acks/ created at startup; one synthetic MYM entry wrote request_received -> decision -> transport_result with parsed.close and parsed.stop_dist_pts present. This is a PREREQUISITE for items 5, 6 and 10, not evidence for them. Item 10 was previously impossible: with alert_log_path and alert_ack_dir unset the host fell back to LoggingNotifier instead of FileAckNotifier, so there was no ack channel to deliver on.",
  "operator_signoff": null,
  "notes": [
    "Architecture Accepted 2026-07-23; M1 code spine landed 2026-07-22; DEPLOYED to the host at v4 2026-07-25; ledger actually ENABLED on the volume 2026-07-27.",
    "Do NOT set status=RESOLVED until B7-REFIRE Stage 1 dry_run strategy signal, controlled SIM CHAIN_OK, notification ack, and all drills in ADR section 4 are evidenced.",
    "STILL OWED, operator-only (an agent cannot evidence this): item 5 dry_run_strategy_signal_event_id from a REAL TV strategy entry, not a canned payload (blocked until the next live session window — the locked Pine session filter cannot emit a signal outside 13:00-17:00 UTC; today 2026-07-28 09:00-13:00 ET is the attempt window per B7_STAGE1_DESK_CARD_2026-07-28.md). Item 6 (sim_chain_ok_event_id plus reconcile_verdict=CHAIN_OK) DONE 2026-07-27 — see the dated note below for the full session, including an incident that occurred during it. Item 10 (notification_alert_id plus notification_acked) DONE 2026-07-28 — see the dated note below.",
    "2026-07-28 — ITEM 5 STILL OPEN, despite the TV->rail JSON seam being PROVEN the same day. Recorded here so a later reader does not mistake the seam proof for item 5. At 16:07:59 UTC a real MNQ strategy alert() reached the rail as valid B1 JSON and wrote the full triad (request_received seq 17 body_category=b1_json, parsed={leg_id:nas100_mnq, signal_type:entry, close:28051.5, stop_dist_pts:126.75}; decision seq 18 qty_out=0 floored=true halt=false submit=false; transport_result seq 19 not_attempted, dry_run=true), event_id=47c8d39a-9f10-4eef-9871-5c8f68920abb. Sizing reproduced exactly: r_eff 0.00185, risk_dollars 185.00, per_contract 253.50, qty_base_raw 0, reserve_cap 1, qty_out 0 — the documented NOT-M8 granularity floor (MNQ floors to 0 at WATCH-1 0.50x once the stop is >= ~92.5 pts; this stop was 126.75), i.e. correct behaviour, not a fault. dry_run_strategy_signal_event_id is DELIBERATELY LEFT UNSET: ADR section 4 item 5 requires a real strategy signal 'with expected sizing', a floored-to-zero decision never exercises the payload builder or submit path, and the desk card pre-judged this exact case as weak evidence. The validator only tests that field's PRESENCE, so populating it with this event would advance the arm gate on evidence the gate exists to exclude. Item 5 needs a NON-ZERO entry — MYM on Fri 2026-07-31 (typical ~60 pt stop sizes to 8). Provenance caveat: bar_time is 11:45:00 ET on a clean 15m boundary but the request arrived 12:07:59 ET, right after the alert was saved, and saving an alert can make TradingView re-evaluate and fire — genuine alert(), but not a clean real-time delivery.",
    "2026-07-28 — M1 item 10 (notification channel) DONE. Fired one WARNING-level notification via the deployed FileAckNotifier (agent-triggered, in-container, PYTHONPATH=/app/core required — the deployed ops/c1_rail_telemetry.py predates the standalone-bootstrap fix on main; no order/sizing/dry_run/execution-state touched): alert_id=6ceffdec-c4b4-4bae-bd97-875020534a3b, ts_utc=2026-07-28T12:06:07Z, written to /data/c1_rail_alerts.jsonl (verified by direct host read). Operator (Joshua) then read the alert and wrote the ack via FileAckNotifier.acknowledge(alert_id, operator=\"joshua\") from a native-PowerShell in-container call, producing /data/c1_rail_alert_acks/6ceffdec-c4b4-4bae-bd97-875020534a3b.ack.json (acked_utc=2026-07-28T12:12:23Z) — verified by direct host read, not just the operator's is_acked=True report. The agent generated the alert; only the operator wrote the ack, per the FileAckNotifier docstring's own bar (\"a test double alone cannot pass M1\"). Arm gate unmoved: rail dry_run=true throughout; item 5 remains the only item still owed before RESOLVED can be considered (plus operator_signoff).",
    "Item 8 escape clause exercised as written when this line was authored — CrossTrade order_id idempotency was then unproven. It is now DISPROVEN by the 2026-07-27 SIM CHAIN_OK session (see note below): a resent payload carrying an unchanged order_id executed as a brand-new order rather than being deduplicated. No correctness claim in the sizing/reconcile logic itself rests on order_id idempotency — this is a live-fire operational hazard, not a spec violation. Operational consequence: every payload's order_id/bar_time tag MUST be freshly generated before each fire; a re-sent file is not a safe no-op.",
    "Item 11 attested by suite-level regression (124 passed) plus the mutation test recorded in offline_tests_note (07-27 original + 07-30 re-attest on the pinned deployed build); the intended-becomes-executed-base assertion is absent by design and its replacement is drilled by partial_fill_confirmed_base.",
    "2026-07-30 — mutation-sensitivity RE-ATTESTED on the pinned deployed build (report filed into this artifact after a report-only desk pass left no durable record). Plant-and-revert of the 07-27 exercise against bytes matching all six fixture_hashes: permissive add fallback (qty 60 submit=true) caught by rejected_entry_no_add + restart_confirmed_base; flat dropped from pre-sizing early return caught by exit_on_equity_failure[flat] ([exit] control still PASS). Both mutations reverted; ops/c1_sizing_host_reference.py hash restored to ecd292d2…; drills 9/9 green. Arm gate unmoved — item 5 + operator_signoff still owed.",
    "2026-07-27 SIM CHAIN_OK session (item 6 evidence + item 8 finding). Armed 17:38:09 UTC. Intended chain PASSED: entry event_id=7f80b4be-4b90-4097-bd3b-c927bc3200a5 (order_id dj30_mym-entry-B4-mym-entry-2026-07-27-simchain1) → CrossTrade Alert History RECEIVE/VALIDATE/EXECUTE all Success → Tradovate Filled BUY MYMU6 qty 8 @52,240.00 → flat event_id=0a44903a-7635-4a65-ae72-9be362e8c8a4 → CrossTrade Success → Tradovate Filled SELL MYMU6 qty 8 @52,241.00, confirmed flat on Tradovate (\"No open positions\"). Operator-attested BrokerEvidence appended to /data/c1_broker_evidence.jsonl and reconciled via `python ops/c1_rail_telemetry.py --events /data/c1_rail_events.jsonl --event-id 7f80b4be-4b90-4097-bd3b-c927bc3200a5 --evidence /data/c1_broker_evidence.jsonl` (required PYTHONPATH=/app/core — see the standalone-CLI import-bootstrap fix landed the same day) → printed CHAIN_OK. Separately, an UNINTENDED second live fill occurred: the rail was left armed after the intended test completed (no disarm was performed between 17:49:56, when the flat confirmed, and 18:23:38, the next restart). That restart re-read /data/c1_rail_config.json, which still held dry_run:false (the file had never been edited — only the process had been bounced), so the host rebooted straight back into DRY_RUN=false. At 18:25:28 the identical, unmodified b4_mym_entry.json payload (same order_id) was POSTed again and CrossTrade executed it as a fresh order — event_id=395d7156-c080-4559-aba0-d0ef29cd7529, Filled BUY MYMU6 qty 8 @52,295.00, Sender=Python/Origin=External confirmed via the CrossTrade Alert History payload inspector. No SL/TP rode the position (by design — B4 payload semantics); it sat open 34 minutes until closed manually on Tradovate at 18:59:05 @52,353.00 (net realized +$205.60 across both fills — favorable by chance, not a validation result). Rail disarmed 19:39:00 UTC; confirmed DRY_RUN=true in the subsequent boot line. Root cause: `fly apps restart` re-reads the config file, it does not reset it — the dry_run edit and the restart are two separate required actions, in that order, and disarm must be the very next action after a confirmed fill with no interval. Full timeline: docs/notes/rail_build/RUNBOOK.md B7 arming log, 2026-07-27 entry.",
    "2026-08-06 — DELETED prior notes[2] send-shaped gate sentence (\"Next dry_run=false entry/add send remains gated on RESOLVED.\") per claim-alignment C22. Addendum 2026-07-31b moved the trigger send → arm; a corrected send-shaped sentence would still be a fifth copy of an invariant with one owner. Enforcement lives in `ops/c1_rail/c1_rail_arm.py::m1_acceptance_reason`. ⚠ **HARDENED 2026-08-09:** the arm path now invokes `scripts/validate_c1_monitoring_acceptance.py` with `--require-resolved` semantics (fail closed on any validation error) — a status-only forged `{\"status\":\"RESOLVED\"}` no longer clears the gate (programme audit 2026-08-08 §5 item 4). Item 5 is undischargeable **while** no strategy is deployed to emit a signal (contingent, not a permanent property). `status` remains CODE_LANDED; no event ids changed.",
    "2026-08-07 — ITEM 5 ORIGIN RE-POINTED (not discharged). S2 ADR `docs/adr/2026-08-07-loop-s2-signal-host-fork.md` expressly supersedes the TradingView-only signal-origin limb: forward discharge is a real strategy signal from the ruled Python-native host (daemon B1 POST → listener, expected non-zero sizing, dry_run=true). Prior notes that name REAL TV strategy entry remain historical. dry_run_strategy_signal_event_id stays unset; status remains CODE_LANDED; no event ids fabricated; operator_signoff still null. Daemon build not authorized by S2 alone (waits S2b + build GO).",
    "2026-08-08 — S2b Accepted + build ADR Accepted + operator build GO. Daemon infrastructure authorized warm with emit_enabled=false. Item 5 still unpaid — needs real strategy-signal B1 from the daemon at non-zero dry-run sizing; no event ids fabricated.",
    "2026-08-07 — S4 EventLedger EQ schema CODE_LANDED (pre-fill). BrokerEvidence gained additive optional fields: intended_price, fill_slippage_pts, fill_latency_ms, commission_usd, signal_origin, exit_fill_price, exit_slippage_pts. Pre-S4 overlays remain loadable. status stays CODE_LANDED; no fill evidenced; operator_signoff still null. Q-MONSURF-1 F2 limb unblocked (rail retained).",
    "2026-08-19 — Deployed-vs-main skew (open since at least 2026-08-09, actually since 2026-08-02 — the running image was never redeployed in between) CLOSED via a fresh redeploy from origin/main @ 31fd642. Surfaced by the weekly c1-rail persona-panel-proposed safety check, when ops/c1_rail/c1_rail_arm.py was found unreachable in-container at its documented path (the deployed layout was still flat, pre-2026-08-03-move). All six deploy preconditions walked in order and host-verified; dry_run=True/armed_until=None confirmed both before and after, boot line + health clean. fixture_hashes re-pathed and refreshed from fresh in-container hashes taken post-deploy (see the dated fixture_hashes_note entry above for full detail). status remains CODE_LANDED; item 5 + operator_signoff still the only bars to RESOLVED; no order, no position, no arm at any point.",
    "2026-08-20 — CI-red investigation (PR #62), NOT a re-pin. tests/test_validate_c1_monitoring_acceptance.py::test_live_artifact_records_the_open_skew_rather_than_hiding_it fails on Linux CI (GitHub Actions checks out LF; this pin is CRLF). Verified byte-for-byte that all six fixture_hashes are exactly the CRLF encoding of the current git HEAD content for each file (CRLF-normalizing each pinned file's working-tree bytes to LF reproduces that file's own current git blob hash) — i.e. no code has drifted from main since the 2026-08-19 refresh above. Considered re-pinning to the LF/git-blob hashes to turn CI green, then stopped: the fixture_hashes_note's own dated \"CRLF CORRECTION\" entry already investigated this exact question and found the deployed bytes are genuinely CRLF (the flyctl deploy path copies Windows working-tree bytes into the Linux image without normalizing), so an LF re-pin would assert bytes that likely do not match what the host actually runs — the same mistake the 08-02 entry made and later corrected. Operator decision: leave fixture_hashes untouched; this CI check stays red. It is structural, not a staleness signal — as long as the deploy pipeline copies CRLF bytes into a container and Linux CI checks out LF, this specific test cannot pass without either normalizing the deploy pipeline's checkout or re-deriving the check itself, neither of which this note or PR #62 attempts. status remains CODE_LANDED; item 5 + operator_signoff still the only bars to RESOLVED; no fixture_hashes changed; no order, no position, no arm at any point."
  ]
}
