# MSL first slate — three cards (Stage 0–1 only; $0 · K=0)

**Status:** RATIFIED 2026-08-12 — order elected **C2 → C3 → C1** (Board B2) · parent charter RATIFIED
**Parent:** [MSL charter](../../spec/2026-08-12-msl-manual-sourcing-loop-charter.md). A card licenses **charter steps 1–4 only** (dedup/door-check/$0 screens/cheap falsifier). No card becomes a candidate until the **operator's B4 GO**, executed as a G0 freeze by the campaign manager (charter step 5 — no self-freeze). No Pine is authored and no K is spent before that.
**Design box every card is written into** (sources pinned in the charter Reads line): chunky per-trade R with hard stop · k=1 independent entries, **no pyramiding** (TNEC N-SHAPE) · wide stop (cost tax < ~0.12R only at ≥40-tick stops, ENV-1) · high-WR / truncated-loss shape (Q-TXG-1 bar names "demonstrably different loss-side shape" as the re-entry route — a designed shape is a claim, **demonstrated only at scoring**) · flat by 16:00 ET · worst EOD day ≤ $750 and $200 all-win day **at one declared design point** · no promised pass speed.
**DSR discipline (all cards):** screening floor = `floor_at_k(K_intrinsic)` — **0.65 at K_intrinsic=1 on every family** (ADR 2026-08-04: banks are disclosure, not a gate; per-family floors are the retired summed-K form). Family banks disclosed per card below.
**Expectation setting:** the honest prior is that cards die at Stage 1 — 8 consecutive zero-yield closes since 2026-08-08 across sibling lanes. A $0 kill with a registry entry is the channel *working*.

---

## MSL-C1 — MYM failed-break reclaim (session-scale, exit-managed)

**Direction:** fade the *failure* of a break of **prior-day RTH extremes (PDH/PDL)** — the one level class this card names, chosen by mechanism reasoning with zero data contact (the most visible, most stop-clustered reference class). Entry on reclaim confirmation at 15m timing resolution; wide stop beyond the swept extreme; truncated-loss exit management does the archetype work. Any additional level class (overnight extreme, OR boundary) charges K_intrinsic +1 each and is **not** licensed by this card.
**Mechanism story to sharpen at Stage 1 (Req 1a):** stops cluster at visible prior extremes; a sweep that fails to find follow-through forces the breakout cohort to unwind — the constraint (level + failure) must SELECT the trade; delete/flip tests mandatory, scored only on the declared IS partition. ⚠ Adjacent precedent: ADR 2026-07-26 §2-A ruled a bare "stops get hunted" story inadmissible, and SLR-MYM-1's two framings failed delete/flip — C1's framing must be constraint-selective where those were not, or it dies at Stage 1 (correctly).
**Role-asymmetry note (exit side):** the USOIL spike-fader entry rejection explicitly does not bar probing a fade signal in the exit/filter role — "a fade *signal* may still be probed as an exit/filter without clearing this entry-rejection" (`docs/rejected_candidates.md`, USOIL spike-fader entry; role-asymmetry as a (mechanism, role) dedup axis per ADR 2026-06-14 §3). Scope: that entry's own bar — not a registry-wide clean bill. C1's **entry-side** admission rests on the SLR route-① precedent + delete/flip alone.
**Instrument rationale (mechanism-independent):** finest Dow granularity ($0.50/pt); the SLR route-① precedent is native to MYM; disclosure: K_banked(MYM)=2 (ST-EH-1 + Q-TXG-1 cell, manifests).
**Occupancy:** Board **B8** releases `MYM1!` for new non-Striker MSL / Tradeify-shaped research and G0 ([occupancy ADR](../../adr/2026-08-12-msl-mym-occupancy-release.md)). S1 keep-warm + Striker redeploy bar stand. Door-check still answers S3/F2-occupancy in writing citing that ADR. **Slate order places C1 third** (after C2/C3) so the campaign does not idle on B8.
**Route declaration plan:** index instrument ⇒ single-index intraday OHLCV **RAISED BAR** binds. Declare **route ①** citing the SLR-MYM-1 precedent (mean-reversion-at-a-level is outside the bar's all-momentum/continuation evidence — ruled CLEAR and surviving; closure §6/§8). Cite it; do not re-argue it.
**Known graveyard adjacencies (dedup starting set, to be executed):** SLR-MYM-1 (Stage-0 FALSIFIED; framings failed delete/flip; mechanism never tested) · opening-range continuation on MYM (dead, 7 gates) · the 2026-08-10 dense-1m packet's "forbids PDH/PDL θ" bar — **Stage 1 verifies whether that bar is lane-scoped or binds this card** · Q-TXG-1 lane bar (new mechanism, non-transfer) · implied-SR of the designed geometry (disclosure only — not a kill; [`ADR 2026-08-13`](../../adr/2026-08-13-implied-sr-report-only-fade-reopen.md)).
**Stage-1 kill list:** delete/flip on the level-selection constraint (IS partition only) · cost-law at $2.82 RT vs designed span · $200/$750 limbs at the declared design point · entry-rate honesty (levels/day × failure rate) — **if < ~1/week, N-ACT FAILS as a solo construct: kill or redesign, don't disclose** · occupancy answered via B8 ADR. Implied-SR is printed, not a kill.

## MSL-C2 — MGC session-scale displacement construct (non-index escape hatch)

**Direction:** fade a **failed** extension of the **London session high/low** (formed before COMEX RTH) into the COMEX open — reclaim after a break that does not follow through; structural stop beyond the swept London extreme; truncated-loss exit; flat by 16:00 ET; k=1, first valid signal per session. Frozen a priori at Stage 1 (zero data contact). Explicitly **NOT** a settlement/fix-flow bp-scale idea; **NOT** Asia-range; **NOT** join-on-confirmation.
**Instrument rationale:** MGC sits **outside** the single-index intraday OHLCV domain bar (not an index future); symbol unoccupied (no rail claim); instrument standing per `ops/instruments/MGC.md` — "RE-ENTERED — K-void cleared; class-attested; not elected" — and TNEC-1 step 2 / edge-cohort L3 re-entry. Guardian-era gold intuition is available as *design* prior only (a transfer is barred — new mechanism only).
**Known graveyard adjacencies:** R8 gold PM-fix δ-extraction SCREEN-FAIL (causal public residue 1.3–3.2 bp vs 6.3–10.3 bp hurdles) — the metals-fix *family* is dead; the MGC DEAD row is family-scoped, verify scope at dedup · Guardian→MGC transfer cell DEAD(N-SURV) (bust 42.2%/72.4%/16.5%) — the *transfer* direction is barred and its loss-side shape is the anti-pattern · envelope marks MGC **NON-EMPTY-CONDITIONAL(power)** ⇒ Clause-N confirm-power ≥ 0.50 is a mandatory Stage-1 screen.
**Framing + door-check:** **R-FRAMING = §2.1** (ratified) — non-index cards proceed; record §2.1 framing in the door-check. Stage-0 registered `bars: free-data-5th-leg-snag-closed-2026-07-01` on [`MGC.md`](../../../ops/instruments/MGC.md) (#770) — door-check non-vacuous; SNAG answered CLEAR by domain mismatch under §2.1 (see Stage-1 record).
**Stage-1 kill list:** re-price the cost floor at MGC's own tick/commission schedule (firm_rules comments: MGC $1.06/side; envelope RT$ table — never inherit $2.82) · Req 1a delete/flip on the displacement constraint (IS partition) · Clause-N power ≥ 0.50 at realistic event frequency · $200/$750 limbs at the declared design point. Implied-SR is printed, not a kill ([`ADR 2026-08-13`](../../adr/2026-08-13-implied-sr-report-only-fade-reopen.md)).

## MSL-C3 — M2K construct (distinct small-cap complex)

**Direction:** open — the card pins the instrument and the box, not the mechanism. The campaign manager authors **2–3 mechanism stories frozen before any data contact** (mechanism-causal, not envelope-derived; authoring order recorded); **≤1 story proceeds to scoring**, elected by delete/flip survival in a frozen priority order declared with the stories; each additional story that reaches scoring charges K_intrinsic +1 (EM0 catalogue ≤3).
**Instrument rationale (mechanism-independent):** envelope **NON-EMPTY (full)**; symbol unoccupied at the full 80-micro cap (no rail claim); a genuinely distinct index complex (small-cap flow structure ≠ Dow/NDX, where the estate's graveyard is concentrated); $5/pt with $0.50 ticks ⇒ same $2.82 RT arithmetic ⇒ 4× hurdle ≈ **2.26 RTY pts/trade**. Disclosure: K_banked(M2K)=0 — the screening floor is the same 0.65-at-K_intrinsic=1 as every family (no "cheapest bar" claim; that comparative is void under ADR 2026-08-04).
**Route declaration plan:** M2K is a US equity-index future ⇒ the RAISED BAR binds. **Default route: the SLR MR-at-level precedent** (route ①, mean-reversion-at-a-level). A temporal-selectivity criterion is admissible **only** by explicit operator election acknowledging the 2026-08-12 Q-TNEC-CON-5 pause ("dense-1m OHLCV temporal-selectivity lane default paused pending a new modality or non-route-① thesis") and stating why session-scale M2K work sits outside that paused lane — pick ONE route at Stage 1 and record it.
**Known constraints to read first (Stage 0):** the edge-cohort ADR §2-C L3 verbatim — **"M2K's one-shot bank rule + WSTRUCT sequencing + W4 cost-dry-run gate stand"** — and `ops/instruments/M2K.md`: "Any probe electing M2K spends the one-shot bank and must be sequenced against WSTRUCT-M2K-1 by the operator first." The campaign manager records what the one-shot spends and the WSTRUCT sequencing ruling **before** electing this card.
**Stage-1 kill list:** the L3 one-shot + WSTRUCT reading · route declaration · delete/flip (IS partition; frozen story order) · cost-law at 2.26 pts · **Clause-N confirm-power ≥ 0.50 at the story's event rate** (rebalance-class stories are the D3/D7 monthly-frequency hazard) · $200/$750 limbs at the declared design point. Implied-SR is printed, not a kill ([`ADR 2026-08-13`](../../adr/2026-08-13-implied-sr-report-only-fade-reopen.md)).

---

## Election (Board B2 — locked)

**Order: C2 → C3 → C1** (serialized — one card in flight at a time; operator TV seat is the bottleneck). C2 first because R-FRAMING=§2.1 clears its framing gate with no B8 wait; C3 next (L3 one-shot + WSTRUCT read before flight); C1 third after occupancy release (B8). A card that dies at Stage 1 hands its slot to the next; **three Stage-1 deaths without a G0 triggers a Board review of the slate-generation method** (plan §7), and the charter's yield falsifier (6 consecutive pre-G0 deaths across ≥2 families, or 12 weeks with zero G0s) sits above it.
