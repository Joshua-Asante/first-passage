# Step-0 slate — daily-geometry mechanisms on the non-index triad (Tier-1 screen queue)

**Status:** `PROPOSED` — operator marks owed (§6). $0 spent, K=0 bound at authoring; every screen
is its own frozen prereg on GO, in the `H-DSTRUCT-MNQ-1` mold.
**Type:** commissioning slate (MSL first-slate lineage, minimal-spec style) — decides nothing;
queues Tier-1 mechanism screens for the deep-iteration-lane program.
**Date:** 2026-08-18 · **Authors:** Claude Code (drafter), operator thread (JA commission:
"mechanism-by-mechanism testing → Step-0 slate")
**Pre-Q gate:** D: direction-class daily/intraday OHLCV rows deleted from the candidate set —
test: four independent kills on the class (MNQ ledger N5 · Q-WLEGB-1 · SIZEDIV F3 ·
H-DSTRUCT-MNQ-1), documented artefact class, not a forbidden mechanism-prejudgment. S: corpus
compressed to three runnable geometry rows + four parked rows with bars quoted. A: rows carry
frozen screen sketches so each GO costs one session.

---

## §0 — Rule-0 reads (this session, worktree HEAD `4062562`, 2026-08-18)

| Source | Supplies |
|---|---|
| [`deep-iteration lane charter`](../adr/2026-08-16-deep-iteration-lane-charter.md) (full) | Counters: abandoned 1 (consec 1/2) · falsified 0/2 · no active campaign; triad bar schemas (1d/1h/1m) priced **$0.0000** both windows (addendum table); §7 steps 2–4 (`--lane deep` flag, doc scoping, skill wiring) licensed, **not yet built** (verified: `register_search.py` knows only `blind\|mechanism-first`) |
| [`TNEC-1`](../spec/2026-08-08-tradeify-necessary-conditions-target-spec.md) | N-ACT/N-SURV/N-EDGE/N-SHAPE/N-SIZE roster; **2026-11-08** falsifier clock; book co-idleness clause |
| [`ops/instruments/MNQ.md`](../../ops/instruments/MNQ.md) (DEAD table incl. today's row) | The four-kill profile on direction-class daily/intraday OHLCV structure (N5 · Q-WLEGB-1 · SIZEDIV F3 · `H-DSTRUCT-MNQ-1` NULL 2026-08-18) |
| [`ops/instruments/MGC.md`](../../ops/instruments/MGC.md) / [`MCL.md`](../../ops/instruments/MCL.md) / [`M6A.md`](../../ops/instruments/M6A.md) (full) | MGC `RE-ENTERED — class-attested`; MCL `OPEN — geometry-cleared, mechanism-owed` (cost pin 5.3423 bp/RT @2023 median; **W1 monthly roll ~14% session exclusion; W3 session-window ruling owed**); M6A Stage-2 ex-FOMC N=450/floor 0.0924 POWER-bound; **WHO-track 2026-08-14: FX event-window doors died** |
| [`ops/instruments/MECHANISMS.md`](../../ops/instruments/MECHANISMS.md) | Compression-family cells are all ENTRY-role (XAUUSD F4 design-mismatch · CON-2 gross+/net− · CON-3 AMBIGUOUS-HOLD); no conditioner-role cell exists; no daily-range-geometry class yet (`NEW` id lands with a prereg commit, never with this slate) |
| [`docs/rejected_candidates.md`](../rejected_candidates.md) (greps pasted in session log 2026-08-18a) | Gates-on-ORB kills (GEX/T10Y3M/Friday — G-era-confound battery imported); EIA/NG/TAS direction kills; both domain-level raised bars (index intraday OHLCV directional timing 07-21; EOD-adversity 08-02) |
| `discovery_manifests/*.json` (all 15, listed this session) | Family banks for disclosure: GC/MGC 3,177 (DISC-CAMP-0) · CL 1 (`fb_eia_cl_reversal`) · 6E/6J/6CL 1 (`fc_carry`) — **disclosure-only** per [K-bank ADR](../adr/2026-08-04-family-k-bank-disclosure-not-gate.md) |
| [`Q-POLFRONT-1 intraday-honest`](../../lab/analysis/c1/q_polfront_1_2026-08/RESULTS_INTRADAY_HONEST.md) · [MSL slate-2 design box](../adr/2026-08-13-msl-slate-2-design-box.md) | Sizing envelope R ≈ $75–200 at the $100K eval (conservative upper bounds); design box rr∈[2,3] · WR 0.30–0.42 · hard stop · k=1 · no pyramiding |

## §1 — Context (one paragraph)

The program (operator thread 2026-08-17→18) tests simple mechanisms individually, then composes
survivors into a daily-cadence framework inside Tradeify's constraints. Two Tier-1 results this
week fixed the slate's shape: direction-class daily structure is four-times dead on index
micros, and every validated structural fact (W layer) dies at expression, not at existence. The
surviving unclaimed ground is **geometry**: mechanisms that predict *range, session shape, and
event concentration* rather than direction. Geometry attacks the estate's actual binding wall —
`cost_R = rt/range`, so range-state prediction is cost-hurdle prediction, the gate that killed
D5, H-OD-1, and CON-2. Instruments: the non-index triad (no raised bar, no F2 guard, lane
charter's own target set, $0 bar data), where MCL's ledger already says "mechanism-owed."

## §2 — The slate

Every row: screen is **K=1** (one frozen object, one sign, no sweeps), battery = the
`H-DSTRUCT-MNQ-1` four limbs (n-floor · block-CI · halves · **base-rate-matched
clustering-preserving placebo as primary**), verdict SIGNAL / NULL / AMBIGUOUS, measurement-only
(no outcome promotes; SIGNAL routes to a lane prereg under the charter's §2 predicates).

### S1 — Daily range-state persistence · MGC + MCL (two single-instrument screens, S1a/S1b)

> **S1a (GC) — GO'd and RUN 2026-08-18: `NULL`, near-miss.** Conditional hit rate 0.5299
> (n=451); 3 of 4 frozen limbs pass (n-floor, halves, placebo p=0.0095); the 60-day
> block-bootstrap CI lower bound (0.4545) fails by 4.55pp. Adversarially verified before trust
> (4-lens + synthesis workflow; one MUST-FIX caught and fixed — CI block size — both
> corrections verdict-preserving). Full detail:
> [`RESULTS_S1A.md`](../../lab/analysis/_inbox/rangestate_gc_2026-08/RESULTS_S1A.md).
>
> **⚠ S1b RAN 2026-08-18 → raw `SIGNAL`, downgraded to `NOT-CONFIRMED` — the shared placebo
> is invalidated, and this row's own "S1b is a live prior for it" framing is retracted.**
> Adversarial review found the four-limb battery's placebo does not control for True-Range
> autocorrelation (20/20 zero-mechanism AR(1) surrogates cleared it at a higher rate than
> either real dataset); S1b's raw SIGNAL also failed a regime-concentration test (crisis-year
> cluster removal flips it to NULL). **S2 and S3 below are PAUSED, not GO-able, until the
> structural fix named in the audit note lands.** Full detail:
> [`RESULTS_S1B.md`](../../lab/analysis/_inbox/rangestate_mcl_2026-08/RESULTS_S1B.md) ·
> [audit note](../../notes/audits/2026-08-18-block-shuffle-placebo-does-not-control-for-tr-autocorrelation.md).
>
> **✅ OFFICIAL corrected-null re-score landed 2026-08-18 (same day, operator PROCEED per the
> frozen spec's ADDENDUM-1 A16):** the structural fix (IAAFT normal-scores null + L4 by-year
> regime limb; [frozen battery spec](../spec/2026-08-18-magnitude-persistence-corrected-null-battery.md))
> ran officially. **S1a (GC) = NULL** (driving L2+L4; obs at the 8.4th pct of GC's own
> linear-ACF band — the near-miss dissolved, CASE A) → counts toward §4's FALSIFIED tally.
> **S1b (CL) = SIGNAL-GENERIC** (69th pct of its own band — canon-attributed volatility
> clustering; L4 boundary-exact 6/8 under the rule's own small-n exclusion, a disclosed
> prediction-miss adjudicated rules-govern) → **counts toward §4 RESOLVED, with A6 guard-rails:
> not a mechanism, no mechanism-owed discharge, no conditioner license.** Record:
> [`RESULTS_CORRECTED.md`](../../lab/analysis/_inbox/rangestate_corrected_2026-08/RESULTS_CORRECTED.md).

- **Frozen claim (sign = persistence):** `P(TR_{d+1} > median(TR, trailing 60d) | TR_d in top
  quintile of trailing 60d)` exceeds its block-shuffled null. Grounding: evidence-robustness
  limb (volatility clustering — five decades, every asset class, the ARCH/GARCH canon), not a
  WHO claim.
- **Use if SIGNAL:** participation/sizing conditioner for breakout-class hosts — trade the
  design-box archetype only in persistent high-range state, where the 4× hurdle is cheapest.
- **Dedup:** compression-family cells are ENTRY-role and index/CFD-resident (§0); the
  conditioner role on MGC/MCL is untested. Q-TNEC-CON-2's gross+/net− is the motivating
  adjacency, not a bar.
- **Data:** `ohlcv-1d` GC/MGC + CL/MCL ($0 per charter addendum; estimate pasted before pull).
  MCL: W2 weekend-bar drop + W1 monthly-roll handling frozen at prereg (TR spanning a roll day
  is excluded, not spliced).

### S2 — Overnight→day-session range transmission · MGC + MCL

> **⚠ PAUSED 2026-08-18 — do not GO.** Queued to reuse the S1 battery, which is invalidated
> (see the S1a row above and the audit note). Un-pause only after a corrected, autocorrelation-
> matched null lands and is itself GO'd for use here.

- **Frozen claim:** overnight-session range state (top quintile) predicts elevated day-session
  range beyond the unconditional base — same battery; the conditioning is known **before** the
  session opens (fresher than S1; the two rows are deliberately the same family at two
  information times, disclosed as such).
- **Use if SIGNAL:** morning participation switch for session-anchored hosts.
- **Dedup:** MSL-C2 London-range fade (MGC) was FALSIFIED as an *entry*; the
  `overnight-range-failed-extension-fade` class rows are entries; sessconf/eodadv (MNQ) were
  exit-clock studies. Transmission-as-conditioner is untested.
- **Data:** `ohlcv-1h` ($0 est.); session windows per instrument frozen at prereg — **MCL W3
  explicitly inherited** (an energy session ruling is part of the prereg, equity-RTH constants
  are not valid there).

### S3 — Event-day range concentration · MCL only (EIA Wednesday 10:30 ET)

> **⚠ PAUSED 2026-08-18 — do not GO.** Same reason and same lift condition as S2 above.

- **Frozen claim:** post-release session TR on EIA days exceeds matched non-event Wednesdays by
  a frozen factor (≥1.25× at the median) with year-stability (≥6 of 8 years) — geometry only,
  no direction.
- **Use if SIGNAL:** event-window participation component (~1 day/wk alone — co-idleness
  disclosed per TNEC book clause; composes with S1/S2 hosts, never a standalone N-ACT claim).
- **Dedup:** every prior event kill on this family was **direction/flow** (H-FBEIA-1
  wrong-signed; NG bracket; TAS settlement; MGC LBMA fix). The range-concentration claim is
  untested. **M6A is dropped from this row** — its event-window doors died at the 2026-08-14
  WHO track; re-adding it needs fresh grounds, not this slate.
- **Data:** `ohlcv-1h`/`1m` MCL ($0 est.; 2023 chunks already cached); EIA release calendar from
  the primary source (verify-source: EIA's own schedule incl. holiday shifts, never a scraped
  mirror).

### Parked rows (visible restraint — not in the queue, bars quoted)

| Parked | Why | Re-entry bar |
|---|---|---|
| P1 prior-day-level geometry (PDH/PDL class, triad) | Adjacent kills: reclaim entries (MYM/M2K), CON-4 hold, MNQPROX power-void | A sharper object than distance-to-level, stated a priori |
| P2 W-layer weekly fact | Validated, no venue-legal expression (WSTRUCT cost · Q-WLEGB-1 · DSTRUCT) | Asymmetric-payoff claim (weekly) or intraday-carry mechanism argument (sub-weekly) — the standing bars, quoted |
| P3 ICT D bear-FVG | Both MNQ expressions dead (MNQFVG-1, MNQPOOL-1); route-1 on MNQ×ict-liquidity "presumptively exhausted" | Operator eyes-open GO naming why a triad/weekly expression escapes the level-reach wall; else component-shopping |
| P4 M6A event-window | WHO-track deaths 2026-08-14 (event-window door named) | Fresh grounds at the sourcing layer, not a slate row |

> ⚠ **2026-08-18 — Q-EXPR-1 H1 admission screen (append-only intercept).**
> [`Q-EXPR-1`](closures/Q-EXPR-1-closure-resolved.md) `RESOLVED` (H1): a claim whose native
> horizon is weekly or daily is rejected at $0 unless it names a same-horizon (session /
> flat-by-16:00) expression *before* any build. P2's "asymmetric-payoff claim (weekly)"
> re-entry and P3's "triad/weekly expression" re-entry do **not** clear that screen on
> their own. Body of the table unedited (Trap #12).

## §3 — Queue (P(changes verdict) ÷ cost)

**S1a (MGC) → S1b (MCL) → S2 → S3.** S1 first: cheapest data (1d), strongest prior, and its
NULL would devalue S2/S3 before they spend anything (persistence is the family's load-bearing
premise). S2 and S3 stop early if S1 double-NULLs — see §4.

## §4 — Falsifiable hypothesis (the slate's own)

**H-SLATE:** the daily-geometry class on the non-index triad contains at least one Tier-1
regularity — i.e. **≥1 SIGNAL among the GO'd screens** under their frozen batteries.

- **RESOLVED** — any screen returns SIGNAL → that row routes to a deep-lane prereg (charter §2
  predicates; K, partition, SPA, cost arithmetic all live there, not here).
- **FALSIFIED** — all GO'd screens (minimum S1a+S1b+S2) return NULL → the class is exhausted at
  Tier-1 on the triad; recorded here + rejected-registry row; re-entry bar = a **named
  mechanism** (who is constrained, at what clock), not another geometry functional.
- **AMBIGUOUS** — screens blocked on data/defect, or <3 screens run by **2026-09-15** with marks
  given (inertness: report at the next quarterly audit, mirror of the lane charter's own limb).

**⚠ Clock paused 2026-08-18, not stopped.** S1a and S1b both ran, but their shared placebo was
found invalidated (autocorrelation-uncontrolled null; [audit note](../notes/audits/2026-08-18-block-shuffle-placebo-does-not-control-for-tr-autocorrelation.md))
— neither a RESOLVED nor a FALSIFIED-contributing read can be drawn from either screen as run.
S1a's raw `NULL` and S1b's raw `SIGNAL→NOT-CONFIRMED` both count as **zero** toward §4's
RESOLVED/FALSIFIED tallies until re-run under a corrected null. The 2026-09-15 inertness date
is **not** extended by this pause — if the structural fix and a re-run haven't landed by then,
the slate reports AMBIGUOUS at that date regardless, same as any other stall.

**✅ §4 STATUS: `RESOLVED` (2026-08-18, official corrected-null re-score, operator PROCEED).**
Tally under valid tests: S1a (GC) = valid NULL; **S1b (CL) = SIGNAL-GENERIC → the "≥1 SIGNAL
among GO'd screens" trigger fires.** Per this section's own RESOLVED consequence: the row
routed to a conditioner-engineering prereg per D2, then **PARKED** by
[`Q-CONDVAL-1`](closures/Q-CONDVAL-1-closure-falsified.md) (`FALSIFIED` 2026-08-18; O2
discharged — the lift does not buy a material fraction of `hurdle_4x` at the N-EDGE cell).
That prereg is not electable. **S2/S3
pause states after the fix:** S2 un-pause condition (1) is now satisfied (diagnostic PASS on
both instruments); conditions (2) stage-1 cheap falsifier and (3) stage-2 null design review,
plus the operator GO, remain outstanding. S3's un-pause (matched-day prereg redesign + design
review) is unchanged and not IAAFT-gated.

## §5 — Forbidden moves

- **No direction-class rows** without a named mechanism — the four-kill profile is the deletion
  test; re-admitting "one more indicator functional" is the tempting move this slate exists to
  block.
- **No sweeps inside screens.** A second quintile boundary, window length, or event offset is a
  new K and voids the screen's declaration (retroactive-K rule imported verbatim).
- **No index micros under this slate** (raised bar + F2 terrain); no M6A event row (P4).
- **Geometry ≠ edge.** No screen output enters any findings tier as an edge claim (MCL C2
  wording is the standard); SIGNAL licenses a lane prereg, nothing else.
- **No pull without the estimate output pasted** and the operator GO recorded (databento Rule 1;
  the charter's $0 table is a price fact, not a pull authorization).
- **No post-hoc slate rows** without an executed dedup paste; no re-running a NULL with more
  shuffles (outcome-conditional re-testing).

## §6 — Marks owed (operator) & gate

| Mark | Content |
|---|---|
| M-1 | Per-row election: GO / HOLD / KILL for S1a, S1b, S2, S3 (queue order §3 default) |
| M-2 | Pull GOs ride the row GOs (each pull: estimate pasted → GO → pull, expected $0.00 each) |
| M-3 | Optional: license charter §7 steps 2–4 build alongside (owed regardless before the next lane campaign) |

Screen verdicts land as `RESULTS_*.md` per screen in `lab/analysis/_inbox/` or a triad theme
dir, each with its own frozen PREREG (the DSTRUCT pair is the template). **Gate:** the slate
itself closes **RESOLVED / FALSIFIED / AMBIGUOUS** exactly per §4's three triggers (any SIGNAL /
all GO'd screens NULL / blocked-or-inert by 2026-09-15); the closure record carries the typed
`## Iterate` block.

## §10 — Audit hooks (runnable)

```bash
# Slate status + this file's marks
grep -n "Status:" docs/briefs/2026-08-18-step0-daily-geometry-mechanism-slate.md

# Screens that have actually run (inertness check for §4 AMBIGUOUS)
ls lab/analysis/_inbox/ | grep -iE "s1a|s1b|rangestate|ovnight|eia" || echo "no screens run yet"

# The four-kill profile this slate's D-test rests on (must stay quoted, not paraphrased)
grep -n "four independent kills\|H-DSTRUCT-MNQ-1" ops/instruments/MNQ.md

# Lane charter counters unchanged by this slate (slate spends nothing)
grep -n "Running counts (canonical" docs/adr/2026-08-16-deep-iteration-lane-charter.md

# K banks cited are manifest-read, not prose
python -c "import json,glob; [print(json.load(open(f))['run_id'], json.load(open(f)).get('K')) for f in sorted(glob.glob('discovery_manifests/*.json'))]"
```

## Verification

```bash
python C:/Users/joshu/.claude/skills/brief-authoring/scripts/check_brief.py docs/briefs/2026-08-18-step0-daily-geometry-mechanism-slate.md
git log --oneline -1   # §0 anchor: 4062562 at authoring
```
