# T01 packet — close S2 as the enforced work boundary with the G3 launch repair landed (PR #436)

**Type:** cc_handoff (two-part: GLM work order by reference + coordinator closeout)
**Date:** 2026-09-20
**Executor, part A:** GLM, under the coordinator-authored [`2026-09-20-glm-s2-acceptance-continuation.md`](https://github.com/Joshua-Asante/first-passage/blob/12d6a6f/docs/briefs/handoffs/2026-09-20-glm-s2-acceptance-continuation.md) (committed `12d6a6f` on the #436 branch). **This packet does not restate or amend GLM's work order; where anything below differs from it, that packet governs GLM.**
**Executor, part B:** the coordinator (the operator or the Claude coordinator session), after reading GLM's return.
**Status:** DRAFT for coordinator review. Commit onto `claude/s2-enforcement-gaps-fab5e6` beside the continuation packet when accepted.
**Owning task:** [T01 in the Tradeify deployment checklist](../../superpowers/plans/2026-09-20-tradeify-deployment-checklist.md#t01--close-s2-and-make-real-worker-launch-reliable-500k750k). This packet **supersedes** the acceptance/merge content of [`2026-09-20-glm-s2-closeout-s3-dispatch.md`](https://github.com/Joshua-Asante/first-passage/blob/03ef76a/docs/briefs/handoffs/2026-09-20-glm-s2-closeout-s3-dispatch.md) (§1(1)–(7)) — its "G3 is not a merge gate" ruling is reversed by T01 — and **withdraws its step 6**: S3 packet authoring is T02's opening act. (Links to files that exist only on the #436 branch are pinned; they resolve relatively once this file is committed there.)
**Authority (part B only):** the ledger `docs/superpowers/plans/2026-09-18-full-e1-execution-slices.md` (one append-only acceptance entry); the PR #436 body; the merge of #436. Nothing else. A `DONE` in GLM's return supplies no permission; every acceptance sentence is the coordinator's.

## 0. Verified starting state (2026-09-20 ~15:45Z, read from `origin`; re-verify before acting)

- `origin/main` = `b703448`. **PR #436** head **`12d6a6f`** (= `03ef76a` + the continuation packet), base `main`, mergeable, 33 commits, **zero review threads, zero reviews**. `03ef76a` = `origin/main` merged (`b0ced74`) + independent acceptance review ([note](https://github.com/Joshua-Asante/first-passage/blob/03ef76a/docs/notes/audits/2026-09-20-pr436-s2-integrated-acceptance-review.md): **VERIFIED WITH CAVEATS, 0 BLOCKING / 9 ADVISORY / 10 NOTE**) + the G4 packet.
- S2 runs: fifteen-green on code-identical heads 35487909157 (`db667a2`), **35489657203 (`5ac3ad0`)**, **35493582848 (`bbafe68`)**; 14/15 on `4281d2e` (35494972519, root-caused = R-G3). Merge-head runs 35517586779 (`915035c`) and 35518729717 (`03ef76a`) were both **cancelled by the next push**; **35519689776 (`12d6a6f`) in progress** — the first head-bound run on the `main`-merged tree.
- **Workers stopped by the operator mid-task; GLM finishes both** (continuation packet §1): G3 `claude/s2-g3-resume-before-exec` @ `0ba2775` (one commit; packet repair + coordinator addendum A2-guardian-half and A4 funding-pending tolerance; **stopped before Windows verification, no Linux run, §7 empty — every commit-message claim unverified**); G4 `claude/s2-g4-store-invariants` @ `8c764d8` (**coordinator-pushed WIP of uncommitted edits, no tests, hooks bypassed, mid-rewrite of `claim_void_authentication`, possibly non-compiling**).
- Residuals: **R-G3** (SIGUSR1 reaches `runc:[2:INIT]` after setuid, before `execve`; Go dies; container exits non-zero < 0.5 s; fail-closed, ~1 in 12 payload starts on a fast host; kills real S3 workers) — G3; **A9-3** work-id prefixes `control_`/`event_` collide with supervision-object GLOBs — G4; **A4** README ~:156-170 "manage-units scope" wording — the continuation packet folds the code half (funding-pending tolerance) into G3 but names **no owner for the README sentence**: part B (§2 step 4); **OOM** terminal duality (test-only, disclosed); **bridge** never claimed. Review A3 (restart destroys running work), A6 (OOM stop Windows-proven), A8 (no guardian-exit watcher) are recorded S3 preconditions, not repaired (continuation packet §0.3).

## 1. What success looks like

**Part A (GLM) is complete when** its return is `RESOLVED` per the continuation packet §4: G3 and G4 finished with Windows regressions and one fifteen-green S2 run each on their own heads; merged into the integration branch with owned files byte-identical to their heads; §2.6 lines 1–3 + `check` on the merged tree; **the PR-path S2 run on the integrated head fifteen green, `invariants.json passed`, cleanup ok, `source_stable`, artifact read**; Codex round dispositioned; ledger entry "GLM continuation 2 — S2 acceptance candidate, 2026-09-20" listing A1/A2/A4/A5/A7/A9-3 closed vs A3/A6/A8 recorded; PR body updated. `DONE_WITH_CONCERNS`/`CHECKPOINT`/`BLOCKED` go to part B step 1 as-is.

**Part B (coordinator) — T01 is successful when, in this order:**

1. **GLM's return judged** (fable-judge posture: re-run the cited Windows selections or read the records; download and read the integrated run's artifact; `git diff <G3 head> <integrated head> -- <G3-owned files>` empty outside the expected touch points, same for G4; `campaign_probe.py`, profiles, ceilings, release literals byte-identical to `4281d2e`; no version bump landed without a `CHECKPOINT`). The two verdicts T01 requires are stated **separately**: (i) boundary evidence — the pre-G3 fifteen-green runs plus a head-bound run on the `main`-merged tree; (ii) **G3 readiness — the integrated post-G3 run is fifteen green with no `descendants` failure.** Report both even when both are ready.
2. **G3 is a merge gate.** A `descendants` red on the post-G3 head is a G3 defect — **no rerun allowance**; return to GLM with the artifact facts. The single R-G3-signature rerun (`descendants` work; payload exit non-zero < 0.5 s after the first RESUMED; no burner children; no OOM) exists only for pre-G3 heads. If G3 cannot reach fifteen-green inside T01's envelope, #436 is **not merged**; the boundary acceptance is held back and T01 returns `DONE_WITH_CONCERNS`.
3. **Every G4 item dispositioned.** A1, A2-store, A5, A7, A9-3: **landed** (named Windows test + record ID) or **deferred** (reason; named in the acceptance entry as a T02 precondition). A9-3 landed closes the work-id collision — reserved prefixes derived from the GLOB constants, not copied strings; deferred ⇒ T02's packet carries the S3 naming rule as the interim guard.
4. **A4 README sentence landed** (small docs commit on the integration branch, or delegated back to GLM in one line): `tools/qualification_verification/README.md` §S2 states verbatim "under the accepted polkit rule qexec can start arbitrary transient units including `User=root`; polkit contributes no containment for unit starts; hierarchy membership rests on the guardian's code checks and `bootstrap.py`'s `campaign_control` fixed-prefix check"; "manage-units scope" removed; grep `polkit|manage-units` across README, plans and `campaign_host.py` comments first. A docs-only push re-fires the S2 run; it must stay fifteen green (docs cannot change bytes under test, so a red here is a stability finding, not a reroll).
5. **Acceptance entry recorded, evidence-bound.** One append-only ledger entry `### Coordinator acceptance — S2 enforcement gaps (G1/G2) with G3 launch repair, 2026-09-20` naming: runs 35489657203 (`5ac3ad0`), 35493582848 (`bbafe68`), the head-bound `main`-merged run, G3's own run, G4's own run, the integrated post-G3 run — each with record ID, counts, `source_stable`/`capture_complete`, invariants, cleanup, checked-out SHA; steering findings 1–2 resolved at `9d7a731`; G3 accepted (with the observed `comm`/`exe` values for runc-init vs python if GLM captured them); the G4 disposition table; the Codex/CodeRabbit dispositions; the **S3 handshake contract sentence** — "a real worker's entrypoint must call the probe-side block-then-await resume helper before any work; the guardian resumes only after the observed init pid has exec'd the installed interpreter"; the A4 sentence; and the **disclosed-limitations list**: any deferred G4 item (as T02 precondition), A3 restart-destroys-running-work, A6 OOM stop Windows-proven, A8 no guardian-exit watcher, OOM terminal duality, untested isolation bridge, and "accepted work boundary only — no statistical execution, no N1/N2/Part A dispatch, no activation". The entry says "S2 ACCEPTED as the enforced work boundary; G3 launch repair landed" and nothing wider; the ledger's stopping lines stay.
6. **Merged.** #436 merged with a merge commit after: `skills (3.12)` green on the merge head; the post-G3 run green; review clear; `mergeStateStatus` not `BEHIND` (if `main` moved, merge it in — the S2 run repeats on the new head and must be fifteen green). Branch kept. PR body: "ACCEPTED as the enforced work boundary; G3 launch repair landed" + the limitations list. Merge SHA in §7.

**Not in scope:** the S3 packet, any S3 code, N1/N2/Part A dispatch, G5 verdict, result, seal, activation.

## 2. Steps (part B)

1. ⏸ Wait for GLM's return message. `git fetch origin`; record deltas from §0 in §7. Read the ledger "GLM continuation 2" entry and the integrated run's artifact yourself.
2. Judge per §1(1); state the two verdicts. `CHECKPOINT` from GLM (version bump) → rule on the smallest change and its blast radius before anything else; `BLOCKED`/red post-G3 run → §1(2).
3. Disposition G4 items per §1(3).
4. Land or delegate the A4 README sentence per §1(4); wait for the docs-push S2 run.
5. Write, `git diff --stat`, commit and push the acceptance entry per §1(5).
6. Merge per §1(6). Record the SHA. Update memory/STATE as the session's own closeout requires; T02 dispatch is a separate act.

## 3. Forbidden (part B)

Writing "S2 ACCEPTED" before the post-G3 integrated run is fifteen green; merging #436 with a `descendants` failure on the post-G3 head; re-rolling outside the single pre-G3 R-G3 rerun; accepting Windows results as Linux evidence; accepting a version bump that arrived without a `CHECKPOINT`; editing GLM's work order; authoring S3 code or the S3 packet; any activation, N1/N2/Part A dispatch, G5 verdict, result or seal; `git stash`; commits without `git diff --stat`.

## 4. Verification to cite (part B)

Linux (run ID, record ID, 15/15, 0 skips, `source_stable`, `capture_complete`, invariants passed, both owned-cleanup receipts ok, checked-out SHA): the `main`-merged head-bound run, G3's own run, G4's own run, the integrated post-G3 run, the docs-push run if §1(4) landed separately, plus `5ac3ad0`/`bbafe68`. Windows (record IDs from GLM's return, spot-re-run at least the named G3 regressions and one landed G4 test): §2.6 lines 1–3 and `check` on the integrated head. Byte checks: `campaign_probe.py` unchanged since `4281d2e`; worker-owned files equal to their heads after the merge.

## 5. Checkpoints

Report before writing the acceptance entry if either §1(1) verdict is not ready; before merging if `main` moved or the review has an open thread; immediately on any version bump, profile/ceiling/release-literal or probe-side change found in GLM's return.

## 7. Return

_Pending._ `RESOLVED` / `DONE_WITH_CONCERNS` / `BLOCKED` with: the two §1(1) verdicts; final integration head; merge SHA of #436; ledger entry heading; every Linux run cited with record ID / counts / checked-out SHA; Windows record IDs; the G4 disposition table (five rows); the A4 wording diff; review dispositions; the limitations disclosed; anything not verified.
