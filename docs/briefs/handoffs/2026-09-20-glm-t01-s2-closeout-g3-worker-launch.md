# GLM handoff — T01: close S2 as the enforced work boundary with the G3 launch repair landed (PR #436)

**Type:** cc_handoff (coordinator-bounded closeout + integration of two live sub-workers)
**Date:** 2026-09-20
**Executor:** GLM (continuation owner of the S2 integration branch). **Coordinator:** the operator, who accepts.
**Status:** DRAFT for coordinator review; dispatch after §0 is re-verified. Commit this file onto `claude/s2-enforcement-gaps-fab5e6` as your first act; keep §7 as your return.
**Owning task:** [T01 in the Tradeify deployment checklist](../../superpowers/plans/2026-09-20-tradeify-deployment-checklist.md#t01--close-s2-and-make-real-worker-launch-reliable-500k750k). This packet **supersedes steps 1–5 and 7** of [`2026-09-20-glm-s2-closeout-s3-dispatch.md`](https://github.com/Joshua-Asante/first-passage/blob/03ef76a/docs/briefs/handoffs/2026-09-20-glm-s2-closeout-s3-dispatch.md) and **withdraws its step 6** (S3 packet authoring is T02's opening act). (Links to files that exist only on the #436 branch are pinned to `03ef76a`; they resolve relatively once this file is committed there.) Its §1(1)–(6) acceptance wording carries over unchanged except where this packet says otherwise; where the two disagree, this packet governs.
**Authority:** branch `claude/s2-enforcement-gaps-fab5e6` / [PR #436](https://github.com/Joshua-Asante/first-passage/pull/436) and its worktree `C:/Users/joshu/multi_firm_operations/.claude/worktrees/pr-363-babysit-671b91`; merging the worker branches `claude/s2-g3-resume-before-exec` and `claude/s2-g4-store-invariants` into it; the ledger `docs/superpowers/plans/2026-09-18-full-e1-execution-slices.md` (append-only entries); `tools/qualification_verification/README.md` §S2 (A4 wording only); the PR body. Nothing else. You do not edit the workers' branches or worktrees; you judge and merge their returns. A `DONE` status supplies no permission; every acceptance sentence is the coordinator's.

## 0. Verified starting state (2026-09-20 ~15:30Z, read from `origin` and the local worktrees; re-verify before acting)

- `origin/main` = `b703448` (#439 over #437 `86d5ed1` over #438 `994a59f` over `f2606b0`). No S2 source file changed on `main` since `f2606b0`; #437 changed provisioning/cleanup under the S2 fixture (tree bindings, `docker_client` preflight).
- **PR #436**: OPEN, ready-for-review, head **`03ef76a`**, base `main`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE` (in progress: S2 supervision, `pytest (3.11)`, `Qualification execution boundary (2)`; green: `skills (3.12)`, `build (3.11)`, boundary (1), listener, daemon, CodeRabbit, semgrep). **Zero review threads, zero reviews.** 32 commits.
- Branch since `915035c` (G3 packet): `21f02b6` (prior GLM closeout packet committed) → `cf77edd` (the 09-19 GLM continuation/steering packets backfilled) → `b0ced74` (**`origin/main` `b703448` merged, clean**; the closeout's step 2 merge is done) → `03ef76a` (independent acceptance review [`docs/notes/audits/2026-09-20-pr436-s2-integrated-acceptance-review.md`](https://github.com/Joshua-Asante/first-passage/blob/03ef76a/docs/notes/audits/2026-09-20-pr436-s2-integrated-acceptance-review.md): **VERIFIED WITH CAVEATS, 0 BLOCKING / 9 ADVISORY / 10 NOTE**; plus the G4 packet [`2026-09-20-full-e1-s2-g4-store-invariants.md`](https://github.com/Joshua-Asante/first-passage/blob/03ef76a/docs/briefs/handoffs/2026-09-20-full-e1-s2-g4-store-invariants.md) ruling A1/A2-store/A5/A7/A9-3).
- S2 runs: fifteen-green on code-identical heads 35487909157 (`db667a2`), **35489657203 (`5ac3ad0`)**, **35493582848 (`bbafe68`)**. 35494972519 (`4281d2e`) 14/15 = R-G3. 35517586779 (`915035c`) **cancelled** (superseded by the `03ef76a` push). **35518729717 (`03ef76a`) in progress** — this is the head-bound merge run the prior packet's §1(2) requires.
- **G3** — worker worktree `.claude/worktrees/agent-aa27445e25371ac7c` (locked = agent active), branch `claude/s2-g3-resume-before-exec` @ `0ba2775` = one code commit over `4281d2e`: `campaign_supervisor.py` (+158/−), `test_campaign_supervision.py` (+290), `test_campaign_supervision_linux.py` (+67); message "never resume a payload before its entrypoint has exec'd; retain the image and the payload exit; A2/A4 review folds". Worktree clean. **Not on `origin`; no S2 run on that branch; packet §7 still `_Pending._`.** Its packet: [`2026-09-20-full-e1-s2-g3-resume-before-exec.md`](https://github.com/Joshua-Asante/first-passage/blob/03ef76a/docs/briefs/handoffs/2026-09-20-full-e1-s2-g3-resume-before-exec.md) (frozen repair §2, forbidden §3).
- **G4** — worker worktree `.claude/worktrees/agent-a9467fccf90ea1b7b` (locked), branch `claude/s2-g4-store-invariants` @ `03ef76a` with **uncommitted** edits to `campaign_store.py`, `campaign_funding.py`, `campaign_budget.py`. Not on `origin`; §7 pending.
- Known residuals on the candidate: **R-G3** (resume signal reaches `runc:[2:INIT]` after setuid but before `execve`; Go dies on SIGUSR1; container exits non-zero < 0.5 s; fail-closed, no credit; ~1 in 12 payload starts on a fast host; would kill real S3 workers) — repair in flight (G3); **A3 / A9-3** work-id prefixes `control_`/`event_` collide with the supervision-object GLOBs — validator in flight (G4); **A4** README ~:156-170 still says "polkit rule for the qexec `manage-units` scope" and omits that qexec can start arbitrary transient units incl. `User=root` — unowned until this packet; **OOM** terminal duality (settle vs stop-on-OOM → IN_DOUBT; test-only, disclosed); **bridge** — the Windows isolation bridge exceeds 1800 s on the coordinator's box and was never claimed. Review advisories A1/A2/A5/A7 are S3 preconditions unless G4 lands them; A6 (OOM stop Windows-proven), A8 (no guardian-exit watcher) stay disclosed.

## 1. What success looks like

**T01 is successful when all of the following hold, in this order:**

1. **Boundary evidence complete on the merge head.** Run 35518729717 (or its single permitted rerun, §2 rule) is 15 passed / 0 skipped, `source_stable`, `capture_complete`, invariants 15/15, both owned-cleanup receipts ok, and its checked-out SHA is `03ef76a` (or the rerun's head).
2. **G3 accepted.** G3's §7 is `RESOLVED` or `DONE_WITH_CONCERNS` with nothing outside its frozen repair: resume sent only when the observed init pid's `/proc/<pid>/exe` basename is the installed interpreter and `comm` is not `runc:[…]`; `comm`/`exe` retained on every PROCESS and RESUMED event (bounded strings; `parse_supervision_event` whitelist updated); a `PAYLOAD_EXIT` event `{container_id, exit_code, oom_killed, finished_at}` retained on **both** the credited and non-credited settlement paths; Windows regressions (a) runc-init-first → no send, python-next → exactly one RESUMED, (b) non-zero exit with identity → `PAYLOAD_EXIT` + measured settle + no completion, (c) parser rejects unbounded/unknown fields; Linux assertions only inside existing registered nodes (no new node; `charge >= limits.cpu_ns` not relaxed); `campaign_probe.py`, profiles, ceilings, release literals byte-identical to `4281d2e`. G3's branch is on `origin` with **one fifteen-green S2 run on its own head** (run ID, record ID, cleanup). If G3's commit message's "A2/A4 review folds" touched anything G4 owns (`campaign_store.py`, `campaign_funding.py`, `campaign_budget.py`, `service.py`) that is a cross-ownership `CHECKPOINT`, not a merge.
3. **G4 dispositioned item by item.** For each of A1, A2-store, A5, A7, A9-3: **landed** (named Windows test + record ID; no schema/profile/release/snapshot version bump without a `CHECKPOINT`; no refund or skipped charge on a BOUND campaign) or **deferred** (reason; named in the acceptance entry as a T02 precondition). A9-3 landed closes A3 — reserved prefixes derived from the same constants the GLOBs use, not copied strings; deferred ⇒ the S3 naming rule in T02's packet is the interim guard, stated in the entry. G4's branch on `origin` with one S2 run; a `descendants` red on G4's pre-G3 bytes with the R-G3 signature is disclosed, not re-rolled.
4. **Integrated, byte-checked, Windows-green.** G3 then G4 merged into the integration branch by merge commits; each worker's owned files byte-identical to its head after the merge, except at the two expected touch points (`record_work_transition` CAPTURED-after-settlement refusal from G4 must coexist with G3's guardian-side IN_DOUBT-before-settle; G4 must not enumerate `parse_supervision_event` data keys that G3 extended). A4 README wording landed (see (6)). Windows §2.6 lines 1–3 and `./fp.ps1 check` green on the integrated head with record IDs.
5. **Post-G3 head-bound fifteen-green.** The PR-path S2 run on the integrated head is 15/15 with **no rerun allowance** — after G3 a `descendants` failure is a G3 defect. This run, not the pre-G3 runs, is the load-bearing evidence that real worker launch is reliable.
6. **Acceptance entry recorded, evidence-bound.** One append-only ledger entry `### Coordinator acceptance — S2 enforcement gaps (G1/G2) with G3 launch repair, 2026-09-20` naming: runs 35489657203 (`5ac3ad0`), 35493582848 (`bbafe68`), the merge-head run from (1), G3's own run, G4's own run, the integrated run from (5) — each with record ID, counts, `source_stable`/`capture_complete`, invariants, cleanup; steering findings 1–2 resolved at `9d7a731`; G3 accepted with the observed `comm`/`exe` values if captured; the G4 dispositions; the review dispositions from (7); the **S3 handshake contract sentence**: "a real worker's entrypoint must call the probe-side block-then-await resume helper before any work; the guardian resumes only after the observed init pid has exec'd the installed interpreter"; the **A4 sentence** verbatim: "under the accepted polkit rule qexec can start arbitrary transient units including `User=root`; polkit contributes no containment for unit starts; hierarchy membership rests on the guardian's code checks and `bootstrap.py`'s `campaign_control` fixed-prefix check"; and the **disclosed-limitations list**: any deferred G4 item (as T02 precondition), A6 OOM stop Windows-proven, A8 no guardian-exit watcher, OOM terminal duality, untested isolation bridge, and "accepted work boundary only — no statistical execution, no N1/N2/Part A dispatch, no activation". The entry says "S2 ACCEPTED as the enforced work boundary; G3 launch repair landed" and nothing wider; the ledger's stopping lines stay. The same A4 sentence replaces "manage-units scope" in README §S2 (grep `polkit|manage-units` across README, plans and `campaign_host.py` comments first).
7. **Review clear.** One Codex review round requested on the diff since `03ef76a` only (G3 + G4 + README + ledger). Every Codex/CodeRabbit thread dispositioned: confirmed-and-repaired (re-enters (5)), refuted-with-evidence, or deferred-to-named-owner; listed in the entry.
8. **Merged.** #436 merged with a merge commit after: `skills (3.12)` green on the merge head; run (5) green; review clear; `mergeStateStatus` not `BEHIND` (merge `origin/main` in again if it moved; then (5) repeats on the new head). Branch kept. PR body rewritten: "ACCEPTED as the enforced work boundary; G3 launch repair landed" + the limitations list. Merge SHA in §7.

**Not in scope:** the S3 packet, any S3 code, N1/N2/Part A dispatch, G5 verdict, result, seal, activation. If the coordinator wants the S3 packet authored in the same session, that is a separate dispatch under T02.

## 2. Steps

1. **State read (no writes).** `git fetch origin`; confirm §0 or record deltas in §7; read run 35518729717's result and artifact; `gh api graphql … reviewThreads` on #436 (expect 0); `git -C <G3 worktree> log --oneline 4281d2e..HEAD && git status --short` and the same for G4 — read-only; check both packets' §7.
2. **Boundary run.** Apply the rerun rule: 14/15 whose only failure carries the R-G3 signature (`descendants` work; payload exit non-zero < 0.5 s after the first RESUMED; no burner children; no OOM) → record as R-G3, dispatch **one** `gh workflow run qualification-s2-supervision.yml --ref claude/s2-enforcement-gaps-fab5e6`; a second failure or any non-R-G3 failure → §7 return `BLOCKED` with the artifact facts, no re-roll.
3. **Draft the acceptance entry** locally (do not commit yet) with everything from §1(6) that is known at this point. ⏸ **Wait for G3's §7.** Judge it against §1(2) — read the diff `git diff 4281d2e...<G3 head> -- ops tests`, run the three Windows regressions by name, confirm `git diff 4281d2e <G3 head> -- ops/c1_rail/qualification/execution/campaign_probe.py` is empty, confirm the S2 run on its head. **Checkpoint (T01): report two separate verdicts — boundary evidence (steps 1–3) and G3 readiness — even if both are ready.** Proceed unless told to stop.
4. ⏸ **Wait for G4's §7.** Disposition per §1(3). If G4 returned `CHECKPOINT` on a version bump, report it and hold G4 out of the merge; the acceptance entry then names every G4 item as deferred.
5. **Integrate.** In the #436 worktree: `git merge --no-ff claude/s2-g3-resume-before-exec`, then `git merge --no-ff claude/s2-g4-store-invariants`; resolve only at the two expected touch points; `git diff <G3 head> HEAD -- <G3-owned files>` and the G4 equivalent must be empty outside those points. Commit A4 README wording as its own docs commit. Windows: `./fp.ps1 doctor`; §2.6 line 1 (ten execution files, `--workers 2`, zero skips), line 2, line 3 (`tests/ops/qualification` + phase3); `./fp.ps1 check`; `git diff --check`. `git diff --stat` before every commit. Push.
6. ⏸ **Post-G3 run.** Wait for the PR-path S2 run on the pushed head. Green → finalise the acceptance entry, commit, push. Red → diagnose from the artifact; §7 return (`DONE_WITH_CONCERNS`, boundary acceptance held back) — no rerun.
7. **Review.** Request one Codex round on the diff since `03ef76a`; disposition threads; repairs re-enter step 6; update the entry.
8. **Merge #436** per §1(8). Update the PR body first. Record the merge SHA.
9. **Return** (§7).

## 3. Forbidden

Editing the G3/G4 worktrees or branches; rebase/squash/force-push of the integration branch; relaxing any Linux assertion; re-rolling a failed S2 run outside the single R-G3 rerun on a pre-G3 head; claiming Windows results as Linux evidence; touching `campaign_probe.py`, the probe roles, profiles, ceilings, release literals, `role_policy.TREE_BINDINGS`, `host.json`, gates or required checks; accepting a version bump that arrived without a `CHECKPOINT`; authoring S3 code or the S3 packet; any activation, N1/N2/Part A dispatch, G5 verdict, result or seal; closing or re-opening issues; `git stash`; commits without `git diff --stat`.

## 4. Verification you must cite

Linux (run ID, record ID, 15/15, 0 skips, `source_stable`, `capture_complete`, invariants passed, both owned-cleanup receipts ok, checked-out SHA): the merge-head run (step 2), G3's own run, G4's own run, the integrated post-G3 run (step 6), plus the two accepted runs for `5ac3ad0`/`bbafe68`. Windows (PowerShell, `./fp.ps1`, doctor first; record IDs): §2.6 lines 1–3 and `check` on the integrated head; the named G3 regressions (a)(b)(c) and each landed G4 test; `git diff --check` clean. Byte checks: `campaign_probe.py` unchanged since `4281d2e`; worker-owned files equal to their heads after the merge.

## 5. Checkpoints (report, do not wait unless stated)

After step 3 (the two-verdict checkpoint — **wait** for the coordinator only if either verdict is not ready); before step 5 if either worker returned `BLOCKED`/`CHECKPOINT`, if G3's diff crosses into G4-owned files, or if the merge is not clean at the expected touch points; before step 8 if the step-6 run is not fifteen-green; immediately on any version bump, profile/ceiling/release-literal or probe-side change found in a return.

## 7. Executor return

_Pending._ Return `RESOLVED` / `DONE_WITH_CONCERNS` / `BLOCKED` with: branch + final head; merge SHA of #436; ledger entry heading; run IDs / record IDs / counts / checked-out SHAs for every Linux run cited; Windows record IDs; G3 verdict with the observed `comm`/`exe` values if captured; the G4 disposition table (five rows); the A4 wording diff; review thread dispositions; the limitations you disclosed; anything you could not verify.
