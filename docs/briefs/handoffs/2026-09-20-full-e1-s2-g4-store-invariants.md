# CC handoff — Protected Full E1 / S2-G4: store-side invariants from the PR #436 acceptance review (A1, A2-store, A5, A7, A9-3)

**Type:** cc_handoff (bounded repair set; coordinator rulings on independent-review advisories)
**Date:** 2026-09-20
**Status:** dispatch now. Branch `claude/s2-g4-store-invariants` off `claude/s2-enforcement-gaps-fab5e6` @ `915035c` (or its current head — `git fetch` first). Push, no PR. A sibling worker (G3, branch `claude/s2-g3-resume-before-exec`) is concurrently editing `campaign_supervisor.py`, `campaign_probe.py` and `test_campaign_supervision.py` / `test_campaign_supervision_linux.py` — do not touch those.
**Authority:** `ops/c1_rail/qualification/execution/{campaign_store.py, campaign_funding.py, campaign_budget.py, service.py}`, `tests/ops/qualification/execution/{test_campaign_cancellation.py, test_campaign_funding.py, test_campaign_recovery.py, test_campaign_supervision_persistence.py (new, if you need a store-only home), test_service.py, test_campaign_budget.py}`, `tests/integration/qualification_boundary/test_campaign_service_linux.py` (assertions in existing cases only), this file §7. No schema/profile/release/snapshot version change (report if indispensable → `CHECKPOINT`), no allowance change, no G3-owned file. Source of the findings: the review note is reproduced in §1; the reviewer's file:line anchors are at `915035c`.

## 1. Rulings and repairs (frozen)

**A1 — VOID must remain recordable on a terminal-but-VALID campaign.** Today `claim_void_authentication` refuses unless the funding doc is `BOUND` (etc.), so a legitimate VOID that lands in the ms dispatch-pending window is refused, the pending-cancellation barrier then fails the running guardian's next positive transition, the campaign goes IN_DOUBT/terminal, and every later claim is refused on `state != BOUND` → `void_pending=true`, `validity=VALID` forever; `void_retry` never answers. Spec §2.8 (`VALID → VOID` irreversible, VOID as a negative fact) and the N1_ONLY VOID (verifies regardless of budget state) both require recordability. **Ruling:** keep the funded charge exactly as is whenever the campaign budget is `BOUND` and fundable. When the campaign is `VALID` but its budget is terminal / not BOUND (IN_DOUBT, BUDGET_EXHAUSTED, BUDGET_UNCERTAIN, or any non-BOUND funding state with no pending bootstrap/recovery/dispatch), the queued body is authenticated **once** under `controller_cpu_guard()` with **no charge object** (there is no allowance to charge and no positive authority to protect), producing either the VOID receipt or a retained refusal (reuse `void_refusal_*` with a distinct schema/prefix if the pairing rule in `integrity()` needs it — report), and clearing the body either way; a second distinct body gets its own single attempt. The dispatch-pending / recovery-pending / funding-pending refusals stay (those windows close), so the sequence VOID-during-window → refused → barrier → IN_DOUBT → **retry authenticates** → VOID recorded now works. Tests: the scenario above end-to-end on Windows (claim refused on dispatch pending; barrier terminalises; exact retry → VOID receipt; `void_pending=false`); a forged body on a terminal campaign → refusal retained, cleared, still VALID, no charge object; Linux: extend `test_campaign_service_linux.py` case 1 or 2 with the terminal-campaign VOID if it fits the existing flow (no new registered node).

**A2 (store half) — `record_work_transition` must refuse `CAPTURED` (and `SIGNING_INTENT`/`COMPLETED`) on a work that already carries a settled observation.** Today it refuses only `START_INTENT|RUNNING` after settlement (~`:1157-1158`). G3 is making the guardian transition IN_DOUBT before settling the non-credited path; the store must independently refuse credit after settlement. Windows test.

**A5 — completion requires a retained alive-verified payload identity, store-side.** Add a precondition in `record_work_transition` for target `CAPTURED`/`COMPLETED`/`SIGNING_INTENT`: at least one retained `PROCESS` supervision event for that work whose retained `cgroup` lies under the work's enrolled payload slice (G2-visible via `full_campaign_objects` roles `supervision_event_*` and `supervision_<work>` enrollment; the guardian's own PROCESS event is in the guardian unit's cgroup, so it must not count). Add the same rule to `integrity()` ("COMPLETED/CAPTURED ⇒ ≥1 payload-scope PROCESS"). Windows tests: transition refused with no payload identity; accepted with one; integrity refuses a tampered journal. Coordinate the event shape with the existing `parse_supervision_event` (read-only for you; G3 is adding `comm`/`exe` fields — tolerate their presence by not enumerating data keys beyond what you need).

**A7 — one allowance view.** Snapshot totals exclude the VOID authentication charges; the funding projection includes them. `reserve_work` (and anything reading `remaining_cpu_ns` from the snapshot) can spend charged allowance twice. **Ruling:** without a snapshot schema change, make `reserve_work` (and `bind_budget`'s feasibility check if it reads remaining) consult `void_authentication_charge` and refuse when `remaining_after_charges < requested`; document in `budget_snapshot`'s docstring that snapshot totals are pre-charge and `diagnostic_status`/the projection are authoritative. Windows test: after N charged attempts, a `reserve_work` that fits the snapshot's remaining but not the projection's is refused.

**A9-3 — work-id prefix validator.** Work identifiers beginning with `control_` or `event_` collide with the `supervision_control_*` / `supervision_event_*` object roles used by GLOB queries (`_recovery_pending`, integrity walk, `campaign_host.cleanup`). Refuse such ids at every producer entry (`reserve_work`, `claim_scheduler_bootstrap`/`parse_request` in `campaign_funding.py`, `prepare_campaign_work` is G3's — so refuse at the store/funding layer they call) with a clear error; Windows tests. Also refuse `admission` as a scheduler work id if not already refused.

## 2. Verification
PowerShell via `./fp.ps1`: the G2 packet's §2.6 line 1 selection (ten execution files, `--workers 2`, zero skips) and line 2; `git diff --check`. Then ONE S2 workflow run on your branch (`gh workflow run qualification-s2-supervision.yml --ref claude/s2-g4-store-invariants`): fifteen green required; if a G1/G3-owned case fails on your branch, diagnose from the artifact and report — do not re-roll. Note that your branch does not include G3's guardian repair, so the pre-exec resume race (packet `2026-09-20-full-e1-s2-g3-resume-before-exec.md` §1) can still hit at low rate; if it does, say so with the artifact evidence rather than re-running.

## 3. Forbidden
Version bumps without `CHECKPOINT`; refunding or skipping the charge on a BOUND campaign; letting any peer other than the operator role queue; touching G3's files; relaxing existing tests; `git stash`; commits without `git diff --stat`.

## 4. Return
`RESOLVED` / `DONE_WITH_CONCERNS` / `CHECKPOINT` / `BLOCKED` with: branch + head; `git diff --stat <base>...HEAD`; Windows record IDs and counts; Linux run ID, record ID, counts, cleanup; changed interfaces; limitations.

## 7. Executor return

**Status: RESOLVED** (GLM continuation 2). The stopped worker's WIP (`8c764d8`) carried A9-3, the A2/A5/A7 store edits and the A1 constants; the successor completed the A1 three-way claim rewrite, wired the new refusal role into `integrity()`, rescoped A5, wrote every Windows regression and the Linux extension, and verified. History note: the first-round completion was committed as `9727d95` (operator-committed mid-session); the second-round repairs are `d165f1a`, the branch head.

Branch `claude/s2-g4-store-invariants`, head `d165f1a` (plus this §7 docs commit). `git diff --stat 03ef76a...d165f1a`:

```
 ops/c1_rail/qualification/execution/campaign_budget.py  |  23 +
 ops/c1_rail/qualification/execution/campaign_funding.py  |   8 +-
 ops/c1_rail/qualification/execution/campaign_store.py    | 270 ++++++++++++++++++--
 tests/integration/qualification_boundary/test_campaign_service_linux.py |  38 ++-
 tests/ops/qualification/execution/test_campaign_budget.py |  57 +++++
 tests/ops/qualification/execution/test_campaign_cancellation.py | 279 ++++++++++++++++++++++-
 tests/ops/qualification/execution/test_campaign_funding.py |  14 +
 7 files changed, 654 insertions(+), 35 deletions(-)
```

**Ruling dispositions.**

- **A1 — terminal-but-VALID VOID recordable.** `claim_void_authentication` is now three-way: the funded charge exactly as before whenever the funding doc is `BOUND` with no overlay and the bound fits; **(None, status)** while a bootstrap/recovery/dispatch window is open (those windows close), on a non-VALID campaign, or with no funding projection; and **sequence 0** — one uncharged one-use attempt — when the campaign is VALID but its budget is terminal or not BOUND with no open window. Sequence 0 routes `refuse_void_authentication`/`complete_void_authentication` through the pending body itself as the one-use token: the refusal is retained as `void_terminal_refusal_<sha256(body)>` (`qualification_campaign_void_terminal_refusal/v1`), the VOID receipt or refusal clears the body either way, `queue_diagnostic_void` refuses a re-queue of an already-refused digest (`TERMINAL_BODY_REFUSED`), a distinct body gets its own single attempt, and `integrity()` validates role/digest binding and sequence contiguity. No version bump anywhere (the ruling's CHECKPOINT condition never arose).
- **A2 (store half).** `record_work_transition` refuses CAPTURED and SIGNING_INTENT on a work that already carries a settled observation ('settled work cannot acquire credit') and COMPLETED unless credit was established before it (state CAPTURED/SIGNED) or the work is one of the two capture-less productions (the admission; a linked signing retry — 'completion requires credit'). The worker's WIP additionally refuses COMPLETED-from-RUNNING *without* settlement; kept as a strengthening consistent with A5 (no production path completes from RUNNING), disclosed here because the ruling's letter mentions only settled works.
- **A5 — store-side identity gate.** For target CAPTURED/SIGNING_INTENT/COMPLETED, a work other than the admission requires an enrollment and a retained PROCESS event whose cgroup lies under the work's enrolled payload slice (component-wise; the guardian's own unit cgroup never counts) — **a linked signing retry included**: on the host the retry runs a container and retains a payload identity like any other work, so the correct exemption is "no capture required" (A2), never "no identity required" (operator correction on the interim retry exemption, which is dropped). **Scoping, disclosed:** the packet's unqualified precondition refuses the compact funding model and the store-only budget/recovery journals, which legitimately enroll (materialize writes the enrollment) and credit without any OS layer. The rule as landed binds only when the attempt carries a **PROCESS-kind** supervision event. This relies on a production invariant: the admission guardian's first durable act is retaining its own PROCESS identity, so the observation layer is engaged on every really supervised attempt and dormant only in store-only tests (recovery-only CLEANUP shapes included). The Windows linked-retry scene now retains enrollment plus a payload-scope PROCESS identity for both the parent and the retry, so the retry's COMPLETED is proven under the engaged rule rather than through a waiver. `integrity()` walks the same scope: a credited work on a PROCESS-carrying attempt without a payload-scope identity, or unenrolled, is refused on reopen. **[S2-G5 R3 correction, 2026-09-20:** the A5 scoping above (presence-based engagement) was reopened by review P2-1 and is closed by ruling R1 (packet S2-G5): the identity rule now binds by enrollment — an enrolled non-admission work needs ≥1 payload-scope PROCESS event (either schema version) for credit, enforced on transition and on reopen; disclosed limitation (a) is superseded.**]**
- **A7 — one allowance view.** `reserve_work` refuses when the request fits the snapshot's pre-charge remaining but not the projection's (`_remaining_after_charges`), terminalising exactly as an over-cap request does; `bind_budget`'s cap check adds the charged term (zero at bind time, kept for the one-view rule); `budget_snapshot`'s docstring states that snapshot totals are pre-charge and the projection/diagnostic_status are authoritative. `claim_scheduler_bootstrap` already read the projection. No snapshot schema change.
- **A9-3 — work-id prefix validator.** `campaign_budget.validate_work_id` refuses `control_`/`event_` prefixes and the fixed `admission` identity (except for its one legitimate producer, `begin_admission`, which mints it inline); wired at `reserve_work` and `campaign_funding.parse_request` (work_id and signing_retry_of). No production caller regresses (the service's admission manifest is a work manifest, not a scheduler request).

**Windows verification** (ops-env Python 3.13.2 through this checkout's `fp.ps1`, doctor 62 packages): §2.6 line 1 (ten execution files, `--workers 2`) **434 passed / 0 skipped**, exit 0, record `20260920T164945Z-39ed1e178dfe`; line 2 **79 passed + the known Windows symlink skip**, exit 0, record `20260920T170309Z-b00d4c8f8f82`; `git diff --check` clean. Two intermediate line-1 runs (7 and 2 failures) diagnosed and repaired the shapes above; the final run is on the committed bytes. One **existing test was updated to the ruling's semantics** (not relaxed): `test_negative_transition_proceeds_under_the_barrier_and_leaves_the_body_pending_on_a_terminal_campaign` pinned the pre-A1 behavior (a queued body on a terminal campaign never authenticates); it now proves the exact retry records the VOID uncharged with `void_authentication_attempts == 0` and no charge object.

**Linux run:** run `35524805026` on `d165f1a`, record `054a307723654a28bf1055990800c3b2`, exit 1/1, `source_stable=true`, `capture_complete=true` — **14 passed / 1 failed / 0 skipped**. The one failure is the successor's own case-2(c) extension leg, a test-only defect, root-caused from the artifact: the restart's recovery settled the interrupted wall work with the counters lost alongside its retired slice, so the campaign terminalised **BUDGET_UNCERTAIN** (attempt `linux-3431fdfbe…`, journal shows RECOVERY_CLAIM → SETTLE_WORK → RECOVER_WORK ending BUDGET_UNCERTAIN with the work IN_DOUBT) while the leg's wait predicate demanded exactly IN_DOUBT and expired. Repaired test-only (the predicate now accepts the full terminal set, like the two-descendants case's duality); no production change. Per operator steering, the integrated PR run proves the repaired leg (no second G4-branch run); the other fourteen nodes — including every G1/G2/G3-owned node and the charged-VOID metering case — are green on this run's artifact.
**Linux extension:** the packet's conditional terminal-campaign VOID "if it fits the existing flow" fits case 2 as a new leg (c): a fresh attempt schedules a `wall` probe, waits RUNNING, restarts (recovery claims the work → IN_DOUBT → terminal-but-VALID campaign), and the operator's valid body then authenticates once **uncharged** under the real controller guard — no `void_authentication_*` object, `void_authentication_attempts == 0`, `pending_void` cleared, and the remaining allowance unchanged across the authentication (restart recovery legitimately moves reserved→settled). No new registered node.

Limitations: (a) the A5 scoping (PROCESS-kind engagement + the admission exemption alone) means an event-less journal — no guardian ever retained a PROCESS identity — never binds; the store cannot distinguish a store-only model journal from an illegitimate event-less producer, and the binding relies on the production admission guardian always retaining its own PROCESS event first (§ above); (b) the uncharged terminal attempt has no durable claim-time marker (like the charged path's D3 interrupted-attempt semantics): a crash between claim and outcome leaves the body queued and the retry attempts again — one-use is enforced per completed outcome via the digest-keyed refusal; (c) `TERMINAL_BODY_REFUSED` bites only the terminal path — a body refused pre-admission or charged-refused on a BOUND campaign can still be re-queued after its outcome clears it (each such re-queue is a fresh charged/pre-admission attempt, as before); (d) the retry-scene identity addition touches `test_campaign_supervision.py` (G3's file, operator-directed): the integrated branch's copy is G3's head plus exactly that scene edit.
