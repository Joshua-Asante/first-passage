# CC handoff — Protected Full E1 / S2-G4: store-side invariants from the PR #436 acceptance review (A1, A2-store, A5, A7, A9-3)

**Type:** cc_handoff (bounded repair set; coordinator rulings on independent-review advisories)
**Date:** 2026-09-20
**Status:** dispatch now. Branch `claude/s2-g4-store-invariants` off `claude/s2-enforcement-gaps-fab5e6` @ `915035c` (or its current head — `git fetch` first). Push, no PR. A sibling worker (G3, branch `claude/s2-g3-resume-before-exec`) is concurrently editing `campaign_supervisor.py`, `campaign_probe.py` and `test_campaign_supervision.py` / `test_campaign_supervision_linux.py` — do not touch those.
**Authority:** `ops/c1_rail/qualification/execution/{campaign_store.py, campaign_funding.py, campaign_budget.py, service.py}`, `tests/ops/qualification/execution/{test_campaign_cancellation.py, test_campaign_funding.py, test_campaign_recovery.py, test_campaign_supervision_persistence.py (new, if you need a store-only home), test_service.py, test_campaign_budget.py}`, `tests/integration/qualification_boundary/test_campaign_service_linux.py` (assertions in existing cases only), this file §7. No schema/profile/release/snapshot version change (report if indispensable → `CHECKPOINT`), no allowance change, no G3-owned file. Source of the findings: the review note is reproduced in §1; the reviewer's file:line anchors are at `915035c`.

## 1. Rulings and repairs (frozen)

**A1 — VOID must remain recordable on a terminal-but-VALID campaign.** Today `claim_void_authentication` refuses unless the funding doc is `BOUND` (etc.), so a legitimate VOID that lands in the ms dispatch-pending window is refused, the pending-cancellation barrier then fails the running guardian's next positive transition, the campaign goes IN_DOUBT/terminal, and every later claim is refused on `state != BOUND` → `void_pending=true`, `validity=VALID` forever; `void_retry` never answers. Spec §2.8 (`VALID → VOID` irreversible, VOID as a negative fact) and the N1_ONLY VOID (verifies regardless of budget state) both require recordability. **Ruling:** keep the funded charge exactly as is whenever the campaign budget is `BOUND` and fundable. When the campaign is `VALID` but its budget is terminal / not BOUND (IN_DOUBT, BUDGET_EXHAUSTED, BUDGET_UNCERTAIN, or any non-BOUND funding state with no pending bootstrap/recovery/dispatch), the queued body is authenticated **once** under `controller_cpu_guard()` with **no charge object** (there is no allowance to charge and no positive authority to protect), producing either the VOID receipt or a retained refusal (reuse `void_refusal_*` with a distinct schema/prefix if the pairing rule in `integrity()` needs it — report), and clearing the body either way; a second distinct body gets its own single attempt. The dispatch-pending / recovery-pending / funding-pending refusals stay (those windows close), so the sequence VOID-during-window → refused → barrier → IN_DOUBT → **retry authenticates** → VOID recorded now works. Tests: the scenario above end-to-end on Windows (claim refused on dispatch pending; barrier terminalises; exact retry → VOID receipt; `void_pending=false`); a forged body on a terminal campaign → refusal retained, cleared, still VALID, no charge object; Linux: extend `test_campaign_service_linux.py` case 1 or 2 with the terminal-campaign VOID if it fits the existing flow (no new registered node).

**A2 (store half) — `record_work_transition` must refuse `CAPTURED` (and `SIGNING_INTENT`/`COMPLETED`) on a work that already carries a settled observation.** Today it refuses only `START_INTENT|RUNNING` after settlement (~`:1157-1158`). G3 is making the guardian transition IN_DOUBT before settling the non-credited path; the store must independently refuse credit after settlement. Windows test.

**A5 — completion requires a retained alive-verified payload identity, store-side.** Add a precondition in `record_work_transition` for target `CAPTURED`/`COMPLETED`/`SIGNING_INTENT`: at least one retained `PROCESS` supervision event for that work whose retained `cgroup` lies under the work's enrolled payload slice (G2-visible via `full_campaign_objects` roles `supervision_event_*` and `supervision_<work>` enrollment; the guardian's own PROCESS event is in the guardian unit's cgroup, so it must not count). Add the same rule to `integrity()` ("COMPLETED/CAPTURED ⇒ ≥1 payload-scope PROCESS"). Windows tests: transition refused with no payload identity; accepted with one; integrity refuses a tampered journal. Coordinate the event shape with the existing `parse_supervision_event` (read-only for you; G3 is adding `comm`/`exe` fields — tolerate their presence by not enumerating data keys beyond what you need).

**A7 — one allowance view.** Snapshot totals exclude the VOID authentication charges; the funding projection includes them. `reserve_work` (and anything reading `remaining_cpu_ns` from the snapshot) can spend charged allowance twice. **Ruling:** without a snapshot schema change, make `reserve_work` (and `bind_budget`'s feasibility check if it reads remaining) consult `void_authentication_charge` and refuse when `remaining_after_charges < requested`; document in `budget_snapshot`'s docstring that snapshot totals are pre-charge and `diagnostic_status`/the projection are authoritative. Windows test: after N charged attempts, a `reserve_work` that fits the snapshot's remaining but not the projection's is refused.

**A9-3 — work-id prefix validator.** Work identifiers beginning with `control_` or `event_` collide with the `supervision_control_*` / `supervision_event_*` object roles used by GLOB queries (`_recovery_pending`, integrity walk, `campaign_host.cleanup`). Refuse such ids at every producer entry (`reserve_work`, `claim_scheduler_bootstrap`/`parse_request` in `campaign_funding.py`, `prepare_campaign_work` is G3's — so refuse at the store/funding layer they call) with a clear error; Windows tests. Also refuse `admission` as a scheduler work id if not already refused.

## 2. Verification
PowerShell via `./fp.ps1`: the G2 packet's §2.6 line 1 selection (ten execution files, `--workers 2`, zero skips) and line 2; `git diff --check`. Then ONE S2 workflow run on your branch (`gh workflow run qualification-s2-supervision.yml --ref claude/s2-g4-store-invariants`): fifteen green required; if a G1/G3-owned case fails on your branch, diagnose from the artifact and report — do not re-roll. Note that your branch does not include G3's guardian repair, so the pre-exec resume race (packet `2026-09-20-full-e1-s2-g3-resume-before-exec.md` §1) can still hit at low rate; if it does, say so with the artifact evidence rather than re-running.

## 3. Forbidden
Version bumps without `CHECKPOINT`; refunding or skipping the charge on a BOUND campaign; letting any peer other than the operator role queue; touching G3's files; relaxing existing tests; `git stash`; commits without `git diff --stat`.

## 4. Return
`RESOLVED` / `DONE_WITH_CONCERNS` / `CHECKPOINT` / `BLOCKED` with: branch + head; `git diff --stat <base>...HEAD`; Windows record IDs and counts; Linux run ID, record ID, counts, cleanup; changed interfaces; limitations.

## 7. Executor return
_Pending._
