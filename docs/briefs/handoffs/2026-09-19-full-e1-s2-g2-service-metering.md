# CC handoff — Protected Full E1 / S2-G2: service-side metering (funded post-admission VOID authentication, safe admission retry)

**Type:** cc_handoff (frozen-spec implementation with coordinator defaults)
**Date:** 2026-09-19
**Status:** dispatch now — coordinator assignment after PR #433 (R2b) and PR #434 (six S2 host fixes + S2 CI job) merged to `main` at `f2606b0`. Closes two of the five recorded S2 enforcement gaps (R3 and the Codex "resume an existing unstarted admission" finding); the sibling packet [S2-G1](2026-09-19-full-e1-s2-g1-supervisor-enforcement.md) closes the other three in parallel on disjoint files.
**Spawn target:** Claude Code subagent in its own git worktree off `claude/s2-enforcement-gaps-fab5e6` (which is `main@f2606b0` plus these two handoffs). Branch `claude/s2-g2-service-metering`. Push the branch; no PR (the coordinator integrates both packets into one PR).
**Parent:** [coordinator handoff §S2-R3, §Shared decisions](2026-09-19-full-e1-coordinator-handoff.md) · [R2 contract and R2a acceptance](2026-09-19-full-e1-s2-r2-funded-scheduler.md) (the funded-intent pattern) · [execution-slices plan, S2 + ledger](../../superpowers/plans/2026-09-18-full-e1-execution-slices.md) (every 2026-09-19 acceptance entry, incl. the R2b restart-ownership ruling) · [governing spec §2.5–2.6, §2.8 (VOID ordering), resource scope](../../superpowers/specs/2026-09-17-protected-full-e1-campaign.md) · Codex finding on PR #429 `service.py:292` (quoted in §1).
**Authority:** code, tests, harness cases, `S2_CASES` registration, invariant-manifest node registration, and this file's §7 return section only. No new allowance (phase ceilings, the 20 s orchestration charge and its guardian 13+1 / START_OWNER 2 / START_CLIENT 2 / RECOVERY_OWNER 2 decomposition are fixed); no edits to `campaign_supervisor.py`, `worker.py`, `deploy/qualification/bootstrap.py`, `tools/qualification_verification/campaign_host.py`, `container_ownership.py`, `tests/ops/qualification/execution/test_campaign_supervision.py`, `tests/integration/qualification_boundary/test_campaign_supervision_linux.py` (G1 owns them); no statistical dispatch, no S2 acceptance claim, no merge. A versioned profile/release/snapshot/DB change is a `CHECKPOINT`, not a default. A `DONE` status supplies no permission.

## 0. Rule 0 reads (Phase 0 — post the read-report in §7 before writing code)

Currency: `git fetch origin main claude/s2-enforcement-gaps-fab5e6`; record both SHAs. At authoring `main` was `f2606b043fa5b663d5edb9505fb14b2d6885d83a`. Hard checks: `git merge-base --is-ancestor f2606b0 HEAD` exits 0; `test -f docs/briefs/handoffs/2026-09-19-full-e1-s2-g1-supervisor-enforcement.md`; `grep -n "queue_diagnostic_void" ops/c1_rail/qualification/execution/service.py` prints one line. All anchors below are at `f2606b0`.

- `ops/c1_rail/qualification/execution/service.py` (608 lines) — `permitted`/`uid_roles`/`schedule_eligibility` :64-82; `ExecutionService.__init__` :95-127 (startup preload, `schedule_eligible`); `handle_request` :138-227 (schedule route :140-141, campaign route :142-143, N1 `VOID` :209-226 for comparison); `_campaign_request` :229-290 (non-diagnostic v2 VOID :265-282); **`_diagnostic_campaign_request` :292-336** — `SUBMIT_E1` :294-310 (`dispatch_lock` + `controller_cpu_guard` :295; existing-row exact-retry branch :296-303 returns status without ever calling `run_campaign_work` — the gap-4 site; `begin_admission` :304-305; manifest :306-308; `run_campaign_work` :309), `STATUS` :311-312, `FETCH_PLAN_CHUNK` :313-316, **`VOID` :316-335** (`void_retry` :318-320; queue when no trust domain :321-323; unmetered `verify_detached_approval` :326-333 — the gap-3 site; `void` :335); `_schedule_request` :338-360 (the funded-claim → guard → materialize chain to mirror); **`recover_service` :482-537** (per-attempt loop :489-518; funded attempts :492-506; every work goes to `recover_campaign_work` :513-517 — including a `RESERVED` admission work).
- `ops/c1_rail/qualification/execution/campaign_store.py` (1151 lines) — `retry` :43-52; `claim_supervision_control` :110-162 (one-use slots; "slot already spent"); `_recovery_pending` :257-273; `complete_recovery` :275-303; **`queue_diagnostic_void` :336-352** (`record_byte_limit` bound, single pending body, 'pending cancellation differs'); `diagnostic_status` :354-381 (`pending` reporting :368/:381); **`finish_diagnostic_admission` :383-468** (pending-void authentication under charged guardian work :419-433 — keep its semantics); `context` :470-478; **`void_retry` :497-504, `void` :506-524** (validity + `VOID` budget event); `_save_budget` :565-590; **`_check_budget` :592-608**; `begin_admission` :624-680; `bind_budget` :681-716; `reserve_work` :732-783; `settle_work` :813-836; `record_work_transition` :838-894; `recover_work` :896-944; integrity :952-1151 (`pending_void` handling :1066-1067).
- `ops/c1_rail/qualification/execution/campaign_funding.py` — `parse_request` :38-60; `FundingStoreMixin` :275 (`_funding` :279, `_bootstrap` :293, `_funding_gate` :314-326, `_funding_projection` :327-400, `_validate_*` :401-466, `_project_funding` :467-508, `scheduler_status` :509-527, **`claim_scheduler_bootstrap` :529-645** (the durable one-use, pre-work debit pattern), `materialize_scheduler_bootstrap` :647-757, `recovery_budget_snapshot` :767-775, `_funding_integrity` :777+). Report whether the funding tables/projection admit an additional intent kind without a schema/profile/snapshot version change.
- `ops/c1_rail/qualification/execution/campaign_budget.py` — `recovery_pending` :143, `dispatch_pending` :200, `validate_*`; `journal_snapshot.py` (snapshot/v5 strict parser — find `parse_campaign_budget_snapshot` and the funding/event-kind whitelists); `profile.py` :37-44 (`CAMPAIGN_RESOURCE_SCOPE`: `control_cpu_seconds=1`, `control_wall_seconds=10`, `cpu_granularity_seconds=1`), :84-116 (`parse_campaign_budget_profile`, `funding_intents` :97), :118-155; `release_schema.py` (v4 literal); `campaign_protocol.py` `permitted` :45-47 (operator: STATUS/VOID — unchanged).
- `ops/c1_rail/qualification/execution/campaign_supervisor.py` (read-only for you) — `run_campaign_work` :261-277 (RESERVED → `prepare_campaign_work` → `_launch`; non-RESERVED returns status: the idempotent resume primitive), `prepare_campaign_work` :89-118 (START_OWNER + START_INTENT + enrollment atomically, before any OS effect), `_recover_campaign_work` :204-216 (the `RESERVED` branch: `recover_work` + CLEANUP ABSENT + `_complete_recovery` — establish whether this spends the RECOVERY_OWNER slot), `controller_cpu_guard` :659-683, `_assert_authority` :694-701.
- `tests/ops/qualification/execution/test_service.py`, `test_campaign_admission.py`, `test_campaign_recovery.py` (:223 uses a RESERVED work), `test_campaign_funding.py`, `test_campaign_scheduler.py` (the R2b route tests: crash-after-materialization, six refusals, concurrent duplicates — your templates), `test_campaign_budget.py` helpers, `lifecycle_model.py`/`test_lifecycle_model.py`.
- `tests/integration/qualification_boundary/conftest.py` — `Boundary` :51-224 (`admin` :68, `identity` :79, `request(operation, *, role, **fields)` :86 — find the operator role name for `VOID` —, `raw_request` :101, `schedule` :109, `restart` :186), `real_boundary` :251; `test_campaign_supervision_linux.py` helpers :22-70 (`snapshot`, `work`, `wait`, `admit`) — import them, do not copy; `tools/qualification_verification/host.py` (`run`, `save`); `fixture_producer.py`/`fixture_install.py` (how a signed operator VOID approval is produced for tests — the N1 boundary tests already exercise VOID; find them).
- `scripts/qualification_boundary_verification.py` :19 (`S2_CASES` single file), :82 (`required` selection), :97-99 (`--s2` runs `[S2_CASES]`; N1 mode ignores it); `tests/ops/qualification/invariant_manifest.json` (S2 node list ~:360-375); `tests/test_qualification_invariant_manifest.py`; `.github/workflows/qualification-s2-supervision.yml`.

## 0.75. Local-only dependency check

`N/A — no gitignored vendor data, no secrets.` Environment facts: (1) Windows: `./fp.ps1 doctor` from your worktree resolves the shared operations environment `C:/Users/joshu/multi_firm_operations/tmp/ops-env` (Python 3.13.2, 62 locked packages) — verified 2026-09-19 from a sibling worktree; run every pytest/check through `./fp.ps1` (PowerShell 7.3+; use the PowerShell tool). (2) Linux evidence comes only from the S2 CI workflow on your pushed branch: `gh workflow run qualification-s2-supervision.yml --ref claude/s2-g2-service-metering`, then `gh run list --workflow=qualification-s2-supervision.yml --branch claude/s2-g2-service-metering --limit 3`, `gh run watch <id> --exit-status`, `gh run download <id> -n qualification-s2-supervision -D <scratchpad>/s2-<id>`. Every host-side S2 step so far failed on first contact; budget iterations. Windows/mock results are never Linux evidence.

## 0.5. Coordinator defaults (apply unless Phase 0 contradicts; then return `CHECKPOINT` quoting the conflict)

- **(A) Gap 4 — safe admission retry.** Default: in `_diagnostic_campaign_request` `SUBMIT_E1`, an existing row with byte-identical request whose `admission` work is `RESERVED` (no START_INTENT, no dispatch row, no recovery row for that work), campaign `VALID` and `PROVISIONAL`, same boot ID, deadline not reached, no recovery/dispatch pending, resumes through the existing `campaign_supervisor.run_campaign_work(self, reservation, manifest)` with the deterministically rebuilt manifest bytes (:306-308), under the same `dispatch_lock` + `controller_cpu_guard`. Any other existing-row state keeps today's status-only reply. It can never double-start: `prepare_campaign_work` retains START_INTENT before any OS effect and `run_campaign_work` returns status for non-RESERVED works. Restart: `recover_service` must not spend the one-use RECOVERY_OWNER slot on such a structurally absent work (START_INTENT precedes every OS effect — R1); skip it in the loop and report `ADMISSION_RESUMABLE` in `recovery_issues` (informational; it grants nothing). Verify first what `_recover_campaign_work`'s RESERVED branch does today (it is G1's file; you change only `recover_service`); if it does not spend the slot, still skip — a recovery completion row on a never-started work is noise. The funded scheduler path (works materialized directly into START_INTENT; pending intents stay `FUNDING_PENDING`) is unchanged by this.
- **(B) Gap 3 — funded post-admission VOID authentication.** Split the VOID branch into (i) cheap bounded transport that stays unmetered: exact historical retry (`void_retry`), byte-bounded single-body queueing (`queue_diagnostic_void` semantics: ≤ `record_byte_limit`, a different body while one is pending is a cheap refusal), and compact status; and (ii) signature verification, which runs **only** inside a durably funded one-use controller operation charged to the campaign's original allowance: durable claim (records the pending body's SHA-256, reserves `control_cpu_seconds + cpu_granularity_seconds` = 2 s CPU and `control_wall_seconds` wall from the remaining allowance, refused when insufficient / terminal / recovery pending / dispatch pending / VOID already) → `controller_cpu_guard()` → `verify_detached_approval` + enrollment checks (:326-333 unchanged in substance) → settle the reserved charge **whether or not** the signature verified → on success `void(...)`; on failure retain the refusal (event or object; report the kind) and clear the pending body so the operator can queue a corrected request (each attempt is charged). Mirror `claim_scheduler_bootstrap`'s shape (durable before work, one-use, exact-duplicate returns status, terminal overlay → status). Default recording primitive: the existing funding-intent tables/projection with a new intent kind **if** the strict schemas admit it without a version bump; otherwise `CHECKPOINT` with the smallest versioned change and its blast radius (parsers, fixtures, tests), and the fallback of charging through an existing primitive with its over-conservatism quantified.
- **(C) Pending cancellation barrier (coordinator ruling).** While a `pending_void` body exists for an attempt, no **new positive authority** is granted — `reserve_work`, `claim_scheduler_bootstrap`, `bind_budget`, launch/dispatch gates, and completion transitions refuse (`_check_budget`/`_funding_gate` are the natural chokepoints); negative transitions, settlement, recovery and cleanup continue. The barrier is fail-closed, never itself sets `validity=VOID`, and clears only by charged authentication (success → `VOID`; failure → cleared + retained refusal). In the fundable case the same request authenticates immediately, so the barrier is transient; in the unfundable case the campaign is already terminal or pending, so no liveness is lost — state this in §7. `finish_diagnostic_admission` :419-433 already authenticates a pre-admission pending body under charged guardian work before any positive step in the same transaction; keep that ordering so the barrier cannot deadlock admission. Restart preserves the pending body durably; an exact retry of the pending body after restart makes one more charged attempt.
- **(D) Preserved semantics.** VOID grants no new authority; `VALID → VOID` irreversible; the single observable ordering of VOID vs publication (governing spec §2.8; existing tests); exact historical retries return the old receipt; only an enrolled `TEST_ONLY` freeze key on the operator peer can produce `VOID`; `campaign_protocol.permitted` unchanged; N1_ONLY `VOID` :209-226 untouched; the guardian's authority poll remains the VOID→kill wiring (G1 territory; note as limitation, do not fix).
- **(E) File ownership.** You own: `service.py`, `campaign_store.py`, `campaign_funding.py`, `campaign_budget.py`, `journal_snapshot.py`, `lifecycle_model.py`, `profile.py`/`release_schema.py` (only under a `CHECKPOINT`-approved version change), `tests/ops/qualification/execution/{test_service,test_campaign_admission,test_campaign_recovery,test_campaign_funding,test_campaign_scheduler,test_lifecycle_model}.py`, a new `tests/ops/qualification/execution/test_campaign_cancellation.py`, a new `tests/integration/qualification_boundary/test_campaign_service_linux.py`, `scripts/qualification_boundary_verification.py` (turn `S2_CASES` into a tuple of the two Linux files; keep N1 mode ignoring both; keep `--s2` selecting every manifest node under either file), and the S2 node list in `tests/ops/qualification/invariant_manifest.json` (append only; G1 also appends — the coordinator merges). If a G1-owned file must change, stop and return `CHECKPOINT` with the exact interface.
- **(F) Linux proof shape.** New file `test_campaign_service_linux.py` (import helpers from `test_campaign_supervision_linux.py`), registered in the manifest and via `S2_CASES`. Suggested cases: (1) `test_s2_post_admission_void_authentication_is_charged_until_exhaustion`: admit; send an invalid-signature VOID from the operator role repeatedly; each attempt raises `settled_cpu_ns` by exactly the reserved bound and never sets `VOID`; when the claim is refused the status shows the pending body and no authentication ran; a byte-different VOID while pending is refused cheaply (no charge); then, if allowance remains, a valid VOID → `VOID` receipt; an exact retry returns the identical receipt with no further charge; `restart()` preserves everything; (2) `test_s2_void_barrier_blocks_new_authority_but_not_settlement`: with a pending unauthenticated body, a private-route schedule request is refused (compact status, no guardian unit started — check `systemctl list-units`), while a running work still settles; (3) `test_s2_admission_retry_after_service_death_never_double_starts`: send `SUBMIT_E1`, immediately `systemctl kill --signal=STOP` the supervisor service, `restart()`, resend the identical `SUBMIT_E1`; assert whatever state the interruption left (nothing / RESERVED / START_INTENT+) converges without a second guardian start (`NRestarts=0`, ≤ 1 guardian dispatch row for `admission`) and never stays `PROVISIONAL`+`RESERVED`; loop the sequence three times with fresh attempts to sample the window. Deterministic RESERVED-resume coverage belongs to the Windows tests (simulated crash after `begin_admission`).
- **(G) Branch/commits.** Work on `claude/s2-g2-service-metering`; commit in small steps with `git diff --stat` checked before each commit; run `git diff --check`. If the pre-commit gate's child Python fails to start, prefix `PATH=/c/Python314:$PATH` (known Windows quirk). Never `git stash`. Push the branch (no PR).

## 1. Context and deliverables

R2b (PR #433, accepted 2026-09-19) made the warm service the only campaign intent producer and funded the private scheduler route before any construction. Two service-side gaps remain. Codex on PR #429 (`service.py:292`, P1): "If the service dies after `begin_admission()` commits but before `run_campaign_work()` creates the start intent, restart recovery confirms absence while deliberately leaving the admission work `RESERVED`. Every identical `SUBMIT_E1` then takes this branch and returns status without calling `run_campaign_work`, so the attempt remains permanently provisional with its reservation consumed even though it is safe to perform the first launch." The coordinator handoff's R3: "`_diagnostic_campaign_request` authenticates post-admission VOID from enrolled originals/keys inside an unmetered branch. Invalid/nonidentical requests can repeat expensive authentication. Distinguish cheap bounded transport/historical replay from signature verification. Enroll funded bounded authentication before doing it and preserve cancellation's no-new-authority semantics, durable VOID ordering and exact historical retries… Coordinate any temporary pending cancellation barrier explicitly."

**Six-field summary:**
- **Selected outcome:** (3) every post-admission signature verification is preceded by a durable charge against the campaign's own allowance and bounded by the controller guard; unfunded requests are queued cheaply and block new positive authority until charged authentication resolves them; no route forges `VOID`. (4) an exact `SUBMIT_E1` retry resumes a structurally absent `RESERVED` admission exactly once, and service restart neither spends a recovery slot on it nor strands it.
- **Prerequisites:** `main@f2606b0`; this handoff and G1 on the base branch.
- **Ownership:** you write the §0.5 (E) files; G1 writes the supervisor side concurrently; the coordinator integrates, reviews (fable-judge + Codex), and accepts.
- **Verification:** §2.6 Windows selection on frozen bytes; S2 workflow run(s) on your branch with record IDs; `git diff --check`.
- **Checkpoint:** return `CHECKPOINT` before runtime edits if a §0.5 default conflicts with a Phase 0 read, a versioned schema/profile/snapshot/DB change is required, or a G1-owned file must change. Otherwise proceed.
- **Return boundary:** gaps 3 and 4 only. Excludes gaps 1/2/5 (G1), S2 acceptance, S3–S8, provisioning, activation, merge.

**Deliverables (one pushed branch):**
1. `service.py` — resume branch + restart skip (gap 4); split VOID branch with funded authentication (gap 3).
2. `campaign_store.py` / `campaign_funding.py` / `campaign_budget.py` (+ `journal_snapshot.py`, `lifecycle_model.py` as affected) — durable authentication claim/settlement, pending-void barrier in the authority gates, refusal retention.
3. Windows tests: `test_campaign_cancellation.py` (new) and extensions to `test_service.py`, `test_campaign_recovery.py`, `test_campaign_admission.py`, `test_campaign_funding.py`/`test_campaign_scheduler.py` (barrier refusals on the route), `test_lifecycle_model.py` if the model changes.
4. `test_campaign_service_linux.py` (three cases per §0.5 (F)), `S2_CASES` tuple, manifest registration; existing nine S2 cases untouched by you and still passing on your branch.
5. §7 of this file filled in.

**Not asked:** anything in G1's files, VOID→kill wiring, allowance/ceiling changes, the governing spec, the R2 contract, `campaign_protocol.permitted`, N1_ONLY paths, `store.py`, the ledger (the coordinator writes the ledger entry at acceptance).

## 2. Execution plan

### Step 2.1 — Phase 0 report
Read §0 in full; write the read-report into §7 (anchors via `git log -1 --format=%h -- <path>`), including: whether `_recover_campaign_work`'s RESERVED branch spends the RECOVERY_OWNER slot; the operator-role name and the VOID approval producer in the harness; whether the funding projection admits a new intent kind without a version change; the exact list of authority gates that must honour the pending barrier.

### Step 2.2 — Gap 4
- **Action:** per §0.5 (A). Windows tests (simulated runtime via `context.campaign_runtime`): crash after `begin_admission` (raise inside `run_campaign_work` on first call) → restart on a fresh `ExecutionService` over the same store → work still `RESERVED`, no recovery row, no OS effect, `ADMISSION_RESUMABLE` reported; identical retry → exactly one `prepare`/one runtime `start`; a second identical retry → status only; changed bytes → refusal; retry after VOID / expired deadline / boot change / pending recovery → no launch; concurrent identical retries → one launch; a resumed launch that fails takes the R1 path with an **unspent** recovery slot.
- **Gate:** all of the above green; no G1 file touched.

### Step 2.3 — Gap 3
- **Action:** per §0.5 (B)–(D). Windows tests: repeated invalid requests each charged exactly the reserved bound; exhaustion → claim refused, pending stays, no authentication, no VOID; barrier blocks `reserve_work`/`claim_scheduler_bootstrap`/`bind_budget`/launch gates but not settlement/recovery/negative transitions; restart preserves pending; exact retry → one more charged attempt; different body while pending → cheap refusal, no charge; valid approval → VOID receipt; exact historical retry → identical receipt, no claim, no charge; forged/unenrolled key → refusal retained, charged, validity still VALID; publication-vs-VOID ordering tests still pass; client-role peer refused before any cost; pre-admission pending body still authenticated inside `finish_diagnostic_admission` before any positive step.
- **Gate:** all green; `grep -n verify_detached_approval service.py campaign_store.py` shows the diagnostic post-admission call only inside the funded path.

### Step 2.4 — Harness registration
- `S2_CASES` tuple; `--s2` selection = all manifest nodes under either file; N1 mode ignores both files; `tests/test_qualification_invariant_manifest.py` passes; the workflow needs no change.

### Step 2.5 — Linux runs
- Push; dispatch the S2 workflow; iterate; save run IDs, record IDs, counts, `invariants.json passed`, cleanup `ok`. Final green run required: nine existing + your three nodes pass, `source_stable=true`, `capture_complete=true`.

### Step 2.6 — Verification selection on frozen bytes (PowerShell, from your worktree)
```
./fp.ps1 doctor
./fp.ps1 --workers 2 python -m pytest tests/ops/qualification/execution/test_campaign_scheduler.py tests/ops/qualification/execution/test_campaign_funding.py tests/ops/qualification/execution/test_campaign_cancellation.py tests/ops/qualification/execution/test_campaign_supervision.py tests/ops/qualification/execution/test_campaign_budget.py tests/ops/qualification/execution/test_campaign_recovery.py tests/ops/qualification/execution/test_campaign_admission.py tests/ops/qualification/execution/test_profile.py tests/ops/qualification/execution/test_release.py tests/ops/qualification/execution/test_service.py -q --tb=short
./fp.ps1 python -m pytest tests/ops/qualification/execution/test_lifecycle_model.py tests/ops/qualification/execution/test_artifact_acceptance.py tests/test_qualification_invariant_manifest.py -q --tb=short
./fp.ps1 --workers 2 python -m pytest tests/ops/qualification tests/ops/test_phase3_provenance_acceptance.py -q --tb=short
./fp.ps1 check
git diff --check
```
(The fourth line is the isolation bridge child's own selection; the bridge `tests/ops/test_qualification_isolation.py` itself exceeds its 1800 s limit on this Windows box — do not claim it.) Capture each command's exit code before proceeding; a pytest exit 4/5 is not a pass. Record interpreter, record IDs (under `.cache/fp-verification`), counts and skips.

## 4. Falsifiable hypothesis
**H:** both properties are implementable inside the §0.5 (E) surface, without a version bump, without enlarging any allowance, and each has Windows regressions that fail on `f2606b0` bytes plus Linux cases that pass on your branch. **Reject if:** a G1-owned file must change; the charge cannot be recorded without a versioned change (→ `CHECKPOINT`, not a workaround); the barrier would block admission or settlement; a resumed admission can double-start under any interleaving → `BLOCKED — plan-itself-wrong`. **Ambiguous-hold if:** a §0.5 default contradicts a Phase 0 read → `CHECKPOINT`.

## 5. Forbidden moves
- Performing signature verification anywhere outside the funded, guarded path; charging after the work instead of before; refunding a failed authentication.
- Letting a pending body, a claim, or a refusal set `validity=VOID`; letting any peer other than the operator role reach the queue.
- Spending or fabricating a RECOVERY_OWNER/START_OWNER slot for a never-started work; issuing a start from the retry branch for any work not `RESERVED`; touching `_recover_campaign_work`.
- Version bumps (DB/profile/release/snapshot) without a `CHECKPOINT` ruling; enlarging the 20 s / 2 s / 1 s / 10 s bounds.
- `xfail`/`skip` to get green; Windows results described as Linux evidence; claiming S2 acceptance; editing G1's files, the governing spec, the R2 contract, the ledger, `campaign_protocol.permitted`.
- `git stash`; commits without `git diff --stat`; pushing after a pytest run whose exit code you did not capture.

## 6. Return taxonomy and report format
`RESOLVED` = every §2 gate met, §2.6 exits 0 with zero skips in the first selection, twelve S2 nodes green on a recorded workflow run, branch pushed, §7 complete. `DONE_WITH_CONCERNS` = same but with listed limitations. `CHECKPOINT` = design/interface question per §1 Checkpoint (no runtime edits yet). `BLOCKED — plan-itself-wrong` = a §4 reject. Final message to the coordinator: status line; branch + head SHA; `git diff --stat f2606b0...HEAD`; Windows record IDs with counts; Linux run IDs + record IDs + counts + cleanup; changed interfaces (claim/refusal kinds, status fields, barrier chokepoints, `recovery_issues` labels, `S2_CASES` shape); limitations; anything you noticed in G1's territory (findings only).

## 7. Executor return (fill in; this section is yours)

_Pending._
