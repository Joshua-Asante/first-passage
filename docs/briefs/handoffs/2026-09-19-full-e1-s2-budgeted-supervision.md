# S2 — Budgeted admission and real Linux work supervision

> **Activated for local S2 implementation at accepted S1 commit `430fc72186a0e642c8811be17d459fd87cbf7533`. Linux acceptance remains pending.** Use superpowers:executing-plans and applicable implementation workflows. Read AGENTS.md, the entire execution plan's common contract, its S2 slice and the governing specification. The coordinator owns acceptance. This document does not authorize repairing S1 in parallel or advancing to S3.

**Selected outcome:** A fresh execution-capable TEST_ONLY campaign admission and fixed supervised probe work consume the same durable allowance, with real Linux CPU, shared-memory and BOOTTIME enforcement, interruption recovery and owned cleanup. Statistical checkpoint dispatch remains disabled until S3. Local implementation alone does not establish this Linux outcome.

**Prerequisites:** Satisfied for local implementation: repaired S1 is accepted at `430fc72186a0e642c8811be17d459fd87cbf7533`, published in draft PR #428. Both original findings are closed by the repaired packet and independent review. The coordinator verified all 24 publication files against this commit, the publication manifest and both successful final verification records. Remote CI was still running at activation; this is not PR merge-readiness or Linux acceptance. Real-host acceptance requires a disposable supported Ubuntu 24.04 host with cgroup v2, Docker and the existing privileged harness. The existing GitHub two-host workflow is running for S1, but publishing/running a new S2 candidate is not authorized by S1 publication alone. Implement and verify locally now; prepare the concrete Linux candidate and identify the existing route before requesting any missing publication/access authorization. No paid provisioning is assigned.

**Ownership:** The S2 executor owns service/admission integration, trusted supervision and installed configuration for this one boundary. **Coordinate protected E1 execution** owns integration, the S1 dependency decision, S2 acceptance and combined campaign acceptance. The separately assigned S1 repair/review owner retains its work until accepted; no simultaneous writers to shared persistence, profile or service files.

**Verification:** Use the tested checkout's `./fp.ps1 doctor`, behavior tests before implementation, the targeted selection below, `./fp.ps1 check` and `git diff --check`. Retain recorder completion, command and verification exits, stable source, complete capture, actual reports, skips/advisories, interpreter and exact installed source/config identities. Exercise the real Linux cases below through the existing harness with unconditional owned cleanup and retained evidence. Reuse unchanged predecessor evidence. Reserve the integrated E01–E12/two-host full campaign acceptance for S8.

**Checkpoint:** First record the accepted repaired predecessor and actual consumed interfaces; investigate the Linux access route alongside local implementation. Report any mismatch with the repaired S1 schemas or authority/recovery contract before dependent implementation. At return supply an admission-to-reservation-to-process-to-counter-to-settlement/recovery trace, exact installed release/profile identity and evidence. Name any phase whose cost remains unattributed; that gap prevents accepting this boundary. Ordinary fixes within S2 are authorized. A missing host or publication route permits a local implementation return with Linux acceptance explicitly pending.

**Return boundary:** Return after locally verified integration and targeted real Linux supervision evidence, or a concrete prerequisite/interface/access conflict. Exclude N1/N2/Part A draws, actual G5 adjudication, result signing/commit, qseal signing/publication, S3–S8, production authority and account actions. Fixed harmless installed probes may exercise future role scheduling and its accounting. Do not progress into a successor slice or describe a diagnostic release as final E1 authority. Deliver local reviewable changes and a checksummed packet; no commit, push, PR, merge or deployment is assigned here.

## Accepted predecessor and historical review lineage

Start a fresh isolated checkout directly at `430fc72186a0e642c8811be17d459fd87cbf7533` on `codex/full-e1-s1-durable-budget`. This commit includes Task 1a, Task 1b, S1 and both S1 repairs; no recovery overlay is needed. Do not modify the S1 PR branch. Verify its 24 files against `C:/Users/joshu/multi_firm_operations/recovery/full-e1-s1-pr-20260919/manifest.json`; publication source-set SHA-256 is `74226bc65bc0414c36289d21a8a585a54ca820c1d409a6a0917a093ad29003f0`. The unrelated CAP/feed addendum remains in the preserved recovery packet and is deliberately absent from this commit.

Accepted repair evidence: `C:/Users/joshu/multi_firm_operations/recovery/full-e1-s1-repaired-20260919/README.md` and `independent-review.txt`. Publication-normalized source has fresh completed records `20260919T030723Z-de0e1181f90f` (166 passed, zero skips) and `20260919T030742Z-77568a5378ff` (check success, three existing skips/advisories), under `recovery/full-e1-s1-pr-20260919/evidence/`. Both record complete capture, stable source, zero command/verification exits and no capture/report errors. The coordinator checked current files against both records and committed blobs without rerunning unchanged tests.

The original rejected candidate below is retained only as history. Its hold is superseded by this activation; never restore it over the accepted commit.

The original S1 return is reference material, **not an accepted implementation baseline**:

- Packet: `C:/Users/joshu/multi_firm_operations/recovery/full-e1-s1-20260919/README.md`.
- Returned ZIP SHA-256: `55ce847ccebad22adb0efabdcccfe6d691376d3bc240d36c4516072af9f68ff2`.
- Returned runtime/test source-set SHA-256: `a6405e184e433b579f9595fbfa032ea9ab7d4ec58959523a2e6ec9d979af3ab4`.
- Review disposition: **CHANGES REQUIRED**. Original 137 passing tests/check evidence remains valid for covered behavior, but three retained counterexamples expose missing recovery cases.

The accepted repair preserves immutable settlement while independently accepting fresh recovery clock/lifecycle and parent resource facts. It adds a linked reservation through the unchanged `reserve_work` signature: the closed reservation object adds `signing_retry_of`; its parent is a settled SIGNING_INTENT without a candidate, the same phase ceilings apply, and `input_sha256` binds the fixed signing payload. Every retry has its own charged work/scope identity. No stochastic parent, nested retry or concurrent linked retry is allowed. Known interrupted retries close ABORTED while preserving the original intent; unknown accounting bars authority. First linked retry emits snapshot v2, while historical v1 remains readable; database v6 is unchanged. Read the accepted repair plan and tests for exact freshness, terminal-state and candidate-finalization rules.

Use the published accepted commit above. Never use the bare PR425 merge `1e4928360b95812b04725dc1e8da97709d670ff4`: it lacks accepted Task 1a/1b and S1. Preserve every predecessor packet and unrelated work. Record the exact accepted identity before edits.

In that checkout read:

- `docs/superpowers/plans/2026-09-18-full-e1-execution-slices.md`, including the common decisions and S2.
- `docs/superpowers/specs/2026-09-17-protected-full-e1-campaign.md`.
- The repaired S1 return schemas, tests, review closure and migration limitations.

Local S2 implementation is now authorized. The existing S1 publication task owns PR #428/CI monitoring; avoid duplicate maintenance. Report a new source-bound predecessor finding to the coordinator rather than changing the S1 branch. Linux acceptance and any S2 publication remain separate from this local activation.

## Consume S1's actual persisted contract

The returned candidate exposes private CampaignStore methods `begin_admission`, `bind_budget`, `reserve_work`, `settle_work`, `record_work_transition`, `recover_work` and `budget_snapshot`. They return canonical snapshot bytes; preserve unchanged predecessor method meanings. Read the accepted repair for additions or revised signatures, especially the two recovery paths above.

Key integration obligations established by the existing source and return:

- `begin_admission` creates the `admission` reservation and a provisional intent, not a public dormant-admission receipt. Exact retries use the original request/profile/start-clock bytes; do not resample a start to retry it. Integrate a new versioned executable admission route with atomic binding and retained inputs; do not call the old dormant `admit` route as though the provisional row were its receipt.
- `budget_snapshot` is historical: it samples no real clock. New authority needs a fresh trusted observation through the accepted observation/recovery interface. Consumers inspect returned terminal state and validity; a returned snapshot alone grants no authority.
- `recover_work` owns its durable recovery transaction and must return before cleanup. Do not nest it in an outer transaction or roll back a newly recorded terminal fact by attempting an invalid continuation. Incorporate the repaired post-settlement recovery semantics.
- Preserve the authority/accounting revision split. Ordinary settlement must not invalidate a legitimate fixed signing token, while VOID, exhausted/uncertain budget and other authority changes must. Use the accepted metered fixed-intent recovery mechanism for future role scheduling, exercised only with harmless probes in S2.
- Existing dormant release/profile v2 campaigns remain inspectable and admission-only. They and N1_ONLY attempts cannot be upgraded or given fabricated lifetime history. Every new installed executable-capable release/profile revision uses fresh attempts.
- Current S1 migration is additive/lazy v6 under the existing ExecutionStore authority, with exact v4/v5 readability and no downgrade. Consume the accepted repaired version; do not weaken schema checks or introduce a second ledger/database.

## Implement the complete supervision boundary

1. **Installed configuration:** Define reusable resolved phase ceilings once and bind their identity/version to the new installed release/profile. The current S1 phase set includes ADMISSION; N1/N2/PART_A and their CAPTURE/G5 phases; RESULT; SEAL. Compute includes source proof/probes and maximum Part A expansion; capture includes attestation; finalization is budgeted. Read the accepted parser for its exact required fields. Bind the profile's installed-profile digest without cyclic hashing. Validate route feasibility against the frozen cap, while statistical dispatch remains disabled.
2. **Meter admission before planning:** Cheap bounded transport/role checks may precede intent. Persist the provisional intent and owned resource scope before campaign-specific verification, source proof, planning or serialization. Use the installed admission ceiling while the frozen contract is untrusted. Bind the authenticated budget to the original BOOTTIME start, include already-consumed CPU/wall, and reject insufficient allowance. Authentication failure grants no qualification authority. Revalidate originals at the admission publication boundary. Retain small historical inspection without hiding new expensive eligibility reconstruction in an uncharged status call.
3. **Trusted supervision:** Add `execution/campaign_supervisor.py` as an adapter over the existing store/runtime, not another store. The proposed `observe_campaign_clock()`, `run_campaign_work(context, reservation_bytes, input_manifest_bytes)` and `recover_campaign_work(context, reservation_bytes)` interfaces remain S2-owned until implemented and reviewed. Manifests select only installed fixed roles/entrypoints and bounded inputs. Clients cannot supply commands, code, executable paths or authoritative counters.
4. **Real attribution and enforcement:** Enforce cumulative CPU usage, the shared concurrent memory footprint and the original BOOTTIME deadline across descendants, Docker workers and role-separated helpers. CPU rate limiting alone is insufficient. Attribute shared service orchestration conservatively. Persist owned scope/process/container identities before start and charge serialization, capture, deterministic reconstruction and signing-related probe work. Use trusted counters; lost counters consume the reservation. Record memory peak and OOM evidence from the shared scope. Unknown accounting and overruns permanently prevent new authority.
5. **Recovery and cleanup:** Trace RESERVED, START_INTENT/RUNNING without capture, CAPTURED and fixed-intent recovery through the accepted S1 state machine. No missing capture permits a redraw. Preserve exact saved bytes, original deadline/allowance, historical receipts and scope ownership after restart. Commit IN_DOUBT/uncertainty before owned cleanup. Cleanup failure retains uncertainty and history; no late worker result may restore authority. Verify actual process absence before claiming cleanup success.
6. **Diagnostic admission only:** Provide the private scheduling machinery required by later G5/result/seal consumers using installed harmless probes. Admit a fresh execution-capable diagnostic revision while keeping all statistical checkpoints disabled. A later complete release uses fresh attempts, not activation of old diagnostic history. Preserve the existing N1_ONLY boundary and regression guarantees where shared runtime code changes.

## File ownership and verification cases

Planned owners are `ops/c1_rail/qualification/execution/{service,admission,launcher,budget,runtime,profile,release_schema,release,campaign_store}.py`, the new `campaign_supervisor.py`, `tools/qualification_verification/{host.json,host.py,provision.sh,cleanup.py,role_policy.py}`, installed `deploy/qualification/bootstrap.py`, and affected existing instance/image fixtures. Reuse the existing harness, recorder and invariant manifest. Register only executable targeted Linux cases needed for the changed boundary; do not create a parallel acceptance manifest or broad S8 suite in this slice.

Required real-host observations: two sequential operations consume one allowance; two descendants aggregate; cumulative CPU exhaustion stops owned work; shared-memory pressure yields trusted OOM evidence; wall expiry includes downtime/queues; counter loss charges the reservation; supervisor restart preserves owned identities and no-redraw rules; current clock facts still persist after prior settlement; cleanup failure bars authority. Exercise the repaired fixed-intent retry budget with an installed harmless role probe, not real signing. Include sufficient/insufficient allowance, duplicates and invalidation. Verify role membership and refusal of arbitrary client commands/counters. Report exact cgroup/process/container histories and cleanup outcomes.

Local selected checks, extending only as affected interfaces require:

```powershell
./fp.ps1 doctor
./fp.ps1 --workers 2 python -m pytest tests/ops/qualification/execution/test_campaign_supervision.py tests/ops/qualification/execution/test_campaign_budget.py tests/ops/qualification/execution/test_campaign_recovery.py tests/ops/qualification/execution/test_campaign_admission.py tests/ops/qualification/execution/test_profile.py tests/ops/qualification/execution/test_release.py tests/ops/qualification/execution/test_service.py -q --tb=short
./fp.ps1 check
git diff --check
```

Use the current existing host harness and recorder to select the implemented Linux cases; record the actual command, exact node IDs, candidate identity, reports and unconditional cleanup. On Linux use the configured operations environment through `python -I scripts/fp.py`; never bypass validation with system Python. Windows, mocks, host-readiness probes and exit 137 alone do not prove the Linux accounting/OOM boundary. If remote publication is needed but not already authorized for this assignment, complete the concrete local candidate and return the precise publication/host dependency; do not spend, push or launch unrelated infrastructure.

## Return packet

Return the accepted repaired predecessor and exact final source/config identities; actual supervisor/store interfaces; installed release/profile revision and canonical ceiling producer; phase-to-process/cgroup/counter ownership map; admission and interruption traces; all local/Linux verification records and actual cleanup; review findings; schema/profile migration and rollback limits; and any pending host access or unmetered phase. Explicitly distinguish implemented, locally verified and Linux-verified behavior. Update the execution plan ledger as delivered for coordinator review, not self-accepted.

Preserve the complete restore source, updated ledger and evidence as a newly named checksummed packet under `C:/Users/joshu/multi_firm_operations/recovery/`, outside the disposable worktree, without overwriting S1 or Task 1a/1b packets. The coordinator accepts S2 only against that exact candidate and evidence, then selects S3 separately.

## Coordinator interface decision — explicit finalization CPU accounting

**Disposition: authorized within S2, subject to the following contract and source-bound verification.** This is authorization to implement a versioned interface extension, not acceptance of working enforcement. Inspection of accepted `campaign_store.py` confirms observation/v1 settles `charge_cpu_ns` directly from `cpu_ns` (or the reservation when unknown), while `journal_snapshot.py` independently validates that same arithmetic. A bound for work after the last measured counter sample must not be mislabeled as measured CPU or silently omitted.

S2 may extend the canonical installed budget profile and trusted observation schemas to distinguish measured work CPU from a separately identified conservative orchestration/finalization charge. The new charge must be derived from the immutable installed per-phase configuration, reserved before that work, and deducted from the same lifetime cap. It is not an extra allowance outside the phase/campaign budget. The full applicable configured ceiling is charged for the covered residual scope, rather than allowing a caller to choose an arbitrary smaller estimate. Validate phase and maximum-route feasibility including that charge and each permitted deterministic retry.

Define precisely the measurement cutoff and the covered residual work through settlement, receipt/candidate serialization, publication bookkeeping and cleanup/finalization as applicable to S2's diagnostic route. Preserve the raw measured counter and distinguish any intentional conservative overcount from measured usage. Do not refund a reservation while residual work can still consume it, double-refund overlapping scopes, or require an unbounded sequence of charges to account for accounting itself. Durable attribution and reservation precede the covered tail; charged bound and state/receipt changes share the existing transaction authority.

A configured number alone is not evidence that actual work is bounded. Identify and test the installed trusted producer and independent enforcement/termination mechanism that makes the residual ceiling conservative even across service/watchdog interruption. If the proposed shared-process finalization tail cannot be bounded under the existing model, return that concrete enforcement conflict before claiming the interface closes the gap. A timeout or CPU sample after authority escaped is not a substitute. Wall deadline, shared memory, OOM and current validity checks continue through authority-changing finalization; a CPU surcharge cannot waive them.

Preserve observation/v1 and historical snapshot/event semantics byte-for-byte. New observations require a compatible explicit snapshot/profile version; old schema labels must reject new fields rather than silently reinterpret old charges. Canonical parsing, arithmetic and snapshot validation should share their owner rather than duplicate version rules. Additive database changes are permitted only if actually necessary, with exact migration/reopen and rollback limits. Existing N1 and dormant histories remain unchanged.

Immutable settlement retains its original measured value and derived charge. Fresh recovery clock/parent-resource facts remain independently ingestible after settlement. Recovery must not compare a measured counter with a measured-plus-bound total. Counter loss still charges the applicable full reservation and prevents authority; a known overhead allowance does not make unknown work usage known. Linked `signing_retry_of` work receives and settles its own configured overhead charge under the original allowance, preserving the fixed intent and authority token except when terminal facts or VOID invalidate it. Exact retries do not recharge; a genuinely new metered recovery operation cannot reuse previously spent overhead.

The authorized S2 footprint therefore includes the necessary coordinated changes to `execution/campaign_budget.py`, `qualification/journal_snapshot.py`, the lifecycle model and their budget/recovery tests, alongside the already assigned store/profile/supervisor consumers. Update the execution-plan interface notes and return schemas together. S2 remains the single writer; no separate S1 reimplementation is assigned.

Required evidence includes known measured-plus-derived charge arithmetic, bounds reserved before finalization, insufficient allowance before launch/finalization, unknown-counter handling, exact retry/reopen without double charge, immutable settled usage plus fresh recovery facts, linked retries, v1 historical readability/new-field rejection, snapshot/model parity, both VOID orderings and interruption before/after settlement or receipt persistence. Real-host enforcement must demonstrate that the claimed residual ceiling and common CPU/memory/wall boundary hold across supervisor failure. Local schema tests establish only persisted semantics. No statistical dispatch, actual signing/result/seal implementation or new publication authorization is added.

## Coordinator interface decision — campaign resource boundary

The campaign cap is an attributed process/cgroup resource budget, not a total-machine incremental-cost bound. This resolves the shared dockerd/system-manager question under the specification's trusted administrator/Docker-privileged qexec assumption and supervisor-observed process/cgroup accounting model. It does not increase the frozen allowance or waive campaign orchestration accounting.

- Include every campaign-owned controller/guardian, launcher/client, worker, descendant and role helper, plus campaign-specific qexec admission, planning, serialization, custody/commit, reconstruction, authentication, signing and finalization work. Shared qexec handlers cannot hide such work outside accounting. Keep the earlier enforced residual-bound decision.
- Include all CPU and memory reported by the selected trusted accounting scope, including kernel/system CPU or memory charged there; never subtract those costs. Shared platform daemon/system-manager work and host kernel/background resources outside that scope are excluded. Do not claim that the campaign allowance bounds their incremental cost or total host resource use. A dedicated per-campaign Docker daemon is not required by this handoff.
- Platform exclusion permits ordinary fixed, bounded lifecycle requests only. It does not permit moving campaign computation, archive construction, validation, signing or other qualification work into an excluded daemon. Explicitly inventory lifecycle requests and inputs; preinstalled artifacts and bounded request sizes must prevent campaign admission from hiding image builds or other campaign processing in platform infrastructure.
- Queueing, platform waits and downtime still consume the original wall deadline. Resource-scope establishment, daemon failure, guardian failure, lost counters, uncertain termination and cleanup failures retain fail-closed authority rules. Platform exclusion does not establish crash survival or termination proof.
- The common concurrent memory boundary covers all included campaign processes, including controllers and role helpers; it is not a separate full allowance for each. Do not claim whole-host memory enforcement. Demonstrate the actual hierarchy and accounting in Linux evidence.
- Record this resource boundary explicitly in the canonical versioned installed configuration/profile identity and admission/evidence interpretation. Update the governing specification and execution-plan resource sections in the S2 checkout with this coordinator clarification, preserving predecessor history and N1_ONLY semantics. Keep configuration definitions canonical. Document measured CPU versus installed residual charge and excluded platform resources in the return packet.

Continue local S2 implementation within this boundary. The residual CPU bound still requires real enforcement, and Linux acceptance remains pending until demonstrated. Return any remaining concrete conflict without inventing evidence or advancing to S3. Publication, paid provisioning and deployment authorization remain unchanged.
