# CC handoff — Protected Full E1 / S2-G3: never resume a payload before its entrypoint has exec'd; retain the payload exit on every settlement path

**Type:** cc_handoff (bounded repair, coordinator-diagnosed root cause)
**Date:** 2026-09-20
**Status:** dispatch now — coordinator root-cause of the 14/15 on integrated run 35494972519 (`4281d2e`, docs-only over the fifteen-green head `bbafe68`). Branch `claude/s2-g3-resume-before-exec` off `claude/s2-enforcement-gaps-fab5e6` @ `4281d2e`; push, no PR (the coordinator merges into the integration branch / PR #436).
**Authority:** `ops/c1_rail/qualification/execution/campaign_supervisor.py` (PROCESS/RESUMED/new exit event, `parse_supervision_event`, `_process_identity`, `_run_probe`), `campaign_store.py` ONLY if the integrity walk enumerates event kinds and needs the new kind (report it), `tests/ops/qualification/execution/test_campaign_supervision.py`, `tests/integration/qualification_boundary/test_campaign_supervision_linux.py` (assertions only — no new registered node), this file's §7. No allowance, ceiling, profile, release, schema-version or probe-side change; `campaign_probe.py` stays as is. A `DONE` status supplies no permission.

## 1. Root cause (coordinator, from the run artifact — do not re-derive, do verify)

Run 35494972519, attempt `linux-3f9e8e2c…`, work `descendants`: container shim connected 06:45:52.789; the guardian retained PROCESS pid 3609 uid 65532 at 52.852 (+63 ms) and sent the first SIGUSR1 (RESUMED) at 52.860 (+71 ms); the container scope deactivated at 53.18 (+390 ms); observation 53.277 `cpu_ns=115237000`, `termination_known=true`, `oom_events=0`; no CAPTURED/COMPLETED (so the exit code was non-zero); no burner children ever appeared; the guardian exited 0; the test's own restart recovered the work to IN_DOUBT with charge 20.115 s < the 120 s reservation the test correctly requires on the overrun path. Excluded by evidence: pids limit (64), OOM (kernel log has none at that time), the guardian's overrun/OOM kill (no IN_DOUBT transition by the guardian), the probe's 30 s resume timeout (died in 0.4 s), the pre-arm dropped signal (the kernel drops, never kills, a SIG_DFL signal to a namespace init — `sig_task_ignored`/`SIGNAL_UNKILLABLE`). In the green run 35493582848 every payload was first observed 88–122 ms after shim connect; in the failed run 59–77 ms (faster host) — the descendants send simply landed earlier than the others.

**Mechanism:** `_run_probe` sends the resume as soon as `seen_pids` contains the container's init pid, and `_process_identity` verifies only start_ticks/uid/cgroup. runc's init process (`runc:[2:INIT]`, a Go program) drops to the container uid (`setupUser`) *before* `execve` of the entrypoint, so for a few tens of ms the init pid passes the UID check while still being runc. The Go runtime installs a handler for SIGUSR1 whose default action is termination (`_SigNotify+_SigKill`): a SIGUSR1 in that window kills runc init (Go `dieFromSignal` → `exit(2)` since a self-raised SIG_DFL cannot kill a namespace init) — the container exits non-zero within milliseconds, Python never runs, the guardian settles a measured charge, and the campaign is lost. The "blocked signal can never be lost" invariant only holds *after* `block_resume_signal()` runs; before `execve` the resume is lethal. Fail-closed (no credit), but a liveness defect at roughly one in a dozen payload starts on a fast host, and one that would kill real S3 workers.

**Second defect (evidence):** on the non-zero-exit-with-identity path nothing retains the container's exit code (`exit_code` is retained only inside a CAPTURED capture or a PROCESS_UNOBSERVED event), and PROCESS events carry no `comm`/`exe`, so this failure was indistinguishable from a payload crash after the fact.

## 2. Repair (frozen)

1. **Gate the resume on the exec'd entrypoint.** Extend `_process_identity` to also read `/proc/<pid>/comm` and `os.readlink('/proc/<pid>/exe')` (both readable by the guardian's uid for its own-uid processes; report if not and fall back to `comm` alone). Send SIGUSR1 only when the observed init pid's `exe` basename is the installed interpreter (`python…`) — never while `comm` is `runc:[…]`. Keep polling at the existing cadence until then; the bounded re-send window starts at the first eligible send. Retain `comm` and `exe` in every PROCESS event and in the RESUMED event (`parse_supervision_event` whitelist update; bounded strings). Keep the identity gate (`seen_pids`) semantics unchanged otherwise.
2. **Retain the payload exit on every settlement path.** Add a `PAYLOAD_EXIT` supervision event kind `{container_id, exit_code, oom_killed, finished_at}` from docker's inspect `State` (`ExitCode`, `OOMKilled`, `FinishedAt`), retained after the loop breaks and before settlement, on both the credited and the non-credited paths (PROCESS_UNOBSERVED keeps its own event too). Update the parser and, if `campaign_store.integrity()` enumerates kinds, its walk.
3. **Windows regressions** (simulated docker/proc, existing test style): (a) first observed pid reports `comm='runc:[2:INIT]'`, `exe='/usr/bin/runc'` → no SIGUSR1 sent, no RESUMED; next poll reports `comm='python3'`, `exe='/opt/ops/bin/python3.13'` → exactly one RESUMED, then the existing completion path; (b) non-zero exit with identity → `PAYLOAD_EXIT` retained with the exit code, settled measured, no completion; (c) parser rejects an unbounded/unknown-field PROCESS/RESUMED/PAYLOAD_EXIT.
4. **Linux assertions** in the existing registered cases (no new node): in `identity_retained`/`admit`, every retained PROCESS for a payload carries `exe` ending in the interpreter basename; the two-descendants case asserts a `PAYLOAD_EXIT` event exists after settlement. Do not relax `charge >= limits.cpu_ns`.
5. Verification (PowerShell, `./fp.ps1`): §2.6 line 1 of the G1 packet (ten execution files, `--workers 2`, zero skips) and line 2; `git diff --check`; then one S2 workflow run on your branch (`gh workflow run qualification-s2-supervision.yml --ref claude/s2-g3-resume-before-exec`): fifteen green required, record ID + cleanup ok in §7. If the run fails on something unrelated, report it — do not re-roll blindly.

## 3. Forbidden
Changing `campaign_probe.py` or the probe roles; widening the resume window with sleeps; relaxing any Linux assertion; new profile constants; touching G2-owned service/funding/budget files; claiming Windows results as Linux evidence; `git stash`; commits without `git diff --stat`.

## 4. Return
`RESOLVED` / `DONE_WITH_CONCERNS` / `BLOCKED` with: branch + head; `git diff --stat 4281d2e...HEAD`; Windows record IDs and counts; the Linux run ID, record ID, counts, cleanup; the exact `comm`/`exe` values observed on the host for runc-init vs python if you capture them; limitations.

## 7. Executor return

**Status: RESOLVED** (GLM continuation 2; the code and tests are the stopped worker's single commit `0ba2775`, critically re-read and host-verified by the successor; this §7 is the only addition).

Branch `claude/s2-g3-resume-before-exec`, head `0ba2775` (plus this docs-only commit). `git diff --stat 4281d2e...0ba2775` (production + tests, all docs-only commits between are excluded by the merge base):

```
 ops/c1_rail/qualification/execution/campaign_supervisor.py | 158 +++++++++--
 tests/integration/qualification_boundary/test_campaign_supervision_linux.py |  67 ++++-
 tests/ops/qualification/execution/test_campaign_supervision.py | 290 +++++++++++++++++++++--
 3 files changed, 465 insertions(+), 50 deletions(-)
```

**Packet claims, verified by the successor against the code and the evidence below.** (1) The resume is sent only when the init pid's image is the exec'd interpreter: `_pre_exec_init` excludes `runc:[…]`, `_interpreter_image` uses the `exe` basename when readable and `comm` otherwise, `INTERPRETER_NAME='python'`; the send is gated on `init_image` read *this turn* for the docker-reported init pid, and the bounded re-send window starts at the first eligible send. (2) `comm`/`exe` are retained in every PROCESS event (the guardian's own startup identity included), in the RESUMED event, and are parser-enforced (`_bounded_image`; comm 1..64, exe ≤4096, `''` allowed for the foreign-UID ptrace gate); the parser rejects the pre-G3 shape, unbounded values and unknown fields. (3) `PAYLOAD_EXIT {container_id, exit_code, oom_killed, finished_at}` (docker inspect `State`) is retained after the loop breaks and **before** any settlement decision on every path — credited, non-credited and `PROCESS_UNOBSERVED` alike; the only path without one is the no-process-absence-proof raise, which never settles. (4) The non-credited non-zero exit now takes durable IN_DOUBT **before** the measured settlement (coordinator addendum, review A2 guardian half); a linked signing retry keeps the settle-only shape (no legal negative transition), and the intent probe's non-zero exit takes the same IN_DOUBT path instead of attempting SIGNING_INTENT. (5) The poll-loop authority read alone re-polls the funding-pending refusal for a bounded 5 s grace (`FUNDING_PENDING_REFUSAL` is pinned to the store's exact literal by a Windows test against the real funded store); `_assert_authority` for every other refusal and the `_transition` read are untouched.

**Windows verification** (ops-env Python 3.13.2 through this checkout's `fp.ps1`, doctor 62 packages): §2.6 line 1 (ten execution files, `--workers 2`) **432 passed / 0 skipped**, exit 0, record `20260920T153410Z-dcbe7ff032be`; line 2 **79 passed + the known Windows symlink skip**, exit 0, record `20260920T155300Z-cbfd160b4e2e`; `git diff --check` clean. New regressions: the runc-init-then-interpreter scene (one send, after the exec'd turn; no PROCESS names runc), the interpreter-image gate table, PAYLOAD_EXIT parser rejections, the credited/unobserved/non-zero/stop-path exit retentions, the intent-probe non-zero shape (`[START_INTENT, RUNNING, IN_DOUBT]`), the store refusing CAPTURED on the IN_DOUBT work, the funding-pending literal pin, the transient re-poll (completes), the past-grace fail-closed (tolerated 0/2/4 s, refused at 6 s, nothing settled), and the immediate fail-closed on any other refusal.

**Linux run** (dispatched as 35521069166, cancelled by the per-ref `cancel-in-progress` concurrency when an identical dispatch 35521178744 started two minutes later — same head `0ba2775`, same actor): **run 35521178744, record `38306dc03e044f9b89b429a1e0625700`, 15 passed / 0 failed / 0 errors / 0 skipped in 1502.1 s**, exit 0/0, `source_stable=true`, `capture_complete=true`, `invariants.json passed=true` with all fifteen registered nodes present, owned-cleanup receipt ok (image and scope removals recorded). Read from the downloaded artifact, not the check mark.

**Host `comm`/`exe` values observed** (`<attempt>-payload-images.json`, retained by the sequential-roles case): the admission guardian's own PROCESS carries `comm='python'`, `exe='/var/lib/fp-qualification-tests/<run>/env/bin/python'` (own-UID link readable); every payload work's retained PROCESS and RESUMED events carry `comm='python'`, `exe=''` — the ptrace gate refuses the foreign-UID `/proc/<pid>/exe` exactly as predicted, so comm alone carried the image on the resume decision; no retained image anywhere names `runc:[`. Each sequential-roles work also proved a clean `PAYLOAD_EXIT` (`exit_code=0`, `oom_killed=false`).

Limitations: (a) the two-descendants PAYLOAD_EXIT assertion is scoped to guardian-settled runs — this run's descendants case took the absolute-timer path (`settled_by_guardian=false`, no settlement exists to bind an exit to), so the host proof of the *stop-path* PAYLOAD_EXIT is the Windows `test_payload_oom_increment_stops_the_container_and_settles_without_completion` (exit 137 retained) and the sequential-roles credited exits; (b) the 5 s funding-pending grace also delays (never bypasses) the permanent `terminal_overlay` refusal, which raises the same literal — bounded fail-closed, noted here because the packet framed the refusal as transient-only; (c) `campaign_store.py` needed no change — `retain_supervision_event` delegates to `parse_supervision_event`, so the new kind and fields validate through the existing path (the packet's conditional §0 authorization was not exercised).
