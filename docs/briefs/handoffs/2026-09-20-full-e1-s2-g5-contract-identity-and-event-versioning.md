# CC handoff — Protected Full E1 / S2-G5: contract-bound payload identity (P2-1) and versioned supervision events (P2-2)

**Type:** cc_handoff (bounded repair; coordinator rulings superseding two frozen instructions)
**Date:** 2026-09-20
**Status:** dispatch now — the one remaining acceptance outcome for S2. GLM's continuation-2 return (ledger "GLM continuation 2 — S2 acceptance candidate, 2026-09-20") delivered G3/G4 integrated and fifteen-green (run 35530964034 @ `14a0e28`) and correctly stopped at CHECKPOINT on two confirmed P2 findings from the external Codex review. This packet rules on both. Branch `claude/s2-g5-contract-identity` off `claude/s2-enforcement-gaps-fab5e6` @ its current head (`git fetch` first; `035c655` at authoring). Push, no PR; the coordinator merges into #436.
**Authority:** `campaign_supervisor.py` (parser, producers), `campaign_store.py` (identity rule, integrity walk, event readers), `campaign_funding.py` if the compact model's fixtures need an explicit mode, the affected test files under `tests/ops/qualification/execution/` and `tests/integration/qualification_boundary/` (assertions in existing registered cases only), this file §7, and the GLM/G3/G4 packets' §7 **labels** listed in §3. No allowance/ceiling/profile/release/snapshot/DB version change; the supervision **event** schema is versioned by this packet (that is the ruling, not a CHECKPOINT). No S2 self-acceptance, merge or S3 dispatch. A `DONE` status supplies no permission.

## 1. Rulings (supersede the earlier instructions they name)

**R1 — P2-1: payload identity binds by execution contract, not by evidence presence.** Supersedes the coordinator steering "keep the binds-only-when-supervision-events-exist guard" and G4's `_supervision_events_present` scoping. The authoritative contract is **enrollment**: a retained `supervision_<work_id>` enrollment object exists only because a supervised launch was prepared (`prepare_campaign_work` / `materialize_scheduler_bootstrap`), so an enrolled non-admission work is by construction a container-supervised work. Rule: `record_work_transition` to CAPTURED / SIGNING_INTENT / COMPLETED for any work with a retained enrollment, other than the `admission` work, requires ≥1 retained payload-scope PROCESS event (either event version, §R2) whose `cgroup` lies under that work's enrolled payload slice; the integrity walk enforces the same invariant on reopen, so stripping every PROCESS row from a credited journal fails reopen, and an enrolled work with zero PROCESS events can never be credited. **Explicit distinction for legitimate store-only journals:** a work with **no** enrollment is a persistence-layer object (S1-era v2 budget model, funding-model unit scenes) and is outside this rule; integrity additionally refuses a CAPTURED/COMPLETED **enrolled** work without identity. Fixtures: every Windows scene that enrolls and credits must retain a simulated payload-scope PROCESS event (as the retry scene now does); scenes that model pure persistence must not enroll. No versioned change is authorized for this ruling; if an enrolled store-only scene cannot honestly retain a simulated identity, that scene is modelling a supervised work without evidence and must change, not the rule. If you find a *production* path that credits an enrolled work without ever observing a process (other than admission), return `CHECKPOINT` naming it — that would be a real conflict.

**R2 — P2-2: version the supervision event; never rewrite `v1` in place.** Supersedes G3 packet §2.1/§2.3(c) ("parser rejects the pre-G3 shape"). `qualification_campaign_supervision_event/v1` is restored to its pre-G3 strict shape (PROCESS = `{pid, start_ticks, uid, cgroup}`, RESUMED as it was, no `comm`/`exe`, no PAYLOAD_EXIT kind; unknown fields rejected exactly as before), so every historical journal (runs through 35493582848, the #423/#424 evidence journals) reopens. Introduce `qualification_campaign_supervision_event/v2`: PROCESS and RESUMED carry bounded `comm`/`exe` (G3's `_bounded_image`), plus the PAYLOAD_EXIT, DEADLINE and PROCESS_UNOBSERVED kinds as G3 defined them (move the new kinds to v2; a v1 journal never contained them). Producers (`_retain_event` and every caller in the guardian) emit v2 only. Consumers (`parse_supervision_event`, `retain_supervision_event`, the store's event readers, `_payload_identity`, integrity, `campaign_host.cleanup`'s enrollment GLOB is unaffected) accept both versions, each strictly. **Current execution requires the newer evidence:** the resume gate reads `/proc` directly and is unchanged; the identity rule (R1) accepts a v1 or v2 payload-scope PROCESS event so historical credit stands; nothing new is ever written in v1. G3's pinned "rejects the old shape" tests become: v1 rejects `comm`/`exe` and the new kinds; v2 requires them.

**R3 — labels.** A5 is *implemented, reopened by P2-1, closed by R1 on this packet*. G3's launch repair is *host-evidenced*; its journal compatibility is *open under P2-2, closed by R2 on this packet*. A9-4 (README trust-model wording) is **closed** — the text at `tools/qualification_verification/README.md` lines 196–201 on the integration head carries the correction (commit `e85f96c`, in the G3 merge `2f530b1`); GLM's return mislabels it open. Correct these three labels in the G3/G4 packet §7s and note the corrections in your §7; do not rewrite GLM's ledger entry (append, never edit).

## 2. Required regressions (Windows; existing style)
1. **Event-free capture refusal:** enrolled non-admission work, zero PROCESS events, transition to CAPTURED → refused; same for SIGNING_INTENT and COMPLETED; the admission work still completes; an un-enrolled persistence-model work still credits.
2. **Removal of all PROCESS evidence:** credit a work legitimately (enrolled + payload-scope PROCESS), then delete every `supervision_event_*` PROCESS row → `ExecutionStore` reopen / `integrity()` refuses.
3. **Predecessor-journal reopening:** a journal written with v1 events in the exact pre-G3 shape (build it from the parser's v1 contract in-test; if a small real predecessor journal is cheap to include as a fixture, prefer it and record its provenance) reopens clean; its credited works keep their credit; its v1 PROCESS events satisfy R1.
4. **Parser matrix:** v1 accepts the old PROCESS/RESUMED shapes and rejects `comm`/`exe`/PAYLOAD_EXIT/DEADLINE/PROCESS_UNOBSERVED; v2 requires `comm`/`exe` on PROCESS/RESUMED and accepts the new kinds; unknown schema refused.
5. Every existing G3/G4 regression still passes, with the G3 "rejects the old shape" tests re-pointed per R2.

## 3. Verification
`./fp.ps1` (PowerShell 7.3+): G1-packet §2.6 line 1 (ten execution files, `--workers 2`, zero skips), line 2, line 3 (`tests/ops/qualification` + `tests/ops/test_phase3_provenance_acceptance.py`, `--workers 2`), `./fp.ps1 check`, `git diff --check` — on frozen bytes (do not write into the tree while a record is open). Then ONE S2 workflow run on your branch: fifteen green, `invariants.json passed`, cleanup ok, read from the artifact. Return exact source/evidence identities (heads, record IDs with counts, run ID, record hash) for the independent reviewer.

## 4. Forbidden
Presence-based scoping in any form; widening v1; an unversioned in-place shape change; relaxing any Linux assertion; version bumps beyond the supervision event schema; touching the ledger except by append; claiming S2 acceptance; `git stash`; commits without `git diff --stat`.

## 5. Return
`RESOLVED` / `DONE_WITH_CONCERNS` / `CHECKPOINT` / `BLOCKED`: branch + head; `git diff --stat <base>...HEAD`; records and counts; Linux run ID + record; the enrolled-and-credited scenes you changed and why; any production path that credits without observation (none expected); the three label corrections applied.

## 7. Executor return

**Status: CHECKPOINT (repair complete and Windows-verified; the one Linux run is 14/15 on a failure outside this packet's change surface — coordinator ruling needed on that case, not on R1/R2/R3).**

Branch `claude/s2-g5-contract-identity`, head `3db58c8` (code) plus this docs commit. `git diff --stat 9e075db...3db58c8`:

```
 ops/c1_rail/qualification/execution/campaign_funding.py    |   2 +-
 ops/c1_rail/qualification/execution/campaign_store.py      |  99 ++++-----
 ops/c1_rail/qualification/execution/campaign_supervisor.py |  38 +++-
 tests/ops/qualification/execution/test_campaign_cancellation.py | 226 ++++++++++++++++++---
 tests/ops/qualification/execution/test_campaign_funding.py |  13 ++
 tests/ops/qualification/execution/test_campaign_supervision.py |  58 ++++--
 6 files changed, 322 insertions(+), 114 deletions(-)
```

**R1 as landed.** `_supervision_events_present` is deleted — no presence-based scoping remains anywhere. `record_work_transition`'s gate for CAPTURED/SIGNING_INTENT/COMPLETED binds by the execution contract: the admission phase is exempt; a work with a retained `supervision_<work_id>` enrollment requires >=1 retained PROCESS event (either schema version) whose cgroup lies under the enrolled payload slice; a work with **no** enrollment is a persistence-layer object outside the rule. The integrity walk enforces the same invariant on reopen for every credited enrolled work (CAPTURED/SIGNING_INTENT/SIGNED/COMPLETED), so stripping every PROCESS row from a credited journal fails reopen and an enrolled work with zero PROCESS events can never be credited. No version change; no allowance/ceiling/profile/release/snapshot/DB-version change.

**R2 as landed.** `v1` is restored to its exact pre-G3 shape — PROCESS `{pid,start_ticks,uid,cgroup}` and RESUMED `{container_id,pid}` carry no image fields, unknown fields are rejected as before, and the only kind refused outright is PAYLOAD_EXIT. **Packet §2.4 correction (accepted by the coordinator's GO):** DEADLINE and PROCESS_UNOBSERVED (with FAILURE, CONTROL, CLEANUP, CONTAINER) are G1-era kinds present in every historical journal — the pre-G3 producers retained DEADLINE at every guardian start and PROCESS_UNOBSERVED on the unobserved-exit path (retention calls at `4281d2e` lines 879/1146) — so v1 accepts them in their pre-G3 shapes; "a v1 journal never contained them" holds for PAYLOAD_EXIT alone. v2 requires bounded `comm`/`exe` (`_bounded_image`; comm 1..64, exe <=4096, `''` across the ptrace gate) on PROCESS and RESUMED and carries PAYLOAD_EXIT. Producers emit v2 only: `_retain_event` builds v2 for all ten guardian call sites, and the two non-guardian CONTROL producers (`claim_supervision_control`, the compact model's `materialize_scheduler_bootstrap`) write v2. Consumers (`parse_supervision_event`, `retain_supervision_event`, the recovery-completion cleanup read, `_payload_identity`, the integrity walk) accept both versions, each against its own closed shape. The resume gate still reads `/proc` directly and is unchanged.

**Fail-on-base evidence (both P2 reproductions).** Against pre-G5 production at `9e075db` (scratch worktree, detached, with this branch's `test_campaign_cancellation.py` copied in; base production files untouched): `test_event_free_credit_is_refused_for_an_enrolled_work` **fails `DID NOT RAISE`** at the CAPTURED refusal (the presence scope never binds on an attempt with zero PROCESS events — P2-1's mechanism), and `test_a_predecessor_v1_event_journal_reopens_and_keeps_credit` **fails `ValueError: closed schema fields required`** in the parser (base v1 requires `comm`/`exe` — P2-2's mechanism). Both pass on the branch.

**Fixture ledger** (every Windows scene that enrolls and credits; no scene was moved out of the rule's reach): the compact funding scene crediting 'seal' now retains a simulated payload-scope v2 PROCESS after `materialize` (the one scene that enrolled-and-credited with zero PROCESS events; its 'retry' never credits); the supervision retry scene keeps simulated identities for 'seal' and 'retry' with its helper switched to a direct v2 write; the cancellation A5 pair and the three new tests retain simulated PROCESS events (guardian-cgroup as the never-counting negative control); the `_probe_scene` completions keep production-path identities from the real `_run_probe`. **Un-enrolments: none** — the store-only budget/recovery scenes never enroll (the packet's persistence-layer distinction), and scenes that enroll but never credit keep their honest enrollment. **No production path credits an enrolled work unobserved:** enrollments come only from `prepare_campaign_work` and `materialize_scheduler_bootstrap`, both of which continue into a real guardian that retains the payload PROCESS before credit; the admission is the exempt work; the compact model never credits in production.

**Windows verification** (ops-env Python 3.13.2 through the commit worktree's `fp.ps1`, doctor 62 packages; recorded from a clean worktree because the shared main checkout's `tmp/` debris — a nested clone and vendored deps from other sessions — breaks repo-wide source snapshots and pre-commit boundary gates; hygiene item for the coordinator): §2.6 line 1 (ten execution files, `--workers 2`) **448 passed / 0 skipped**, exit 0, record `20260920T224155Z-76415c6fe302`; line 2 **79 passed + the known Windows symlink skip**, exit 0, record `20260920T230342Z-4c16ef3f532b`; line 3 **1521 passed + the Linux-only skip** (1522 collected), exit 0, record `20260920T230459Z-d726c1d98df7`; `./fp.ps1 check` exit 0 (absent-tree data-manifest warnings only), record `20260920T233542Z-57f79b918bed`; `git diff --check` clean. 448 = the integrated 445 + the three new regressions.

**Linux run: 14/15 — CHECKPOINT item.** Run **35543486564** on `3db58c8`, record `db7afc5d5b464e7a8ae301d8c82e4e73` (`status=failed`, exit 1/1, `source_stable=true`, `capture_complete=true`, cleanup ok), read from the downloaded artifact via `scripts/s2_run_evidence.py` (from `origin/main` #447, run outside the repository). The one failure is `test_s2_shared_memory_oom_is_retained_last` (QEXEC-01); the other fourteen nodes — including four resumed sequential-roles payloads, the 100 s two-descendants case, and every identity-bearing completion — are green on this run with v2 events and the enrollment-bound rule exercised throughout. Root cause from the artifact (attempt `linux-c13f1bd2...`): the memory payload (guardian-retained payload-scope PROCESS pid 7276 `comm='python'`, RESUMED, post-G3 gating intact) died **~540 ms after shim connect with docker ExitCode 138 (signal 10 = SIGUSR1, the guardian's bounded resume re-send), `oom_killed=false`, and zero `oom`/`oom_kill` increments anywhere under the shared parent**; the parent's peak stood at 242.7 MB against the 256 MB limit, so the kernel OOM never fired; the guardian then took exactly the designed non-credited path (IN_DOUBT before the measured settlement, charge 20.18 s, campaign IN_DOUBT). The case's own host-side wait for `oom_kill>0` was the only failing assertion. Nothing in this packet's diff touches the resume handshake, the re-send window, the probe, or that case; the contrast with the prior green run 35530964034 on `14a0e28` (same case: **ExitCode 137, `oom_killed=true`, `oom_events=1`, peak exactly 256000000**) isolates a host-side resume-signal delivery that the design's stated safety properties (SIG_DFL drops to a namespace init; blocked + no-op handler is immune) say cannot kill — four hours apart on a rotating `ubuntu-24.04` runner image. Per the packet and the s2-linux-run skill this is reported, not re-rolled and not "fixed": the case and the resume machinery are G1/G3-owned, relaxing any Linux assertion is forbidden, and no root cause at file:line exists inside this packet's authority. **Ruling needed:** re-dispatch (runner rotation), investigate the docker/runc signal path, or open the repair as its own packet.

**R3 — the three label corrections applied.** (1) G4 §7's A5 disposition now carries a bracketed correction: A5 was implemented, reopened by P2-1, closed by R1 (the presence-based scoping — its disclosed limitation (a) — is superseded by the enrollment-bound rule). (2) G3 §7's "parser rejects the pre-G3 shape" claim now carries: the launch repair is host-evidenced; journal compatibility was open under P2-2 and is closed by R2 (the pins are re-pointed to the version matrix). (3) **A9-4 is closed**: `tools/qualification_verification/README.md` lines 196-201 carry the corrected polkit trust-model wording from commit `e85f96c` (in the G3 merge `2f530b1`; verified present on this branch and an ancestor of its head). GLM's continuation-2 ledger entry mislabels it open and stands unedited per the append-only rule; the coordinator's rulings paragraph at `9e075db` and this §7 carry the correction.
