# GLM handoff — Protected Full E1: S2 closeout (PR #436) and S3 dispatch

**Type:** cc_handoff (coordinator-bounded closeout + packet authoring)
**Date:** 2026-09-20
**Executor:** GLM (continuation owner of the S2 integration branch). **Coordinator:** the operator, who accepts.
**Status:** dispatch now. Commit this file onto `claude/s2-enforcement-gaps-fab5e6` as your first act (the branch already carries the G1/G2/G3 packets); keep §7 as your return.
**Authority:** branch `claude/s2-enforcement-gaps-fab5e6` / [PR #436](https://github.com/Joshua-Asante/first-passage/pull/436) and its worktree `C:/Users/joshu/multi_firm_operations/.claude/worktrees/pr-363-babysit-671b91`; the ledger `docs/superpowers/plans/2026-09-18-full-e1-execution-slices.md` (append-only entries); `tools/qualification_verification/README.md` §S2 polkit paragraph; `campaign_store.py` for the A3 validator only; a new S3 packet under `docs/briefs/handoffs/`. Nothing else. A `DONE` status supplies no permission; every acceptance sentence is the coordinator's.

## 0. Verified starting state (2026-09-20, read from `origin`; re-verify before acting)

- `origin/main` = `b703448` = [#439](https://github.com/Joshua-Asante/first-passage/pull/439) over [#437](https://github.com/Joshua-Asante/first-passage/pull/437) (`86d5ed1`, #423/#424 closed: `inspect_environment(…, *, host_config)`, canonical tree bindings, host-evidence workflow) over [#438](https://github.com/Joshua-Asante/first-passage/pull/438) (`994a59f`, gates/inventory) over `f2606b0`.
- PR #436: OPEN, ready-for-review (title "S2 enforcement gaps: G1+G2 integrated — fifteen-node Linux evidence ready for review (S2 still INCOMPLETE)"), head `915035c`, base `main`, `mergeStateStatus=BEHIND` (18 commits behind `main`), `git merge-tree` against `main` **clean**, **no file overlap** with #437's changed files, no unresolved review threads at drafting.
- Branch history since the GLM continuation return: `5ac3ad0` (ledger closeout, run 7 fifteen-green) → `9b2559e` (unobserved-exit attribution correction; run 35492219418 **14/15**, OOM-race case) → `bbafe68` (test-only: OOM case accepts the stop-on-OOM half of the terminal race; run 35493582848 **15/15**) → `4281d2e` (ledger; run 35494972519 **14/15**, `descendants` work — the resume-before-exec race) → `915035c` (S2-G3 packet `docs/briefs/handoffs/2026-09-20-full-e1-s2-g3-resume-before-exec.md`, root cause + frozen repair; `claude/s2-g3-resume-before-exec` **does not exist on `origin`**, §7 pending). Run 35517586779 on `915035c` was in flight at drafting.
- Fifteen-green S2 runs on code-identical heads: 35487909157 (`db667a2`), **35489657203 (`5ac3ad0`)**, 35493582848 (`bbafe68`). Steering findings 1 (payload readiness) and 2 (guardian PROCESS events mistaken for payload observation) repaired in `9d7a731` and evidenced in those runs. Windows records on the code bytes: 421/0, 79+1 known skip, 1494+1 Linux-only skip, `check` exit 0 (ledger "Final Windows verification").
- Known residuals: **(R-G3)** resume-before-exec race — SIGUSR1 can reach `runc:[2:INIT]` after it drops to the payload uid but before `execve`; Go kills on SIGUSR1; container exits non-zero within ~0.4 s; fail-closed (no credit) but a liveness defect at roughly one in a dozen payload starts on a fast host, and it "would kill real S3 workers" (G3 packet §1); **(A3)** work ids prefixed `control_`/`event_` collide with the supervision-object GLOBs (`campaign_store.py` ~:262-268, `campaign_host.py` ~:134) — S3+ names must avoid them, store validator owed; **(A4)** polkit: under the accepted rule qexec can start arbitrary transient units including `User=root`; polkit contributes no containment for unit starts; hierarchy membership rests on the guardian's code checks and `bootstrap.py`'s `campaign_control` fixed-prefix check — the README still says "polkit rule for the qexec `manage-units` scope" (README ~:156) and does not state the arbitrary-unit fact; **(OOM)** the shared-memory case has a documented terminal duality (settle path vs stop-on-OOM path → IN_DOUBT); **(bridge)** the Windows isolation bridge exceeds 1800 s on the coordinator's box and was never claimed.

## 1. What success looks like

**S2 closeout is successful when all of the following hold, in this order:**

1. **Acceptance recorded, evidence-bound.** A new append-only ledger entry `### Coordinator acceptance — S2 enforcement gaps (G1/G2), 2026-09-20` naming the evidence exactly: runs 35489657203 (`5ac3ad0`) and 35493582848 (`bbafe68`), each 15 passed / 0 skipped with record IDs, `source_stable`, `capture_complete`, invariants 15/15, both owned-cleanup receipts ok; steering findings 1 and 2 resolved at `9d7a731`; and a **disclosed-limitations list** that states verbatim: R-G3 (with its rate, fail-closed nature, and that it blocks real S3 worker launch until repaired), A3, A4, the OOM terminal duality, the untested isolation bridge, and "accepted work boundary only — no statistical execution, no N1/N2/Part A dispatch, no activation". The entry says "S2 ACCEPTED as the enforced work boundary" and nothing wider; the ledger's own stopping lines (no activation, no S3 execution) stay.
2. **Compatible with `main` (#437/#438/#439), with fresh integrated Linux evidence.** `origin/main` merged into the branch by a merge commit (no rebase, no force-push, no squash); the Windows selection §2.6 lines 1–3 and `./fp.ps1 check` green on the merged head with record IDs; and **one fifteen-green S2 run whose checked-out SHA is the merge head** (the PR-path run fires on the push). #437 changed provisioning/cleanup under the S2 fixture (tree bindings, `docker_client` preflight), so this run is required even though no S2 source file overlaps. Rule for the known race: if that run is 14/15 and the only failure carries the R-G3 signature (`descendants` work, payload exit non-zero < 0.5 s after the first RESUMED, no burner children, no OOM), record it as R-G3 and dispatch **one** rerun; a second failure, or any non-R-G3 failure, is a return, not a re-roll.
3. **Review clear.** Every Codex/CodeRabbit thread on #436 resolved with a disposition (confirmed-and-repaired, refuted-with-evidence, or deferred-to-named-owner); the ledger entry lists them.
4. **A3 dispositioned before S3 names exist.** Either (preferred, small, G2-owned file) a store-side validator that rejects any work id starting with `control_` or `event_` (and any other prefix that matches a supervision-object GLOB — derive the reserved set from the same constants the GLOBs use, do not copy strings) plus a Windows regression, plus a one-line naming rule in the S3 packet; or an explicit coordinator deferral recorded in the ledger with the S3 naming rule as the interim guard. "Could affect S3's new work names" is resolved when the S3 packet's work names are demonstrably outside the reserved set.
5. **Polkit wording correct where it is read.** README §S2 states the A4 fact plainly (arbitrary transient units including `User=root`; polkit contributes no containment for unit starts; membership rests on the guardian's code checks and the `campaign_control` prefix check); "manage-units scope" phrasing removed; the same sentence appears in the acceptance entry. Grep `polkit|manage-units` across README, plans and `campaign_host.py` comments before declaring this done.
6. **Merged.** #436 merged with a merge commit after: required `skills (3.12)` green on the merge head, the head-bound S2 run from (2), review clear, `mergeStateStatus` not `BEHIND`. Branch kept (worktree + evidence runs reference it). PR body updated to say ACCEPTED-as-boundary with the limitations list. **G3 is not a merge gate** — its absence is disclosed — but see (7).
7. **G3 placed.** Either G3 lands first (branch `claude/s2-g3-resume-before-exec` pushed, fifteen-green on its own run, merged into the integration branch before step 6) **or** the S3 packet carries "no real worker launch until G3 is merged with a fifteen-green run" as a hard precondition and the acceptance entry says so. Do not silently pick; state which and why.

**S3 dispatch is successful when** a packet `docs/briefs/handoffs/2026-09-20-full-e1-s3-n1-capture-g5-decision.md` exists on the branch (or on `main` if #436 has merged) that a fresh executor can run without this conversation, and it:

- Scopes exactly the ledger's S3 selected outcome: one fresh budgeted FULL_E1 campaign runs actual N1 once, archives a genuine capture, and commits G5's deterministic N1 assessment as the metered installed qg5 role; PASS → `N2_READY`, FAIL → terminal statistical prefix; no N2/Part A, no aggregate result, no seal; reduced-depth by construction (trace Q10) and says so.
- Uses `docs/notes/audits/2026-09-19-s3-preparation-trace-read-only.md` as its map and **rules or explicitly defers each of Q1–Q12** — at minimum rules Q1 (capture custody: new versioned campaign tables, `user_version` bump with exact layout validation, never lifting the cross-capability collision rule), Q2 (G5 launch topology and which cgroup its charge is read from; `campaign_host.cleanup` and `container_ownership.campaign_scopes` must learn the new unit kind), Q3 (worker output capture under the S2 container body), Q4 (versioned evidence/snapshot owner change, with the pinned v1 tests kept green), Q5 (handshake reuse: the worker entrypoint gets the same resume gate G3 defines — never a resume before exec), Q6 (release/profile revision without disturbing the v3/v5 budget/snapshot coupling), Q7 (`BudgetGuard` phase limits), Q8 (assessment signing lock ordering), Q9 (`derive_checkpoint_plan('N1')` byte-equals `derive_n1_plan` — assert it).
- Names its work ids and shows they avoid the A3 reserved prefixes.
- Lists files/interfaces per the ledger S3 section, the Windows and Linux verification lines (new `--s3` selector or S2-style registration — decide), the negative cases the ledger requires, the checkpoint (one genuine N1 capture/assessment trace, seed preservation, charged G5 trace, no-redraw recovery evidence), the return boundary, and the G3 precondition from (7).
- Is authored, not executed: no S3 code, no dispatch, no activation under this handoff.

## 2. Steps

1. **State read (no writes).** `git fetch origin`; confirm §0 or record deltas; read run 35517586779's result; read PR #436's review threads (`gh api graphql … reviewThreads`).
2. **Merge `main` in.** In the #436 worktree: `git merge --no-ff origin/main`; expect no conflicts (`invariant_manifest.json` union already resolved earlier; #437 touched none of the S2 files). Run the Windows selection lines and `./fp.ps1 check`; push. Capture the PR-path S2 run ID on the merge head and wait for it. Apply the (2) rule.
3. **A3 validator + A4 wording** on the branch (small commits; Windows tests; `git diff --stat` before each commit). If the coordinator prefers deferral for A3, record it instead — ask via checkpoint only if the validator would touch anything beyond `campaign_store.py` and its tests.
4. **Ledger acceptance entry** (append-only) per §1(1), including the (7) placement of G3 and the review dispositions. Update the PR body accordingly. Mark #436 ready (it already is) and request nothing new from Codex unless a code change landed in step 3 (then one review round on that diff only).
5. **Merge #436** per §1(6). Record the merge SHA in the ledger's next entry and in memory/notes as your return requires.
6. **S3 packet** per §1 "S3 dispatch". Commit it; if #436 has merged, put it on a short docs branch → PR to `main` (required check only), else on the integration branch before the merge.
7. **Return** (§7 below).

## 3. Forbidden

Rebase/squash/force-push of the integration branch; relaxing any Linux assertion or re-rolling a failed S2 run without the R-G3 signature rule; claiming Windows results as Linux evidence; touching `campaign_probe.py`, profiles, ceilings, release literals, `role_policy.TREE_BINDINGS`, `host.json`, gates or required checks; authoring S3 code; any activation, N1/N2/Part A dispatch, G5 verdict, result or seal; closing or re-opening issues; `git stash`; commits without `git diff --stat`.

## 4. Verification you must cite

Windows (PowerShell, `./fp.ps1`, doctor first): §2.6 line 1 (ten execution files, `--workers 2`, zero skips), line 2, line 3 (`tests/ops/qualification` + phase3), `check`; `git diff --check`. Linux: the head-bound S2 run (ID, record ID, counts, invariants, cleanup) for the merge head, plus the two accepted runs for `5ac3ad0`/`bbafe68`. For the A3 validator: the new Windows regression's record ID.

## 5. Checkpoints (report, do not wait unless stated)

Report before merging `main` if the merge is not clean; before step 5 if the head-bound run is not fifteen-green after the one permitted rerun; before authoring the S3 packet with your Q1/Q2/Q3/Q4/Q5 rulings in five lines each (this is the one checkpoint where the coordinator may want to intervene — proceed after reporting unless told to stop).

## 7. Executor return

_Pending._ Return `RESOLVED` / `DONE_WITH_CONCERNS` / `BLOCKED` with: branch + final head; merge SHA of #436; ledger entry heading; run IDs/record IDs/counts for every Linux run cited; Windows record IDs; the A3 disposition and A4 wording diff; G3 placement; the S3 packet path and its Q-rulings summary; limitations you disclosed; anything you could not verify.
