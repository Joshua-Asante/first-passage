# Q-INVENTORY-1 — fundable-inventory zero: replenishment disposition (bounded burst-2 vs accept-idle)

**Status:** `CLOSED — FALSIFIED 2026-07-17` — ratified, §8 frozen ([`pre-registration/Q-INVENTORY-1-verdict-preregistration.md`](pre-registration/Q-INVENTORY-1-verdict-preregistration.md)), and burst executed same session; **0 admissible seeds** → **accept-idle recorded as the default**; closure: [`closures/Q-INVENTORY-1-closure-falsified.md`](closures/Q-INVENTORY-1-closure-falsified.md); evidence: [`lab/archive/q_inventory_1_2026-07/`](../../lab/archive/q_inventory_1_2026-07/RESULTS.md)
**Authored:** 2026-07-17
**Closed:** 2026-07-17 (`FALSIFIED`)
**Authors:** Joshua + Claude Code
**Parent question:** discharges the standing "zero-survivor harvest disposition" carried in `docs/SESSIONS.md` Open/next since 2026-07-16; operates the mechanism-sourcing radar's ON-DEMAND cadence.
**Series:** strategy-R&D priorities 2026-07-17 — **rank 3 of 4**. Runs independently of ranks 1–2 (different axis: discovery vs deployment).
**Loop:** Inquire-phase Pre-Q — closure gates on a bounded sourcing pass with a pre-committed stop rule, zero pulls / zero K.
**Artifact path:** `docs/briefs/Q-INVENTORY-1-zero-survivor-replenishment-disposition.md`

---

## §0 — Rule 0 reads (production-source verification; all read 2026-07-17)

- `docs/methodology/strategy_harvest.md` — anchor `268851b` (2026-07-16). §1 five admission requirements (Req 5 cost-law inequality now binding at admission); §2.1 class tiers (Tier A fund-first / Tier B conditional / Tier C graveyard-watch, no size-based carve-out); §2.3 ranked channels (1 citation-graph from confirmed Tier-A axes · 2 survey/meta · 3 futures-native journals · 4 COT/TFF · 5 encyclopedia-index-only · 6 practitioner-probe-only); §2.4 cadence ON-DEMAND with frozen Q1–Q6 query families inherited by reference, never edited; dedup-first against `docs/rejected_candidates.md` + closed manifests + `docs/methodology/rejected_signals.md`. Req-3 K-banks quoted: GC/MGC 3,177 · ES 2 · MNQ 1 · others 0 — **re-read the manifests at execution; do not trust this snapshot** (the doc's own instruction).
- `STATE.md` — anchor `e5eca1a` (2026-07-17). Harvest-intake §4 doctrine falsifier count **0-of-2** (neither D5 nor H-OD-1 counts — gates unreachable at cohort magnitude); HARV lane §4 falsifier FIRED instead → same-units attestation ADR `Accepted` 2026-07-16; **idle guard: zero screen-PASS seeds beyond D5 by 2026-11-08 ⇒ name the intake idle at the 11-08 review**; radar's own falsifier: next two Tier-A seeds reaching Stage-2 both dying on cost-law despite passing §2.2 falsifies the class-priority aiming.
- Radar burst-1 artifacts (`lab/analysis/harvest/radar_tier_a_burst_2026-07/`, content-read via STATE pointers): `H-TSMOM-6J` CLOSED Clause-N FAIL (N≈86, power≈0.26); carry stubs — Koijen Table-1 moments recovered (Japan/Euro/WTI) but **timing Req-2 still UNSCREENABLE**; month-end Tier-B: no successor (Q-HARV-1 §R already DECLINED).
- Kill ledger since intake ratified (2026-07-15 → now): D5 Stage-2 cost-law KILL (11.06bp hurdle vs ≈2.97bp cohort); H-OD-1 Stage-2 cost-law KILL (5.05bp vs 1.5bp, mechanism itself CONFIRMED in-sample); H-TSMOM-1 Clause-N FAIL at scoping (N≈86, power 0.34, Default-#1-pinned); H-TSMOM-6J Clause-N FAIL. **Fundable inventory: 0.**
- `docs/briefs/pre-registration/Q-KBUDGET-1-screen-preregistration.md` (freeze `b304f2c`, by reference): Clause K (floor ≤ 1.0 ⇔ K_eff ≤ 3) + Clause N (power ≥ 0.50) constants inherited, never re-derived here.
- A4 price-only footprint diagnostic (Q-HARV-0 residue): Cursor handoff staged, **not dispatched**; its PR-#368 workspace pin is stale — any dispatch branches off `origin/main` (STATE).

---

## §1 — Context & motivation

The validation machine is complete and hardened (screen → HARV reachability HARD gate → register_search/K → cost-gated pull → Stage 2–8 → survivor gate), and it has now processed the entire ratified inventory to zero: every seed since the intake ratified has died at screen (power) or Stage-2 (cost-law) — while, notably, the *mechanisms themselves* kept confirming (H-OD-1 +1.444bp t≈5.0). The deployment axis (Q-RAIL-1) does not depend on new seeds, but the discovery program's 11-08 idle guard is live and dated, and the radar cadence is ON-DEMAND — meaning nothing fires it except a decision like this one. The open question is a disposition, not a search: is another bounded sourcing pass positive-EV against the now-measured kill pattern, or is accept-idle (the guard firing as designed) the honest default?

---

## §2 — Prior art / lineage

- **Harvest-intake ADR** (`2026-07-15-external-mechanism-harvest-intake.md`, `Accepted`) + amending same-units ADR (`2026-07-16-…-supersession.md`, `Accepted`) — the lane stands; its falsifier bookkeeping is NOT this brief's to adjudicate (0-of-2 count unchanged by anything here).
- **Q-KBUDGET-HARVEST-1** (`CLOSED-RESOLVED` 2026-07-16) — the burst-shape precedent: bounded literature sweep, zero pulls / zero K, staging-only. This brief re-runs that *shape* on the surviving band with the two newer kill lessons (Req-5 arithmetic at sourcing time; Default-#1 OOS N for power).
- **Radar design spec** (`docs/superpowers/specs/2026-07-16-mechanism-sourcing-strategy-design.md`) — §2.3 channel ranks + burst-1 execution record.
- **Rejected registries** — `docs/rejected_candidates.md`, `docs/methodology/rejected_signals.md`, closed manifests in `discovery_manifests/`: the dedup-first wall. A screened-dead **class** stays dead under a new citation.
- **Tail-methodology-exhaustion (INQHIORI §6 guardrail):** relevant and consciously NOT fired — D5/H-OD-1/H-TSMOM-1 are different mechanisms under different clauses, not three H's on one parent question. But the guardrail's spirit sets this brief's stop rule: one more bounded pass, and a 0-yield outcome is *accepted*, not iterated.

---

## §3 — Question (Q-INVENTORY-1)

**Pre-Q gate test:** symptom-only rephrase — "the fundable inventory is zero, the idle guard is dated, and sourcing has a measured kill pattern." Passes: accept-idle is a first-class answer, so no fix is baked in.

**Q-INVENTORY-1:** Is the admissible band that survives the measured kill pattern — daily-or-intraday event frequency (power-wall), per-event δ clearing the Req-5 cost inequality at the panel basis (cost-wall), family K-bank ≤ 2 (Req-3) — non-empty in the ranked channels, at the cost of one bounded pass?

---

## §4 — Falsifiable hypothesis (H-INVENTORY-1)

**H-INVENTORY-1:** One bounded burst (scope frozen in §7: rank-1 citation traversal seeded from D5's paper + rank-2/rank-3 passes under the inherited Q1–Q6 families; ≤ 1 session; staging only) stages **≥ 1 seed** whose §1 requirements 1–5 all pass at sniff-arithmetic level (manifest block fully populated, screen-ready).

**Reject H-INVENTORY-1 if:** the burst completes its frozen scope with **0** admissible seeds → `FALSIFIED` and **accept-idle becomes the recorded default** (the 11-08 guard then fires as designed; that is the ADR's own success-eligible outcome, not a failure of this brief).
**Accept H-INVENTORY-1 if:** ≥ 1 seed stages with all five requirements passing and the §5 manifest block complete → inventory replenishes; seed(s) route to operator ratification then the standing screen.
**Ambiguous-hold if:** every staged candidate is `UNSCREENABLE` (missing input with a named recovery route — e.g., the carry timing-δ) and none is admissible outright → the disposition escalates to the operator as a **probe-funding fork** (below), not a silent re-run.

**Carry timing-δ sub-limb (pre-declared, because burst-1 already hit it):** if carry re-surfaces as the best candidate and Req-2 remains UNSCREENABLE, this brief does NOT fund the δ-extraction probe — it prices it (data spend + K cost per `strategy_harvest.md` §2.3 rank-6 note / Req-2 relief valve) and hands the GO/NO-GO to the operator as a named fork in the closure. Probe funding is a spend decision, out of scope here.

---

## §5 — Forbidden moves (genuinely tempting)

- **Re-screening a dead class under a fresh citation** (month-end via a different paper; any GC/MGC design at bank 3,177) — dedup-first rule; Req-3 is the one permanent kill.
- **Inventing or transplanting a δ** to make a promising mechanism screenable (SPX→NQ-style transplant refused precedent) — UNSCREENABLE routes to a probe decision, never to a filled-in number.
- **Relaxing the Default-#1 OOS reading** (statistical OOS starts 2019-05-06) to rescue a monthly-frequency power FAIL — operator-pinned 2026-07-16; H-TSMOM-1 died on exactly this and stays dead.
- **Opening a manifest / `register_search` / any pull inside the burst** — this pass is staging-only, zero K, zero spend (Q-KBUDGET-HARVEST-1 shape). Screening PASS ≠ blessing (asymmetry clause).
- **Extending the burst "one more channel" after a 0-yield frozen scope** — the stop rule IS the brief; iteration re-opens as a fresh Pre-Q with new evidence, or not at all.
- **Editing the frozen Q1–Q6 query families** — inherited by reference; a pass wanting new families opens its own pre-registration (`strategy_harvest.md` §2.4).

---

## §6 — Gate criteria (closure verdict)

| Verdict | Trigger condition | Disposition |
|---|---|---|
| `RESOLVED` (inventory replenished) | ≥ 1 staged seed passes §1 Req 1–5 at sniff level, manifest block complete | Seed(s) → operator ratification → standing screen (`floor_scan.py` pattern). Idle guard becomes moot if any later reaches screen-PASS |
| `FALSIFIED` (band empty at this cost) | Frozen scope completes with 0 admissible seeds | **Accept-idle recorded as the default**: the 11-08 idle guard fires as designed; discovery effort stays parked pending new external evidence; the deployment axis (Q-RAIL-1) is unaffected. Zero-survivor disposition item on the SESSIONS board is DISCHARGED either way |
| `AMBIGUOUS-HOLD` | Only UNSCREENABLE candidates staged | Probe-funding fork to operator (carry timing-δ priced, not funded); re-check at the 08-08 packet |

---

## §7 — Execution plan (frozen burst scope)

- **Phase 0 — dedup + bank re-read (≤30 min).** Re-read `discovery_manifests/` K-banks (do not trust §0's snapshot); re-read the three rejected registries. Emit the dead-class list the burst must not re-stage.
- **Phase 1 — rank-1 pass.** Scoped citation-graph traversal from Baltussen et al. 2021 (D5's source — the only screen-PASS lineage): influential-citation filter + futures-cohort keyword requirement (the spec's tractability scoping). Stage candidate rows only.
- **Phase 2 — rank-2/3 pass.** Survey/replication meta-studies (Path-1b feeder) + futures-native journals (JFM/JBF/JFQA), under the inherited Q1–Q6 families verbatim.
- **Phase 3 — sniff arithmetic per staged row.** Req-5 inequality at the would-be panel basis (one division each, commissions explicit); Req-4 power at the Default-#1-compliant N; Req-3 bank check; Req-1 path (1a mechanism or 1b four-part evidence). Kill rows inline with the failing requirement named.
- **Phase 4 — verdict + closure.** Fire §6; write the manifest block(s) (`strategy_harvest.md` §5 template) for survivors; record the carry fork if it re-surfaced; DISCHARGE the SESSIONS zero-survivor line.

Stop rule: Phases 1–3 fit one session. Whatever is staged when the scope completes is the answer.

---

## §8 — Verdict pre-registration

Freeze `docs/briefs/pre-registration/Q-INVENTORY-1-verdict-preregistration.md` (§4 + §6 verbatim, plus the Phase-1 traversal scoping constants) at operator ratification, before Phase 1. Constants inherited from the Q-KBUDGET-1 screen freeze `b304f2c` are cited by reference, never restated with different values.

Pre-registration commit hash: `<populated at freeze>` · Date: `<at freeze>`

---

## §9 — Closure record format

`closures/Q-INVENTORY-1-closure-<verdict>.md`: staged-row table (channel, requirement scorecard, kill reasons), the accept-idle record on FALSIFIED, the priced carry fork if applicable. Lesson candidate only if a kill reason falls *outside* the measured pattern (cost-wall / power-wall / K-bank) — a novel kill mode is worth a registry entry; a repeat is not.

---

## §10 — Audit hooks (runnable)

```bash
# Idle-guard + falsifier bookkeeping untouched by this brief (counts unchanged)
grep -n "0-of-2" STATE.md
grep -n "zero screen-PASS seeds beyond D5" STATE.md

# Staging-only discipline: no manifest opened, no pull, during this brief's window
ls discovery_manifests/ | wc -l    # compare before/after burst; delta must be 0 until operator ratifies a seed
grep -rn "register_search open" docs/SESSIONS.md | tail -3   # none dated inside the burst window

# Q1–Q6 families untouched
git log -1 --format='%h %cs' -- docs/briefs/pre-registration/Q-KBUDGET-1-screen-preregistration.md

# The executing session cites this brief (Trap #10)
grep -rn "Q-INVENTORY-1" docs/SESSIONS.md
```

---

## Verification

```bash
python scripts/check_brief.py docs/briefs/Q-INVENTORY-1-zero-survivor-replenishment-disposition.md --type inquire
git log -1 --format='%h %cs' -- docs/methodology/strategy_harvest.md STATE.md
```

## Ratification record

Ratified 2026-07-17 (operator chat directive: "proceed with Q-RAIL 1 Phase 4 and Q-INVENTORY 1" — the brief's sole pending gate; §8 carries no operator-set numbers). §8 pre-registration frozen before Phase 1 the same session. Phase 0 dedup + bank re-read: [`lab/archive/q_inventory_1_2026-07/PHASE0.md`](../../lab/archive/q_inventory_1_2026-07/PHASE0.md).
