# SPEC: first-passage → first-passage-archive migration (Levels 1–3, full pattern completion)
Status: PROPOSED · 2026-09-01 · authorizes nothing ($0 · K=0) · depends: operator un-archives `first-passage-archive` on GitHub
Objective: relocate ~1,507 closed-research / spent-process files (the full 3-level set from the 2026-09-01 hygiene survey) from the public `first-passage` tree into the private `first-passage-archive` repo, preserving retrievability and every live citation.

Steps:
1. Operator un-archives `first-passage-archive` (currently GitHub-archived/read-only) and confirms push access; the `archive` remote is already configured locally.
2. Land migrated content as a new branch `from-first-passage/hygiene-2026-09` in `first-passage-archive`, mirroring source-repo relative paths 1:1 — never merged into archive's own `main` (histories share no common ancestor; confirmed `git merge-base` empty against `pre-prune-2026-08-08`).
3. Per migration cluster, extract history via `git filter-repo --path <dir>` against a disposable local clone (never the live worktree) — verified: `lab/archive/` (618 files, 4,254 reachable commits) extracted in 7.9s. `git subtree split` is not viable here — timed out 3× (up to 3min) even on a 4-file dir. Windows note: clone destination needs `core.longpaths=true` + a short path, or checkout fails on deep archive paths (`Filename too long`, reproduced and fixed this session).
4. Before any Level-3 link rewrite, enumerate the exact citing files + lines for the 56 previously-uncited-looking `lab/archive/` campaigns actually cited from `ops/instruments/*.md` and `docs/rejected_candidates.md` (~150 files, estimated not enumerated by the 2026-09-01 survey) and fix each pointer to the new cross-repo location (format TBD at execution — proposed: mirror the existing CARD.md `Body:` pointer style).
5. Stage as sequential PRs, one per cluster, ordered by risk: Level 1 (zero-citation subset, ~213 files) first to prove the pipeline end-to-end → Level 2 clusters (~687 files: lab/archive uncited subset, legacy/, harvest/inbox closed clusters, closures/, handoffs/, notes/audits/, spec/ closed retirements, etc.) → Level 3 (pre-registration pairs → notice/ → the 56 cited `lab/archive/` campaigns with link rewrites landing in the *same* PR → `notion-redirect-map.md`). Each PR pushes to the archive branch before deleting from `first-passage`, and runs `check_adr_graph.py` / the relevant `pytest` subset / `lab-catalog` gate / a dead-link grep before opening.
6. Tag `first-passage`'s HEAD before each deletion PR (`pre-hygiene-migration-<date>`, matching the Great Prune's `pre-prune-2026-08-08` convention) as a local fallback; independently verify the archive branch's content before the corresponding deletion lands.

Gate: RESOLVED if all 3 levels land, the archive branch's content verifies byte-identical to what was deleted at push time, and every PR in the sequence passes its gate battery with zero HARD findings; FALSIFIED if `first-passage-archive` cannot be un-archived, or the filter-repo/longpaths workaround fails against a real (non-disposable) cluster extraction.
Boundary: no `core/`, `dd_protection`/`firm_rules`, or Pine touched; no move of `docs/pursuits/`, `docs/methodology/` (canon), `lab/analysis/{orb,regime,aegis,mc,striker}/`, or any file cited from an OPEN brief / live instrument ledger / M1-adjacent safety artifact (per the 2026-09-01 survey's exclusion list); `first-passage`'s visibility/branch-protection settings untouched; `first-passage-archive`'s existing branches and `main` untouched (new branch only, never a force-push).
Reads: [`docs/adr/2026-08-08-great-prune.md`](../adr/2026-08-08-great-prune.md) (tag-and-delete precedent) · [`docs/ltm/README.md`](../ltm/README.md) (retrieval convention) · [`docs/operational_rules.md`](../operational_rules.md) §16 (retention test this survey applied) · `git filter-repo` feasibility test this session (`lab/archive/`, 4,254 commits parsed, 7.9s total) · `git subtree split` infeasibility test this session (3 timeouts, up to 3min, smallest on a 4-file dir) · `git merge-base <HEAD> pre-prune-2026-08-08` (empty — confirmed unrelated histories) · `gh api repos/Joshua-Asante/first-passage-archive` (confirmed private, currently archived)
Owner: new — first-passage-archive hygiene-migration survey, this session (2026-09-01)
