# SPEC: first-passage → first-passage-archive migration (code-level verified scope)
Status: PROPOSED · 2026-09-01 · authorizes nothing ($0 · K=0) · depends: operator un-archives `first-passage-archive` on GitHub
Objective: relocate the 233 code-and-doc-citation-clean files enumerated in [`the manifest`](2026-09-01-first-passage-archive-migration-manifest.md) (v2, code-level verified) from the public `first-passage` tree into the private `first-passage-archive` repo, preserving retrievability and every live citation.

**Scope correction (2026-09-01):** the original "Levels 1-3, ~1,507 files" framing is superseded. Codex's PR #252 review caught two doc-only-survey misses with live code dependencies; a 21-agent re-verification against docs + code (imports/`sys.path`/dynamic loads/tests) + skills + `lab/CATALOG.md` status found only 233 of 650 checked items (36%) actually safe. Two entire clusters originally believed closed (`lab/analysis/c1/`, `lab/analysis/harvest/`) turned out to be live research by `CATALOG` status; three more (`docs/briefs/closures/`, `docs/briefs/rnd-pipeline/`, `docs/notes/notice/`) are structurally unsafe because live tooling reads them wholesale. See the manifest's "Entirely excluded clusters" table for why, and its per-cluster SAFE lists for the current scope.

Steps:
1. Operator un-archives `first-passage-archive` (currently GitHub-archived/read-only) and confirms push access; the `archive` remote is already configured locally.
2. Create the persistent migration base **once**, before any cluster work: push an initial commit (README stub is enough) to a new branch `from-first-passage/hygiene-2026-09` in `first-passage-archive` via a direct `git push archive HEAD:refs/heads/from-first-passage/hygiene-2026-09` — no PR, since a brand-new branch has no base to review against. This branch is never merged into archive's own `main` (histories share no common ancestor; confirmed `git merge-base` empty against `pre-prune-2026-08-08`) and becomes the **standing base** every subsequent cluster PR targets with `--base from-first-passage/hygiene-2026-09` (short-lived feature branch → PR → merge into that base, inside the archive repo).
3. **Precondition for the `lab/archive/` cluster specifically:** `scripts/archive_lab_analysis.py:618-630` only emits a catalog row when both the local `CARD.md` stub and the local `lab/archive/<slug>/` directory exist (confirmed by reading the source) — deleting the directory silently drops the row even though the stub still points readers at it. Before the first `lab/archive/` deletion PR, extend that script to recognize an external-body state (e.g. a `Body: first-passage-archive:<path>` marker in the stub, in place of requiring local directory presence) and add a test asserting the catalog still lists an externally-archived campaign. `lab-catalog` must stay green for the right reason, not because the row silently vanished.
4. Per migration cluster, extract history via `git filter-repo --path <dir>` against a disposable local clone (never the live worktree) — verified: `lab/archive/` (618 files, 4,254 reachable commits) extracted in 7.9s. `git subtree split` is not viable here — timed out 3× (up to 3min) even on a 4-file dir. Windows note: clone destination needs `core.longpaths=true` + a short path, or checkout fails on deep archive paths (`Filename too long`, reproduced and fixed this session). **`filter-repo` does not follow renames** — a campaign that moved into its current location (confirmed pattern: `msl_c3_m2k_2026-08` moved `lab/analysis/c1/msl_c3_m2k_2026-08/` → `lab/archive/msl_c3_m2k_2026-08/` at archival time) needs its pre-move path passed too (`--path <old> --path <new>`, or `--path-rename old:new`). The manifest's `lab/archive/` section records recovered pre-rename paths for the SAFE slugs where evidence exists; for the rest, `git log --follow` is uninformative repo-wide (history starts at the single 2026-08-14 public-release squash commit) — treat as flat-origin unless a citation says otherwise.
5. Stage as sequential PRs, one per cluster (see [the manifest](2026-09-01-first-passage-archive-migration-manifest.md) for the explicit SAFE list, exclusion counts, and reasoning per cluster), ordered by how clean each cluster is: `docs/briefs/*.md` root (60/64) and `docs/briefs/pre-registration/` (45/88) first, then `lab/archive/` (49/90), `docs/notes/audits/`+`research/` (33/66), `docs/superpowers/plans/` (14/24), `docs/spec/` (13/21), `docs/briefs/handoffs/`+`docs/historical/` (16/40), then the small remainders (`lab/analysis/_inbox/`, `docs/notes/notice/`'s single file). Do **not** touch the seven entirely-excluded clusters (`lab/analysis/c1/`, `lab/analysis/legacy/`, `lab/analysis/harvest/`, `docs/briefs/closures/`, `docs/briefs/rnd-pipeline/`, `docs/notes/notice/` as a batch, `docs/analytics/`+`lessons/`+`methodology/archive/`+`external/`) without a fresh, separately-scoped decision — the manifest's exclusion is structural, not a per-file judgment call that individual re-triage can overturn. Each cluster PR: (a) merge its content into `from-first-passage/hygiene-2026-09` in the archive repo first (step 2's base), (b) then open a *separate* PR against `first-passage`'s `main` deleting the source, gated on `check_adr_graph.py` / the relevant `pytest` subset / `lab-catalog` / a dead-link grep.
6. Before each `first-passage` deletion PR, tag HEAD as `pre-hygiene-migration-<date>-<cluster-slug>` (cluster slug required — same-date tags collide otherwise) as a local fallback; independently verify the archive branch's content before the corresponding deletion lands.
7. The 38 `NEEDS_OPERATOR_JUDGMENT` items across all clusters (see manifest, per-cluster exclusion notes) are not in scope for this pass — they need an explicit operator ruling per item, not a batch decision either way.

Gate: RESOLVED if the 233-item verified scope lands, the archive branch's content verifies byte-identical to what was deleted at push time, `lab-catalog` stays green for a real reason (not a silently-dropped row), and every PR in the sequence passes its gate battery with zero HARD findings; FALSIFIED if `first-passage-archive` cannot be un-archived, or the filter-repo/longpaths/rename workaround fails against a real (non-disposable) cluster extraction.
Boundary: no `core/`, `dd_protection`/`firm_rules`, or Pine touched; no move of anything in the manifest's "Entirely excluded clusters" table or any item marked `NEEDS_OPERATOR_JUDGMENT`; `first-passage`'s visibility/branch-protection settings untouched; `first-passage-archive`'s existing branches and `main` untouched (new branch only, never a force-push).
Reads: [`the manifest`](2026-09-01-first-passage-archive-migration-manifest.md) (v2, code-level verified — supersedes the doc-only v1; committed 2026-09-01 after a 21-agent re-verification, Workflow `wf_ce66f55a-f3a`) · [`docs/adr/2026-08-08-great-prune.md`](../adr/2026-08-08-great-prune.md) (tag-and-delete precedent) · [`docs/ltm/README.md`](../ltm/README.md) (retrieval convention) · [`docs/operational_rules.md`](../operational_rules.md) §16 (retention test this survey applied) · `scripts/archive_lab_analysis.py:618-630` (catalog-row precondition) · `scripts/check_closure_disposition.py:189-334` (the 3 hardcoded frozensets that exclude `docs/briefs/closures/`) · `scripts/check_advisor_dedup.py:213-218` (the glob that excludes `docs/briefs/rnd-pipeline/`) · `git filter-repo` feasibility test (`lab/archive/`, 4,254 commits parsed, 7.9s total) · `git subtree split` infeasibility test (3 timeouts, up to 3min, smallest on a 4-file dir) · `git merge-base <HEAD> pre-prune-2026-08-08` (empty — confirmed unrelated histories) · `gh api repos/Joshua-Asante/first-passage-archive` (confirmed private, currently archived)
Owner: new — first-passage-archive hygiene-migration survey, this session (2026-09-01)
