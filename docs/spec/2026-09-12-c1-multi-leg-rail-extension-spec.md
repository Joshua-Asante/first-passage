# SPEC: c1 multi-leg signal daemon and rail extension for the Tradeify portfolio (TB-S3)

Status: PROPOSED · 2026-09-12 (rev 3: second Codex round on #360 folded — exit `qty` in the addendum, unknown positions closed at EOD, Python-port manifest lines for the image gate, risk-reducing actions survive barrier timeouts and late arrivals, OCA moot by construction, atomic checkpoint + execution cursor; rev 2: order-shape contract R-B3 and live gate L-2, kill-switch ordering, working orders at EOD, Vanguard zero at WATCH tiers, sell-to-close allowed) · authorizes nothing ($0 · K=0; no deploy, no arm, no emit) · depends: umbrella D-B1, D-B5, D-B7, D-B8, D-B9 (RULED 2026-09-11); campaign record §56 O-1, O-3 (this spec), O-5, O-6, O-8 (RULED 2026-09-12) · needs **operator ratification** (live-risk semantics) and one **S2b build ADR addendum** (§R-B1 below) before TB-I3
Objective: extend the single-strategy daemon and the two-leg long-only listener into the fixed four-leg book's signal host and rail — registry, bar-time barrier, per-leg sides, integer quantities under the ruled laws, account-wide capacity with the D-B8 takeover, prior-close protection mode seeded from the B7 snapshot, EOD scheduler, kill switch, warm restart, execution feedback, private-port packaging and the policy interlock — split into what TB-I3 builds offline now and what waits for a live source.

Owners of the rules: [acceptance record](../notes/2026-09-10-tradeify-protection-selection.md) · umbrella TB-S3 packet and §0.8 · [TB-R2 read](../notes/2026-09-12-track-b-scaling-faithfulness-read.md) · [TB-S2 replay spec](2026-09-12-tradeify-synchronized-replay-spec.md) (replay semantics = rail semantics) · [S2b build ADR](../adr/2026-08-08-s2b-signal-daemon-build.md) · [M1 ADR](../adr/2026-07-22-c1-venue-native-monitoring-maturity.md) · [PREREG-C1-DEDUPE-1](PREREG-C1-DEDUPE-1-intent-key-functional-property.md).

## Reads (anchors at `origin/main @ c41e2be`, 2026-09-12)

`ops/c1_signal_daemon/{daemon,evaluate_loop,strategy_protocol,feed,b1_payload,listener_client,heartbeat,http_status,m1_stage1*,operator_input_source}.py` (one `BarSource`, one `Strategy`, `Signal(leg_id, signal_type, close, stop_dist_pts, bar_time)`, `NullStrategy`, emit disabled; `evaluate_loop._step` returns on `None` so silence is not completion) · `ops/c1_signal_daemon/{book_protocol,book_adapters,tv_broker_emulator,book_parity}.py` @ `8c9d002`/`027895e`/`bda7a48` (delivered adapter contract: sided `OrderIntent`, `Bracket`, `ExecutionEvent`, `set_mode`, `checkpoint`) · `ops/c1_rail/book_policy.py` @ `bda7a48` (`LegSpec`, `CapacityLedger`, `Takeover`, `BookProtectionClock`, `leg_quantities`) · `ops/c1_rail/c1_rail_listener.py` @ `509b524` (`INSTRUMENT_SYMBOLS` two rows + the M1 test row, provisional; `_leg_action` hard-codes `buy`; `handle_signal` gates: test identity, `risk_add_blocked`, `arming_expiry_reason`, pre-send decision persistence, `dry_run` never calls the sender) · `ops/c1_rail/c1_sizing_host_reference.py` @ `509b524` (`LEG_MAP`, `_read_lifecycle_multiplier` halts on a missing key, `_read_dd_scale` = `calculate_protection` on **current** equity with a ratcheted peak, `qty = floor(E_firm·r_eff/per)`, `add = floor(executed_base × pyr%)`, `reserve_cap`) · `ops/c1_rail/c1_rail_http_server.py` (`load_config` implicit-disarm boot, `ratchet_peak_equity`, `resolve_current_equity`, `make_handler`) · `ops/c1_rail/crosstrade_payload.py` (`flatten_first=true` on entry; `SendResult` accepted ≠ fill) · `ops/c1_rail/c1_rail_telemetry.py` (`EVENT_KINDS`, `EventLedger`, `ExecutionStateStore`) · `ops/c1_rail/c1_rail_arm.py` (`validate(require_resolved=True)` interlock; `--arm` operator-only) · `ops/c1_rail/m1_stage1_contract.py` (the fixed-one-micro precedent and its binary64 hazard) · `deploy/c1_signal_daemon/{Dockerfile,README.md}`, root `.dockerignore`, `tests/ops/test_c1_signal_daemon_image_manifest.py` (COPY allow-list guard) · `core/firm_rules.py` lines 255–340 (80-micro aggregate cap; Equity-Index product-group hedging bar; 16:45 ET) · S2b build ADR §2 (B1 contract unchanged; a field change without a superseding ADR is its falsifier limb 3) · PREREG-C1-DEDUPE-1 §R1–R6, §6 · M1 ADR §4 items 1–9 · `M1_MONITORING_ACCEPTANCE.json` (status `CODE_LANDED`; fixture-pin skew and the private `RUNBOOK.md` are Track A / TB-O1 items, not recreated here) · [Striker readmission ADR](../adr/2026-09-05-tradeify-select-striker-expression-readmission.md) same-day amendment.

## Requirements (each: owner file · offline / live tag · named test)

| Id | Requirement | Owner files | Tag | Named test |
|---|---|---|---|---|
| R-A1 | **Registry.** One daemon process; `book_registry.py` holds exactly the four adapters keyed by `leg_id` (`aegis_6j`, `vanguard_mgc`, `dj30_mym_p250`, `orb_mnq_v7`), loaded through `book_adapters.load_port`; the active-leg set and its digest (TB-V1) are read at boot and any mismatch halts the daemon before the first poll. | `ops/c1_signal_daemon/book_registry.py`, `daemon.py` | offline | `test_registry_refuses_unknown_or_missing_leg_and_digest_mismatch` |
| R-A2 | **Sources.** One `BarSource` per order symbol (6J, MGC, MYM, MNQ; 15-minute); the provider-neutral contract is Track A's A9-PREP deliverable; this spec consumes it and adds nothing provider-specific. | `daemon.py` | live (source) / offline (mock) | `test_four_sources_one_per_symbol` |
| R-A3 | **Bar-time barrier.** Every leg reports an explicit **completion event** for every bar timestamp (signal, `none`, or `missing`); the book-wide barrier releases a timestamp only when all four have reported, then admits actions in the frozen global order **D-B8 priority → entry before add → `leg_id` lexical** (identical to TB-S2 RC-5); on a fail-closed timeout (frozen: one bar period + 30 s) it releases every **risk-reducing** action — exits, flats, cancels of resting entries/adds, and stop-tightening amendments — refuses every risk-add and every loosening amendment for that bar, and raises `barrier_timeout` telemetry; a **late arrival for a closed bar** is admitted if it is risk-reducing (an exit or cancel from the leg that missed the barrier still reaches the broker) and refused if it is a risk-add. | `ops/c1_signal_daemon/barrier.py`, `evaluate_loop.py`, `c1_rail_listener.py` | offline | `test_barrier_releases_only_when_all_four_report`, `test_barrier_timeout_releases_exits_and_cancels_refuses_adds`, `test_late_exit_admitted_late_add_refused` |
| R-B1 | **Signal contract (rev 2).** `Signal` gains `side` (`buy`/`sell`) and optional `qty_normal` (integer; the adapter's normal quantity for `adapter_quantity` legs, else absent); the B1 payload carries them as **optional** fields alongside the R-B3 order-shape fields; the five-field payload stays valid for the M1 test identity. **This is a B1 contract change: TB-S3 acceptance includes a dated S2b build ADR addendum authorizing every new field and signal type of R-B1 and R-B3** (S2b falsifier limb 3). The payload builder refuses a `sell` **to open** (kind `entry`/`add`) for an Equity-Index leg at build time; a `sell` that closes a long (`exit`/`flat`) is valid and tested. | `strategy_protocol.py`, `b1_payload.py`, S2b ADR addendum | offline | `test_b1_payload_optional_fields_and_five_field_compat`, `test_payload_refuses_equity_index_sell_to_open`, `test_index_leg_exit_sell_allowed` |
| R-B3 | **Order-shape contract (rev 2, Codex finding on #360).** The adapters emit more than market orders: ORB's buy-stop entry, per-fill brackets (stop / limit / trailing), bracket amendments, cancels, OCA siblings and quantity-scoped exits; the outbound builder today emits `order_type=market` only. The full adapter → listener → broker mapping is: (a) market entry/add → market order at receipt (the `THIS_CLOSE` / `NEXT_OPEN` distinction becomes "at receipt of the closed bar's signal", a recorded deviation from the emulator measured in Phase 8 execution parity); (b) **stop entry** (ORB) → a resting broker stop order **if** CrossTrade's Tradovate route accepts a stop order type (**live gate L-2**: capability verified by documentation or an authorized transmitted validation, never assumed); otherwise the daemon holds the resting stop and emits a market entry on the first closed bar whose high crosses the level (deviation: fill at that bar's receipt rather than at the level; recorded and carried as a stress cell); (c) **brackets** → `stop_loss` / `take_profit` per entry fill through the builder's existing fields, with the trailing stop managed daemon-side and expressed as `amend` signals (cancel/replace of the protective stop), which also needs the L-2 capability read; (d) **cancels** → `cancel` signals keyed by `order_id`; **OCA is moot for the fixed book by construction**: under D-B7 the buy-only ORB port emits exactly one resting stop entry per day and never a short sibling, so no two OCA siblings are ever live at the broker (the 2026-07-21 same-bar-reversal class recorded in `PORT_MANIFEST.sha256` arose from two live siblings); a test asserts that the daemon never has two resting entries for one leg, and daemon-side OCA is **not** implemented; (e) **scoped exits** → `exit` with `qty` (R-D). New B1 signal types `cancel` and `amend`, and fields `qty` (on `exit`/`flat`, and the scaled quantity on `entry`/`add` for `adapter_quantity` legs), `stop_price`, `stop_loss`, `take_profit`, `order_ref`, are part of the R-B1 addendum; without `qty` the listener cannot distinguish a partial from a full exit. Each mapping has an end-to-end test (adapter action → payload → listener decision → simulated broker outcome → `ExecutionEvent` back to the adapter); the live packet cannot open until L-2 is recorded. | `strategy_protocol.py`, `b1_payload.py`, `c1_rail_listener.py`, `crosstrade_payload.py`, `book_registry.py` | offline (mapping + tests) / **live (L-2)** | `test_orb_stop_entry_mapping_both_branches`, `test_bracket_fields_per_fill`, `test_trailing_amend_cancel_replace`, `test_cancel_by_order_id`, `test_orb_never_two_resting_entries`, `test_scoped_exit_end_to_end_with_qty` |
| R-B2 | **Leg ids and symbols.** `LEG_MAP` gains the four rows at `cap_alloc = 0`; the retired `dj30_mym` / `nas100_mnq` rows stay at zero and are never reused; `INSTRUMENT_SYMBOLS` gains `6J1!` and `MGC1!` marked **provisional**; a **per-symbol verification gate** (operator-attested CrossTrade ticket format, or a separately authorized transmitted validation — never a dry-run receipt) must be recorded for each of the four symbols before TB-B10, and `cap_alloc` stays 0 for an unverified symbol. | `c1_sizing_host_reference.py`, `c1_rail_listener.py`, TB-V1 | offline (rows) / live (verification) | `test_new_rows_at_zero_and_retired_rows_untouched`, `test_unverified_symbol_keeps_cap_zero` |
| R-C | **Direction.** Every `LEG_MAP` row carries `allowed_entry_side` (Aegis `sell`; Vanguard, Striker MYM, ORB `buy`); `_leg_action` derives the action from the payload side and the row; the listener halts an entry/add whose side differs (telemetry `side_refused`); the Equity-Index product-group bar is an **independent second check**: a MYM/MNQ leg can never reach a `sell` action to open even if its row is misconfigured. | `c1_rail_listener.py`, `c1_sizing_host_reference.py` | offline | `test_aegis_cannot_buy_to_open`, `test_index_legs_cannot_sell_to_open`, `test_hedging_bar_independent_of_row` |
| R-D | **Exits.** `exit` carries a quantity that reduces only the emitting leg's confirmed allocation; a quantity-less `closeposition` is reserved for the coordinated account flatten (R-F, R-G). Under D-B9 (one owner per symbol) `flatten_first=true` on entry is harmless and **retained**; its removal for a shared symbol is specified as the multi-owner branch, **NOT BUILT**. | `crosstrade_payload.py`, `c1_rail_listener.py` | offline | `test_exit_reduces_own_allocation_only` |
| R-E | **Capacity controller.** The listener owns a durable `CapacityLedger` (confirmed positions + reservations, micro-equivalents 6J = 10, others 1, cap 80) persisted beside `ExecutionStateStore`; refuse-never-clip; the D-B8 takeover is `book_policy.Takeover` executed against the broker: cancel displaced pending → broker cancel acknowledgement → `closeposition` per displaced symbol → **broker-confirmed zero position** (never an HTTP `accepted`) → reconcile → admit Aegis; any unacknowledged cancel or partial/rejected/unknown/timed-out close refuses Aegis; each step is a telemetry event (`capacity_*`). Under O-5/O-6 the protected Striker ceiling is 77 micro-equivalents, so the protected-mode takeover is a **reachable** path and is tested. | `c1_rail_listener.py`, `book_policy.py` (reused), `c1_rail_telemetry.py` | offline | `test_takeover_cancel_fill_race_refuses`, `test_takeover_partial_close_refuses`, `test_protected_striker_77_takeover_path`, `test_ledger_persists_across_restart` |
| R-F | **Kill switch (rev 2).** `c1_rail_arm.py --kill`, in this order: (1) persist a sticky `block_risk_add` on the ledger and a daemon `emit_enabled=false` marker so no admission can reopen a symbol; (2) quiesce — the barrier refuses every risk-add for the current and later bars; (3) cancel every pending order across all legs and require broker cancel acknowledgements; (4) `closeposition` on every symbol with a confirmed or unknown position; (5) require broker-confirmed **zero positions and zero working orders**; (6) `--disarm`. A failed, partial, rejected or unknown close leaves the block in place, reports the remaining exposure and alerts; success is reported only on confirmed flatness. Operator-invoked, callable with the daemon down; recorded as an `arming_deviation`-class ledger event (`kill_switch`); **never automatic on a signal**. The O-1 beta-death event (three legs de-authorized) routes here as an operator action, not a multiplier. | `c1_rail_arm.py`, `c1_rail_telemetry.py` | offline (logic) / live (send) | `test_kill_switch_blocks_then_cancels_then_flattens`, `test_kill_switch_failed_close_keeps_block_and_reports_exposure`, `test_no_reopen_during_kill` |
| R-G | **EOD scheduler and daily reset (rev 2).** A clock job in the listener runs at the frozen pre-deadline buffer (**16:30 ET** regular sessions; **12:44 ET** on early-close dates): it blocks new entries and adds for the rest of the session, **cancels every pending order across every leg** (broker acknowledgements required; a flat leg's resting entry must not survive to fill after the deadline), then submits a quantity-less `closeposition` for **every owned symbol not broker-confirmed flat — confirmed or unknown position alike** (the same rule as R-F; an unknown position at the buffer is exposure, not a no-op) so the fill lands strictly before 16:45 / 12:59 ET and never in the 17:00–18:00 halt; the daily reset requires broker-confirmed **zero positions and zero working orders**; it consumes the D19 calendar + the TB-C1 forward calendar + the typed closure overlay (same digests as TB-S2's `AccountClock`); on an overlay date it refuses every entry/add, verifies flatness before the prior deadline and alerts; it **fails closed** past `coverage_end` (earliest deadline + alert). Daily reset of per-day state runs only after **broker-confirmed flatness** and ledger reconciliation; a partial/rejected/unknown/timed-out close retains per-day state, blocks risk-adds and alerts. | `ops/c1_rail/eod_scheduler.py`, `c1_rail_http_server.py` | offline (clock, calendar, refusal) / live (send) | `test_flatten_submitted_at_buffer_and_lands_before_deadline`, `test_unknown_position_symbol_is_closed_at_buffer`, `test_pending_orders_cancelled_across_legs_at_buffer`, `test_overlay_date_refuses_entries`, `test_past_coverage_end_fails_closed`, `test_reset_only_after_zero_positions_and_zero_orders` |
| R-H | **Disconnect and restart.** Per-source reconnect with backoff; on listener restart positions are rebuilt from `ExecutionStateStore` + `CapacityLedger` and **broker position truth** is required before any risk-add (the equity GET is not position truth; unknown state fails closed). Adapters restart **warm**: `checkpoint()` is written durably on the daemon volume **atomically together with the R-M execution cursor** after every bar **and after every processed execution event** (a fill, reject or cancel consumed between bars is never lost to a crash); execution events carry idempotent ids so a replay from the checkpoint applies each event at most once; on restart each adapter is restored from its checkpoint, the cursor resumes from the persisted value, and the leg is reconciled with the confirmed position **and its pending-order set** through R-M before emission resumes; a mismatch halts that leg; a cold start (no checkpoint) holds emission for the TB-W1 bar counts. | `daemon.py`, `book_registry.py`, `c1_rail_http_server.py` | offline (restore logic) / live (position read) | `test_adapter_restores_from_checkpoint_and_halts_on_mismatch`, `test_restart_without_position_truth_blocks_risk_add`, `test_checkpoint_and_cursor_persist_atomically_per_event`, `test_replayed_event_applied_once` |
| R-M | **Execution feedback.** The daemon learns outcomes only from the listener: `GET /c1/<path_token>/executions?since=<cursor>` returns confirmed fills, partial fills, rejects, cancels and confirmed positions per leg from the ledger's `broker_evidence` / reconciliation records; adapters advance position-dependent state **only** from these `ExecutionEvent`s, never from their own emitted intent (mirrors TB-S2 RC-9). **Live prerequisite L-1:** an automated broker execution/position read into the ledger does not exist today (`broker_evidence` is operator-supplied); the Phase 8 live test needs either a verified CrossTrade/Tradovate execution read or an attended per-fill confirmation procedure — recorded as a hard live gate beside O-4, not built here. | `c1_rail_http_server.py`, `evaluate_loop.py`, `book_registry.py` | offline (endpoint + mock) / **live (L-1)** | `test_rejected_entry_no_orphan_add`, `test_partial_fill_add_from_confirmed_base`, `test_cancelled_add_no_orphan_exit`, `test_adapter_never_advances_on_intent` |
| R-I | **Duplicate suppression.** Inherit PREREG-C1-DEDUPE-1 unchanged (R1 one derivation, R2 risk-add only, R3 refuse on a prior `accepted`, R4 per-signal, R5 pure predicate, R6 generator retained); executed by **TB-I4** after M1 `RESOLVED` + D-B13 GO + host `dry_run=True`; the TB-I3 live test cannot run without it. | (TB-I4) | live gate | (TB-I4's planted-defect set) |
| R-J | **Private packaging.** Port bodies enter the image only through a build-time private overlay (`deploy/c1_signal_daemon/private_overlay/ports/`, gitignored; the Dockerfile `COPY`s it; the root `.dockerignore` re-includes only that directory) documented for the operator; `test_c1_signal_daemon_image_manifest.py` is extended to assert the overlay's file hashes equal the **Python-port lines** that TB-A0 lands in `core/strategies/PORT_MANIFEST.sha256` (the umbrella's device: one line per port at path `ops/c1_signal_daemon/ports/<module>.py`, distinct from that file's `.pine` edition lines, which pin the sources the ports were ported from and are not compared to Python bytes) and that `effective_inputs.json` is **not** shipped (it is a replay input, not a live input). The test **fails closed** — it does not skip — when the four Python-port lines are absent, so the image gate cannot go green before TB-A0. | `deploy/c1_signal_daemon/Dockerfile`, `.dockerignore`, `tests/ops/test_c1_signal_daemon_image_manifest.py`, `core/strategies/PORT_MANIFEST.sha256` (TB-A0 lines) | offline | `test_overlay_hashes_equal_python_port_manifest_lines`, `test_image_gate_fails_closed_without_port_lines`, `test_effective_inputs_not_in_image` |
| R-L | **Policy source and mode timing.** The sizing host reads **only** `POLICY_REGISTRY["tradeify_portfolio@Tradeify_Select_100K"]` (admitted at TB-D0; absent → every risk-add halts; the frozen `calculate_protection` is no longer the book's DD path). The mode for a session is computed **once from the settled close**: the EOD scheduler, after confirmed flatness, writes `c1_dd_state.json` fields `settled_close_equity`, `settled_date`, `peak_equity` (EOD ratchet), `mode_next` (the row's trigger with six-decimal rounding); `_read_dd_scale` returns `mode_next`'s scale and halts if `settled_date` is not the prior venue session. **O-3:** at TB-B7 the file is seeded from the sealed snapshot (`peak_equity = historical_eod_peak`, `settled_close_equity = balance`, `settled_date` = the snapshot session, `mode_next` computed) and verified before the execution fingerprint is sealed; `c1_rail_arm.py --arm` refuses unless the admitted row equals the TB-E1 sealed cell **and** the dd-state seed digest is in the fingerprint. Offline tests thread the candidate value explicitly. | `c1_sizing_host_reference.py`, `eod_scheduler.py`, `c1_rail_arm.py`, TB-D0 | offline | `test_no_registry_row_halts`, `test_mode_from_settled_close_not_intraday`, `test_arm_refused_on_row_or_seed_mismatch` |
| R-Q | **Per-leg quantity laws (the ruled O-5/O-6).** `LEG_MAP` rows gain `leg_type`: `risk_ladder` (Striker: existing law, `E_firm` = the tier basis, `r_eff = base_risk × mode_scale × lifecycle`, cap `floor(cap_alloc/(1+pyr%))`, add `floor(executed_base × pyr%)`); `fixed_scaled` (Aegis: `qty = floor(8 × mode_scale × lifecycle)`, no adds); `adapter_quantity` (Vanguard: `qty = floor(qty_normal × mode_scale)` on entry **and** add, `qty_normal` from the payload, **with `watch_tiers_zero: true` — any lifecycle tier below AUTHORIZED yields 0 for entry and add**, the ruled O-5 restriction, so no one-contract WATCH-1 state can execute; the row's flag is asserted by a test and the existing parity assertion that WATCH-1 places one contract is retired under TB-I1); `fixed_base_adds_gated` (ORB: entry `floor(1 × lifecycle)` unscaled by mode, add `floor(1 × lifecycle)` in NORMAL and **0** in PROTECTED). Exact-rational arithmetic (the binary64 hazard of `m1_stage1_contract`). Adds are sized from the confirmed base only. The replay (TB-S2) and the rail must produce the same integer and the same refusal for identical inputs — that is the **parity contract** for TB-A1..A4 and the consolidated read. | `c1_sizing_host_reference.py` (TB-I1 for the locked-surface part) | offline | `test_leg_type_laws_match_book_policy_tables`, `test_orb_add_zero_when_protected`, `test_vanguard_zero_when_protected_and_zero_at_every_watch_tier` |
| R-K | **Split.** Offline-testable now (TB-I3): R-A1, R-A3, R-B1 and R-B3 mappings (with the addendum), R-B2 rows, R-C, R-D, R-E, R-F logic, R-G clock/calendar/cancel/refusal, R-H restore logic, R-M endpoint + mock, R-J, R-L, R-Q. Live-source-dependent (a later packet): R-A2 with a real source, R-B2 symbol verification, **L-2** order-type capability (stop entries, stop_loss/take_profit, cancel/replace), R-F/R-G/R-H sends and reads, **L-1** execution feedback read, feed health, the item-5-class dry-run signal, the end-to-end SIM chain — after Track A `RESOLVED`, TB-I5 PASS, TB-I4, TB-D0, TB-V1 in the image and the D-B15 emit GO. | — | — | — |

**O-8 applied:** the live Vanguard adapter keeps the TradingView daily-key latch (`pine_ta.tv_daily_key`) exactly as ported; the daemon's US-holiday table is the same frozen set the ports use and is part of the active-leg digest.

**Telemetry:** `EVENT_KINDS` gains `capacity_reserved`, `capacity_refused`, `capacity_takeover_begin`, `capacity_takeover_cancel_ack`, `capacity_takeover_close_confirmed`, `capacity_takeover_admitted`, `capacity_takeover_refused`, `side_refused`, `barrier_timeout`, `mode_settled`, `eod_flatten`, `overlay_block`, `kill_switch`, `restart_reconcile` (schema version bump; the validator's secret scanner unchanged).

## The `LEG_MAP` / symbol / lifecycle expression table (TB-I1 and TB-I3 implement; TB-V1 releases allocations)

| `leg_id` | `leg_key` (lifecycle) | symbol (provisional) | side | `leg_type` | normal | protected | `cap_alloc` now |
|---|---|---|---|---|---|---|---|
| `aegis_6j` | `Aegis 6J` | `6J1!` | sell | `fixed_scaled` | 8 | 3 | 0 |
| `dj30_mym_p250` | `Striker MYM p250` | `MYM1!` | buy | `risk_ladder` (pyr 250 %) | host ladder, cap 22 | ladder at 0.4× risk, cap 22, add ≤ 55 | 0 |
| `vanguard_mgc` | `Vanguard MGC` | `MGC1!` | buy | `adapter_quantity` (adds from the adapter's own rule; `watch_tiers_zero`) | 1 or 2 | 0 (and 0 at every WATCH tier) | 0 |
| `orb_mnq_v7` | `ORB MNQ v7` | `MNQ1!` | buy | `fixed_base_adds_gated` | 1 + ≤2 adds of 1 | 1, no adds | 0 |

Lifecycle keys are added to `core/lifecycle.STRATEGY_KEYS` (TB-I1); the runtime `/data/lifecycle_state.json` must list all four (O-2, seeded AUTHORIZED by TB-V1) or every risk-add halts. WATCH tiers: Vanguard is **zero at every WATCH tier by rule** (O-5) and ORB floors to zero; Aegis 4/1/2/0; Striker ×0.5 / ×0.25 risk.

## Steps

1. Operator ratifies this spec and the S2b addendum (R-B1); the coordinator records both.
2. TB-I1 (Claude) lands the locked-surface part (lifecycle keys, `LEG_MAP` rows at zero with `leg_type` and `allowed_entry_side`, the four quantity laws, registry-only policy read with explicit-argument threading for tests) test-first from R-Q and R-L.
3. TB-I3 (Codex) implements the offline-tagged requirements against the merged TB-I1 surface; single PR per the umbrella footprint; Codex review; coordinator adjudication.
4. The live packet opens only when Track A reports `RESOLVED`, TB-I5 has passed, L-1 is resolved, TB-I4, TB-D0 and TB-V1 are in the image, every symbol is verified and the D-B15 emit GO is recorded.

Gate: RESOLVED if every umbrella B5 item maps to a requirement above with a named test and an offline/live tag (checked in the TB-I3 PR body); FALSIFIED if any live-risk requirement lacks a fail-closed default (return `BLOCKED — plan-itself-wrong` naming it).
Boundary: no market-data provider or feed adapter · no dedupe re-specification · no `--acknowledge-m1-unresolved` or any arming path · no recreation of `RUNBOOK.md` or fixture-pin refresh from tree bytes · no B1 field change without the S2b addendum · no `POLICY_REGISTRY` row here (TB-D0) · no edit to `core/dd_protection.py`.
Reads: listed above. Owner: umbrella TB-S3 → TB-I1 (locked surfaces) + TB-I3 (offline) + the later live packet; O-3 discharged by R-L; O-8 by the adapter note; L-1 (execution/position read) and L-2 (order-type capability) recorded for the operator beside O-4.
