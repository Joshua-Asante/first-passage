# INSTRUMENT LEDGER — MCL

**Symbol:** NYMEX Micro WTI Crude Oil futures (MCL; Globex, Databento `GLBX.MDP3`) · **Parent:** CL · **Asset class:** energy futures
**Status:** **OPEN — geometry-cleared, mechanism-owed.** Research/discovery only. Frozen fade config `CONFIG-B-MCL-2026-07-31` scored this session under TNEC; **not a candidate; not elected.** No live leg, no allocation, no K spend.
**Last updated:** 2026-08-18

**Purpose:** single source of instrument-level truth (operational rule 10, [`docs/adr/2026-06-11-instrument-ledger-and-cfg-fingerprint.md`](../../docs/adr/2026-06-11-instrument-ledger-and-cfg-fingerprint.md)). Created **2026-08-09** as the live touching session for [`instrument-lane SPEC`](../../docs/spec/2026-08-09-instrument-lane-mcl-mes-mgc-spec.md) — ADR [`2026-07-25-instrument-profile-index.md`](../../docs/adr/2026-07-25-instrument-profile-index.md) §5 (creation-on-touch). Distinct from the CFD ledger [`USOIL.md`](USOIL.md); findings do not transfer automatically.

## PROFILE (machine-readable)

```yaml
symbol: MCL
asset_class: energy-futures
family: []
venue_tradable: true
venue_note: "Tradeify Energy Product Group (CL/QM/MCL/NG/QG). Withdrawn Striker legs no longer reserve cap headroom for new non-Striker research (MSL B8 occupancy release 2026-08-12); Striker redeploy still barred; LEG_MAP code untouched."
k_bank_source: "../../discovery_manifests/"
cost_hurdle:
  value: 5.3423
  units: "bp/round-trip"
  basis: "Stage-0 2023 median cost_bp for MCL (fade stage0 RESULTS.md)"
  source: "#C1"
bars:
  - id: free-data-5th-leg-snag-closed-2026-07-01
    source: "../../docs/rejected_candidates.md"
cells:
  - mechanism: daily-range-state-persistence
    verdict: AMBIGUOUS-PARKED
    date: 2026-08-18
    source: "../../lab/analysis/_inbox/rangestate_mcl_2026-08/RESULTS_S1B.md"
  - mechanism: pullback-failure-resumption
    verdict: DEAD
    date: 2026-08-13
    source: "../../docs/briefs/closures/MSL-S2A-closure-falsified.md"
structure:
  - claim: "Frozen CONFIG-B-MCL geometry clears expressibility under SIGMA-NATIVE; geometry is not edge — N-SURV/N-EDGE/N-SIZE remain U until a mechanism trade series exists."
    source: "#C2"
```

---

## STANDING WARNINGS (read first)

- **W1 — MONTHLY roll (Energy).** MCL rolls monthly; roll-spanning windows are far more frequent than equity-index quarterly rolls. Fade Stage-0 parity measured ~14% session exclusion under the published roll-exclude rule (~3× the MYM quarterly figure) — see [`RESULTS_parity.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS_parity.md). Check roll dates against every window.
- **W2 — `ohlcv-1d` UTC-day bucketing → phantom weekend bars.** Drop settle-date weekday > 4. Feed-general.
- **W3 — Session window is not an equity RTH constant.** Integrity batteries that assume 09:30–16:00 ET certify completeness inside an arbitrary slice of a ~23h Energy contract, not a venue session for MCL. Ruling for which window governs remains a design choice; do not treat equity-RTH PASS as Energy-session validity ([`RESULTS_parity.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS_parity.md)).
- **W4 — FOMC exclusion thins τ_max.** `CONFIG-B-MCL-2026-07-31` pays 180→120min @ 09:30 under the FOMC exclusion ([frozen rulings](../../docs/notes/2026-07-31-fade-stage1-frozen-rulings.md)). Any re-score must keep that hold horizon.

---

## DURABLE FINDINGS

| # | Finding | Evidence | Confidence |
|---|---|---|---|
| **C1** | **Stage-0 cost geometry measured (2023 panel).** Fade stage0 reports MCL Energy-group cost cells including `cost_bp` **5.3423** at the 2023 median — input to §4.2 ranking, not an edge claim. | [`RESULTS.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS.md) | **HIGH** (panel arithmetic). |
| **C2** | **Geometry PASS under `SIGMA-NATIVE-2026-07-31` + `CONFIG-B-MCL-2026-07-31`.** Native session σ **$112.17**; nearer-barrier **$54.93**; frozen primary **n=4** expressible (n=5,6 reported expressible). Implied annSR 9.98–12.23 at pinned p=0.65 is **disclosure only** (non-gated; geometry ≠ edge). K-bank floor figures in the 2026-07-31 ruling table (0.850 from banked K) are **void as gates** under [K-bank ADR](../../docs/adr/2026-08-04-family-k-bank-disclosure-not-gate.md); current screen floor for `K_intrinsic=1` is 0.650 — disclosure only. | [`RESULTS.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS.md) · [`RESULTS_sigma_native.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS_sigma_native.md) · [frozen rulings](../../docs/notes/2026-07-31-fade-stage1-frozen-rulings.md) | **HIGH** on expressibility; **no edge claim**. |
| **C3** | **TNEC class attestation (instrument-level, no mechanism series).** See ACTIVE disposition string. N-ACT/N-SHAPE pass as config/class; N-SURV/N-EDGE/N-SIZE = U. | [TNEC-1](../../docs/spec/2026-08-08-tradeify-necessary-conditions-target-spec.md) · this session | **MODERATE** (class attestation, not filled-construct proof). |
| **C5** | **OFFICIAL corrected-battery verdict (2026-08-18, supersedes C4's interim NOT-CONFIRMED): `SIGNAL-GENERIC`.** Under the frozen class battery ([spec+ADDENDUM-1](../../docs/spec/2026-08-18-magnitude-persistence-corrected-null-battery.md)): presence passes (L2 lb 0.5651; halves; **L4 boundary-exact 6/8** under the rule's own n_cond<20 exclusion — 2010 dropped at n=14, a disclosed prediction-miss adjudicated rules-govern); attribution **GENERIC** (obs 0.6282 at the **69th pct** of CL's own linear-ACF band, p_upper 0.3107) — canon-attributed volatility clustering, SURVIVAL-ONLY durability. **Binding A6 rails: NOT a mechanism; does NOT discharge this ledger's mechanism-owed status (UNCHANGED); NOT a conditioner license** (O2 discharged by C6). Mandatory co-quotes wherever cited: crisis>calm per-year ordering (2011/2016/2014 top); drop-{2011,2014,2016} diagnostic; calm/crisis bucket definitions differ by partition rule (level-based 0.617 vs event-identity 0.537 — year lists required, A7). Conditioner-engineering branch PARKED (C6). Counts toward Step-0 slate §4 RESOLVED. | [`RESULTS_S1B.md`](../../lab/analysis/_inbox/rangestate_mcl_2026-08/RESULTS_S1B.md) §5 · [`RESULTS_CORRECTED.md`](../../lab/analysis/_inbox/rangestate_corrected_2026-08/RESULTS_CORRECTED.md) | **HIGH** (frozen spec + pre-registered interpretation; bit-exact independent reimplementation; official seeds drawn once). |
| **C6** | **`Q-CONDVAL-1` `FALSIFIED` 2026-08-18 — C−U lift does not buy a material fraction of `hurdle_4x` at the N-EDGE cell.** Frozen before substitute: slate-2 center (rr=2.5, WR=0.36), α=0 mapping, R=$75 / RT=$4.12 / material 0.50 → `L_star`=0.4226. Measured L=0.1297 (ΔE=0.034R vs bar 0.110R). O2 discharged. C5 finding unchanged. Re-proposal = named new host geometry before any re-read, or a new finding. | [`closure`](../../docs/briefs/closures/Q-CONDVAL-1-closure-falsified.md) · [`RESULTS`](../../lab/analysis/_inbox/q_condval_1_2026-08/RESULTS.md) | **HIGH** (committed-key arithmetic; prereg sha256 printed before JSON open). |
| **C4** | **`H-RANGESTATE-CL-1` (S1b, Step-0 slate row S1) — raw battery reading `SIGNAL`, downgraded to `NOT-CONFIRMED`.** Daily top-quintile-TR → elevated-next-day-TR conditioner, CL parent train era (2010–2019, `MCL.v.0` 2019+ reserved). Conditional hit rate **0.6282** (n=425/2,056 scored) clears all four frozen limbs, but adversarial review found the placebo null misspecified — **20/20 zero-mechanism AR(1) surrogates calibrated to CL's own measured True-Range autocorrelation (ρ₁=0.4520) cleared the identical battery at a *higher* rate (0.72–0.80) than the real data (0.6282)**, and the effect fails a regime-concentration test (removing the 2011/2014/2016 crisis years together flips the verdict to NULL; a clean crisis-vs-calm split shows the calm-year bucket alone is an independent NULL failing its own placebo). **Does not discharge this ledger's "mechanism-owed" status.** Full detail + root cause: [audit note](../../docs/notes/audits/2026-08-18-block-shuffle-placebo-does-not-control-for-tr-autocorrelation.md). | [`RESULTS_S1B.md`](../../lab/analysis/_inbox/rangestate_mcl_2026-08/RESULTS_S1B.md) · [`PREREG_S1B.md`](../../lab/analysis/_inbox/rangestate_mcl_2026-08/PREREG_S1B.md) | **HIGH** (frozen prereg, adversarially verified — independent reimplementation matched to full float precision; battery defect quantified via 20/20 surrogate trials). |

## DEAD / REJECTED (instrument-specific)

None instrument-specific beyond the CFD-era USOIL closures (do not inherit — different venue/symbol). M6A death under the same fade screen is recorded on the fade artifacts, not as an MCL kill. **MSL-S2A** `pullback-failure-resumption` explore **FALSIFIED** (N-ACT) — construct-scoped; MCL the instrument is not rejected. [closure](../../docs/briefs/closures/MSL-S2A-closure-falsified.md) · [`RESULTS_g2`](../../lab/archive/msl_s2a_mcl_2026-08/RESULTS_g2.md).

## ACTIVE / OPEN

- **Instrument-lane disposition (2026-08-09):** geometry-cleared / mechanism-owed. Operator election (if any) is out of scope for this ledger session — see SPEC step 4.
- **Local panels (verified 2026-08-11):** Assembled `mcl_1m.parquet` is **absent** from the machine-local data root (`lab/analysis/c1/c1_signal_identity_2026-07-28/data/` — gitignored; dir holds only `.gitignore`). Fade stage0 holds RESULTS*/CARD only (panels were never git-tracked; Great Prune class-2 dropped harnesses). **Raw** 2023 `MCL.v.0` `ohlcv-1m` monthly DBN chunks **are** present under `DATABENTO_CACHE` (`~/.databento_cache` by default; campaign `fade-stage0-cost-pins` / phase `oos`) — 12/12 HIT (~5.0 MB); reassembly is a local decode (no pull / $0) while those bytes remain. Committed reusable derived surface: [`RESULTS_sigma_native.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS_sigma_native.md). Extending beyond 2023 still requires databento estimate → operator GO.

### Disposition (ACTIVE cell)

```
GEOMETRY: PASS (expressible @ SIGMA-NATIVE σ=$112.17; barrier $54.93; frozen n=4 primary / n=5,6 reported)
TNEC:     P U U P U | U | U | U
          N-ACT  N-SURV N-EDGE N-SHAPE N-SIZE | bust | P(pass) | μ
STATUS:   OPEN — geometry-cleared, mechanism-owed
```

Limb reading (link out; do not restate σ/K as authority):

- **N-ACT = P** — Wed/Thu config can host ≥1 Mon–Fri week trade by construction (class attestation; not a measured idle-clock).
- **N-SURV / N-EDGE / N-SIZE = U** — no mechanism trade series; geometry ≠ edge; Part A / DSR / frontier unread.
- **N-SHAPE = P** — Energy Product Group · two-sided legal · micro · flat-by-16:00 build target inside venue print (EM5 class). EM3 independence unread until a mechanism exists → disclose “shape-legal as config, not as filled construct.”

## SESSION LOG

- **2026-08-18e** — **`Q-CONDVAL-1` `FALSIFIED` (C6).** Conditioner-engineering branch parked at
  $0/K=0. Cell yaml stays `AMBIGUOUS-PARKED` (profile-schema legal; finding is still
  SIGNAL-GENERIC on C5). Mechanism-owed UNCHANGED.
  [`closure`](../../docs/briefs/closures/Q-CONDVAL-1-closure-falsified.md).
- **2026-08-18b** — **OFFICIAL corrected-null re-score: `SIGNAL-GENERIC` (C5 added; cell stays
  `AMBIGUOUS-PARKED` — a parked canon-attributed finding awaiting its own conditioner-
  engineering prereg; mechanism-owed UNCHANGED).** Frozen battery ran on operator PROCEED after
  a 4-lens pre-official verification (bit-exact reimplementation) and a rules-govern
  adjudication of the L4 prediction-miss (ADDENDUM-1). Attribution decisively GENERIC (69th pct
  of own linear-ACF band). Slate §4 → RESOLVED via this row. $0 / K unchanged (re-measurement).
  [`RESULTS_CORRECTED.md`](../../lab/analysis/_inbox/rangestate_corrected_2026-08/RESULTS_CORRECTED.md)
- **2026-08-18** — **`H-RANGESTATE-CL-1` (Step-0 slate S1b) → raw SIGNAL, downgraded to
  NOT-CONFIRMED after adversarial review.** Daily True-Range top-quintile persistence, CL train
  era; $0/K=1 disclosed. First raw battery reading cleared all four frozen limbs (gateHit
  0.6282, placebo p=0.0005) — the program's first apparent SIGNAL. A heavier adversarial-verify
  workflow (4 lenses + synthesis, scoped specifically because a SIGNAL is the highest-stakes
  outcome type) found the placebo null misspecified: 20 independent zero-mechanism AR(1)
  surrogates calibrated only to CL's own measured autocorrelation cleared the identical battery
  at a *higher* rate than the real data, and the effect fails a crisis-year regime-concentration
  test. **Verdict downgraded to `NOT-CONFIRMED`; mechanism-owed status stays OPEN.** MECHANISMS.md
  `daily-range-state-persistence` heading and the sibling GC screen (S1a) both corrected in the
  same pass. Step-0 slate rows S2/S3 (queued to reuse the same battery) **paused** pending a
  structural fix. Audit note:
  [`2026-08-18-block-shuffle-placebo-does-not-control-for-tr-autocorrelation.md`](../../docs/notes/audits/2026-08-18-block-shuffle-placebo-does-not-control-for-tr-autocorrelation.md).
  [`RESULTS_S1B.md`](../../lab/analysis/_inbox/rangestate_mcl_2026-08/RESULTS_S1B.md)
- **2026-08-14n** — **MSL WHO-track `STILL DRY` (estate-wide).** Sweep went past this instrument’s fade well; no NEW WHO named anywhere on the Tradeify envelope. ACTIVE cell unchanged. notice (`git show 14d71c93:docs/notes/notice/N-2026-08-14-msl-who-track.md`). $0 / K=0.
- **2026-08-14** — **MSL slate-3 BLOCKED (mechanism-dry).** WHO attempt on the reopened fade geometry found no family outside 2026-08-10 INTAKE-DRY; camp not opened. notice (`git show ef48b015:docs/notes/notice/N-2026-08-14-msl-slate-3-constraints.md`). ACTIVE cell unchanged (`OPEN — geometry-cleared, mechanism-owed`). $0 / K=0.
- **2026-08-13p** — **MSL-S2A explore `FALSIFIED` (N-ACT).** [`RESULTS_g2`](../../lab/archive/msl_s2a_mcl_2026-08/RESULTS_g2.md) · [closure](../../docs/briefs/closures/MSL-S2A-closure-falsified.md) — trades/week 0.511; long FLIP FAIL; CONFIRM unread; Pine unpaid. $0 / K=0.
- **2026-08-13o** — **MSL-S2A G0 FROZEN (B4 GO).** [`PREREG_G0`](../../lab/archive/msl_s2a_mcl_2026-08/PREREG_G0.md) — `K_intrinsic=1`; CONFIRM `2025-07-01 → 2026-07-02` unread; roll calendar + FOMC list frozen; explore later FALSIFIED 2026-08-13p. $0 / K=0.
- **2026-08-13d** — **MSL P3.4 / S2A Stage-0:** PROFILE `bars:` registered `free-data-5th-leg-snag-closed-2026-07-01` → `docs/rejected_candidates.md` (same SNAG C2 registered on MGC). Door-check non-vacuous; index OHLCV bar intentionally omitted (MCL is not an equity-index future). `profiles.json` rebuilt same commit. Panel pin (bytes gitignored in this clone): `SHA256SUMS` `5aa504567b943ff68506b8b5c21df293c5a553543fdf1ac606adeb0f5bfbbd23` `MCL_M15.csv`. $0 / K=0.
- **2026-08-13** — **`IMPLIED-SR-REPORT-ONLY-2026-08-13`:** the 2026-08-10 freeze-time FAIL is superseded; implied annSR is report-only; the fade design-region on this instrument is **OPEN** again as geometry (Rulings 1–4 stand). **No mechanism is admitted.** MCL stays in the L3 pool; ACTIVE cell unchanged (`OPEN — geometry-cleared, mechanism-owed`). [`ADR`](../../docs/adr/2026-08-13-implied-sr-report-only-fade-reopen.md). $0/K=0; no `core/` change.
- **2026-08-11c** — **Stale ACTIVE "Local panels" claim repaired (doc-truth only).** Discharges the found-not-repaired note in the 2026-08-11b cell / [`Q-MCLTAS-1` closure](../../docs/briefs/closures/Q-MCLTAS-1-closure-falsified.md) §4. Prior line asserted *"MCL 1m cache … exists for 2023"*; that reading caused the same-day Stage 0b planning error (scoped as a $0 measurement on an "already-committed" cache). Enumerated: assembled parquet gone from the gitignored data root; fade stage0 RESULTS-only; **12/12** 2023 monthly DBN chunks still HIT under `DATABENTO_CACHE` (campaign `fade-stage0-cost-pins`); committed derived σ surface remains [`RESULTS_sigma_native.md`](../../lab/analysis/c1/tradeify_fade_stage0_2026-07-30/RESULTS_sigma_native.md). Sibling sweep: only this line carried the false-positive "cache exists" claim; **M2K.md W4** has the inverse defect (claims no databento cache while the same pull left 12/12 M2K.v.0 chunks — report-only, not fixed here). No pull, no K spend, no `core/` / Pine / allocation / `dd_protection` / rail change.
- **2026-08-11b** — **`Q-MCLTAS-1` δ-extraction probe scoped → `FALSIFIED` on magnitude; the probe was never run. $0 / K=0.** Operator authorized Stage 0a+0b (both free) against the [scoping brief](../../docs/briefs/Q-MCLTAS-1-tas-settlement-delta-extraction-probe-scoping.md)'s pre-registered gate. **Wall B (dispositive):** Req-5 hurdle **11.60 ticks/event** = **14.87 bp** at the 2023 panel basis vs the estate's causal-public δ ceiling **3.21 bp** → **4.63×**, only 3.01× at implausible $120 oil, 3.04× under the forbidden bare-commission ablation. Converted to cohort-bound **δ/σ** against **this instrument's own measured σ surface** (`RESULTS_sigma_native.md`, 15-min column √-time scaled to the 2-min window): required δ/σ **0.62–1.35** vs D5's committed **0.113/0.194** → floor **3.2×** under maximal stacked generosity, **7.0×** defensible. **Wall A:** the {free ∧ signed ∧ price-exogenous ∧ window-aligned} sign-source intersection is **EMPTY** — published TAS volume is **gross by construction**, ΔOI is directionally agnostic, COT is weekly/lagged, and **CME publishes no settlement-window imbalance print** (the structural asymmetry vs the equity closing auction `F1`/MOC traded); one *entitled-but-costed* route (TAS-book order flow in `GLBX.MDP3`) survives **unverified** and cannot reach Wall B. Adverse mechanism prior: TAS exists *so that* mandated flow can lock settlement **without** revealing direction to the outright book ⇒ small public residue by construction — informed-flow signature's **third** instance, on the family where it was **first** confirmed. **The ENV-1 reading "blocked on δ alone, non-circular probe route" is corrected in both halves:** non-circularity is real but scoped to *decay observables*, and the cell was never blocked on δ alone — a magnitude wall no δ could clear was computable at $0 without finding one. ⚠ **Ledger defect, recorded not repaired:** the ACTIVE section's *"MCL 1m cache … exists for 2023"* is **stale — that cache is not on disk** (heavy artifacts gitignored and absent); Stage 0b substituted the committed σ surface. **MCL the instrument is NOT rejected** — this is a design-region kill, per 2026-08-10c. Registry row filed. TNEC string, geometry verdict and ledger status unchanged; no pull, no K spend, no election, no `core/` / Pine / allocation / `dd_protection` / rail change. [`closure`](../../docs/briefs/closures/Q-MCLTAS-1-closure-falsified.md)
- **2026-08-11** — **Block 1 intake screening re-run on the narrower TAS re-open candidate → still `SHAPE-UNSCREENABLE` / `INTAKE-DRY`; no draft admitted. $0 / K=0.** Per the 2026-08-11 operator ruling on [`Q-TNEC-ENV-1` closure item (b)](../../docs/briefs/closures/Q-TNEC-ENV-1-closure.md) ("*BE3's fade-scoped kill does not bar a TNEC-limb-scored TAS settlement candidate… No probe, no candidate*"), executed the full four-gate chain fresh against `mcl-tas-settlement-window-replication` (14:28–14:30 ET CME CL settlement window; 20-tick stop; 120s horizon) per the sequencing note's "full intake chain… before it is a candidate." **Gate 1 dedup (re-executed this session, output confirmed unchanged since the [2026-08-11 notice](../../docs/notes/notice/N-2026-08-11-daily-auction-settlement-MCL.md)):** `rg -in "fix|auction|settlement|benchmark|MOC|imbalance|LBMA|WMR|gold price"` + roll/rebalance sweep over `rejected_candidates.md`/`rejected_signals.md` — nearest rows are MYM/EURUSD auction-imbalance and third-Friday derivative-settlement (different instrument, no MCL/CL energy settlement-window row exists); BE3/SFX-1 are the same-instrument near-dup but their kill is explicitly fade-program-scoped (lifted for TNEC-limb scoring by the 2026-08-10 L2 ruling), not a bar here. **Gate 2 EM0:** `PASS` — 1 cell, `K_intrinsic=1`, floor 0.650. **Gate 3 EM0–EM5:** **`P U U P P P`** → `SHAPE-UNSCREENABLE` — EM1 (edge floor) and EM2 (edge-indexed sizing) can't be scored because **δ is `null`**: no published cohort δ exists for CME CL settlement-window benchmark-replication flow, and cross-instrument transplant from the FX-fix/gold-fix cohorts is refused under harvest Req 2. EM3/EM4/EM5 score `PASS` (independent single daily entry, hard 20-tick stop, no overnight/gap exposure; 252 events/yr ≫ weekly cadence floor; flat by 14:30 ET, well inside the 16:00/16:45 ET flat rule; MCL is Energy not Equity Index so two-sided legal; no live strategy occupies the MCL order symbol). **Gate 4 Req-5 cost-law:** `UNSCREENABLE` for the same δ-absence reason — the hurdle (11.6 ticks/event = $0.116/bbl) is known, but there is nothing to compare it against; the only CL-family δ proxy in the estate (`H-FBEIA-1`, wrong-signed, 0.054× a *different* hurdle) is an informed-flow-signature prior on how this cell is likely to die, not an admissible substitute magnitude under Req 2. **Disposition: unchanged from the 2026-08-10 general-slot `INTAKE-DRY` reading** (`ops/instruments/MCL.md` 2026-08-10 entry, above) — this run narrows *why* (pure δ-absence, not the fade-program design law) and formally discharges the "full intake chain" the 2026-08-11 ruling required before any TAS-re-open seed counts as a candidate. **Re-proposal bar (unchanged, restated):** a completed δ-extraction probe on CME's free-published TAS settlement-window volumes, pre-declaring a **signed, non-price-derived** exogenous imbalance source (reading sign off price action is BE1's laundering kill verbatim) — this probe is **not attempted here**: designing a non-circular signed-imbalance extraction is new methodology work, not a mechanical Block-1 gate check, and no such probe is scoped or owned yet. No pull, no K spend, no election, no `core/` / Pine / allocation / `dd_protection` / rail change.
- **2026-08-10c** — **`IMPLIED-SR-GATE-2026-08-10` ruled (JA): the fade design-region on this instrument is CLOSED on its own arithmetic.** The owed operator disposition on `implied_annualized_sr` resolved **promote-to-gate**; the 4×/`CONFIG-B-MCL` admitted region empties at every `p` (floor 2.98 as-ruled / 2.11 elective-ablated, both over the 1.83 ceiling; nothing clears futures-native 0.89). **MCL the instrument stays in the L3 pool** — what died is the fade bracket-region, not the symbol; any future MCL design starts from a fresh region that must carry the new freeze-time limb. [`ADR`](../../docs/adr/2026-08-10-implied-sr-plausibility-gate.md). $0/K=0; no `core/` change.
- **2026-08-10** — **Intake screening run against the owed mechanism → `INTAKE-DRY`; no draft admitted. $0 / K=0.** Executed the four intake gates ([PR #718](https://github.com/Joshua-Asante/first-passage-archive/pull/718) Block 1) on the standing "mechanism-owed" slot. **Gate 1 dedup (executed, output pasted in the closure):** every MCL-hostable flow family already carries a prior — `BE3` TAS settlement-replication `DEAD`, `SFX-1` settlement+GSCI-roll `DEAD`, `PROPENG-EJECT` `DEAD`, `LIT-EIA-PHYS` `DEAD` ([census](../../docs/notes/notice/N-2026-07-26-forced-flow-census.md) passes 2/3/4), `H-FBEIA-1` CL-EIA `SCREEN-FAIL`, `H-FCCARRY-1` CL-carry `SCREEN-FAIL`; energy-reversion literature is **spread-shaped ⇒ structurally illegal** under §4.1a (two independent instances). The census's own backlog item 1 states the open lane's binding condition — **≥2 independent triggers/day**, which EIA (1/wk) and the roll (5 d/mo) both fail. **Gate 2 EM0:** `PASS` — one cell (`n=4`, R:R 1.0, cost-floor stop 0.5493 pt), `K_intrinsic=1`, floor **0.650**. **Gate 3 EM0–EM5:** **`P U U P P P`** → `SHAPE-UNSCREENABLE` (EM1/EM2 edge-indexed with no measured edge; never patched to PASS). **Gate 4 Req-5 cost-law: `KILL`** — hurdle **21.3692 bp/event** (= 4 × `cost_bp` 5.3423; **$16.48/contract**) re-derived by execution against the only CL-family cohort δ in the estate, **−1.16 bp** (`H-FBEIA-1`, N=445, σ 50 bp, t −0.49) — **0.054× the hurdle and wrong-signed**. **New this session, and δ-independent:** the 2026-07-31b `implied_annualized_sr` finding was re-derived from the pre-prune harness — min over the **entire** admitted region (215 cells, p swept 0.505–0.900 × R:R {0.66, 1.0} × n 1–8) is **2.98 = 1.63× the 1.83 estate ceiling**; the frozen `n=4` cell needs **9.98 = 5.5×**. So the region is empty of plausible cells for *any* mechanism, named or not — which is why gate 1's dryness is structural rather than a failure of search. **Consequence for the owed operator ruling:** promoting `implied_annualized_sr` from report-only to a gate **empties the region at every `p`** (confirmed by execution, not assumed), closing the fade program on its own arithmetic at $0/K=0. That ruling is **not taken here**. Ledger status, TNEC string and geometry verdict are **unchanged**; no rejection registered (an intake-dry pass is not a new kill). No pull, no K spend, no election, no `core/` / Pine / allocation / `dd_protection` / rail change.
- **2026-08-10b** — **Killing-constants sensitivity (same session, by execution): INTAKE-DRY survives every elective ablation; the emptiness is carried by frozen + necessary constants only.** Provenance split of the region's four limbs: `expressible` (barrier ≤ σ_session) is arithmetic necessity; the 4× multiple is operator-frozen (`COST-MULT-4X`); the **$200 payability gate is elective — and skewed: `design_law` gates on it while EM screen §2.1 rules the funded $200 floor *disclosure-only***; the $750 worst-day fraction and the 1.83 `SHARPE_CEILING` cohort (Aegis on the retired Pepperstone CFD panel; futures-native best ≈0.89) are elective. Ablations on the frozen rr∈{0.66, 1.0} grid: dropping payability + σ-window + worst-day moves the floor 2.98 → **2.11 = 1.15× the CFD ceiling / 2.37× futures-native, still zero admissible cells under either** — so the kill rests on {4× × cost-floor stop × σ_session}, not on any elective constant. Widening rr below 0.66 (a new modelling election, **not taken**) opens cells under the 1.83 CFD ceiling only at assumed **80–92% sustained WR** with payability dropped — the high-WR tiny-target fade shape gate 1 already holds `DEAD` (BE3/SFX-1) — and **no cell anywhere, under any ablation, clears the futures-native 0.89 ceiling**. Consequence for the owed `implied_annualized_sr` ruling: promotion-to-gate is robust to the $200 skew being repaired. Arithmetic on assumed-edge space; K=0, $0, no data examined, licenses nothing.
- **2026-08-09** — **Ledger created + frozen CONFIG-B scored** under [`instrument-lane SPEC`](../../docs/spec/2026-08-09-instrument-lane-mcl-mes-mgc-spec.md). Geometry PASS + TNEC `P U U P U | U | U | U` under `SIGMA-NATIVE-2026-07-31` / `CONFIG-B-MCL-2026-07-31`. No pull, no K spend, no election, no `core/` / Pine / allocation / `dd_protection` change.
