# `core/strategies/` — locked-book catalog + stubs

**Open [`CATALOG.md`](CATALOG.md) first.** Hot stubs are `*_CARD.md`; cold
LOCK/CHANGELOG/CANDIDATE bodies live under `_archive/<family>/`.
Pine + ports are gitignored and hash-pinned (`MANIFEST.sha256`,
`PORT_MANIFEST.sha256`).

Disposition ≠ lifecycle authorization — see
[`docs/methodology/strategy_lifecycle.md`](../../docs/methodology/strategy_lifecycle.md).
