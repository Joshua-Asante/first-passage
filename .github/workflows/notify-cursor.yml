name: Notify Cursor of Claude Review

# Disabled 2026-09-04 — operator: Claude reviews were auto-pinging
# Cursor too quickly. Prior trigger: claude[bot] PR comments/reviews
# (except the in-progress ack) posted a mention that woke Cursor.
# Restore those event triggers from git history if re-enabling; do not
# re-add them without a superseding note on
# docs/adr/2026-07-14-cc-cursor-surface-allocation.md.
on:
  workflow_dispatch:

jobs:
  ping-cursor:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Auto-ping disabled
        run: echo "notify-cursor auto-ping is disabled"
