# `_inbox`

Unassigned **until** the camp carries a RESULTS, PREREG, or `**Verdict:**`.
Outcome-bearing work is listed in [`lab/CATALOG.md`](../../CATALOG.md) (In flight
first, then Hot bodies → `_inbox`). It must leave `_inbox` before `--slug`.

Open the catalog first. Do not glob this directory to infer what is live.
The operator work list is [`STATE.md`](../../../STATE.md).
