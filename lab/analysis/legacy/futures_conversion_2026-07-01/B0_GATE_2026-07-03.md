> ⚠ **2026-08-23 reader-intercept (R1 CLOCK repair, fix-pass):** Finding #1/#3 below quote "the C4
> 25K gate number (99.60%)" and "C4's 91.46% gate pass" ([`RESULTS_C4_forceflat_2026-07-03.md`](../../../archive/bulenox_futures_remc_2026-07-01/RESULTS_C4_forceflat_2026-07-03.md)) —
> those are **EOD-clock** figures on the current live `simulate_path` engine, with `intraday_low`
> never populated. 150K's 91.46%/8.54% is already FAIL against the current 3.0% survivor-scoring
> ceiling and can only deepen on the honest clock (monotonicity); 25K's 99.60%/0.40% PASSes on this
> clock but is a named, un-re-run residual. This finding's own granularity conclusions (which tier
> is "cap-bound," "the sweet spot," etc.) are about integer-sizing, not CLOCK, and are unaffected —
> but do not cite 99.60%/91.46% as an honest-clock figure. See
> [`../../c1/firm_model_repair_r1_7tier_2026-08-23/RESULTS.md`](../../c1/firm_model_repair_r1_7tier_2026-08-23/RESULTS.md)
> §2/§4b. Frozen body unedited below.

# Gate B0 — Pine drop verification (prop pair) + deploy-time floors

**Date:** 2026-07-03
**Scope:** DJ30 + NAS100 pairs only (4 of 8 manifest files dropped — the two legs the Bulenox book uses). Guardian/Aegis pairs not dropped; their B tasks (B3/B4) stay gated/parked.

## Step 1–2: byte-identity vs the locked manifest — PASS (prop pair)

`python scripts/check_pine_manifest.py`: **zero MISMATCH** lines; the 4 dropped files hash-MATCH `core/strategies/MANIFEST.sha256`. Only failures are the 4 un-dropped guardian/aegis lines (expected MISSING; exit 1 is the partial-drop artifact, not drift). `validate_params.py`: **0 HARD / 0 WARN** — Pine input defaults agree with `params.toml`.

## Step 3: A5 provisional values — CONFIRMED, floors stand

Read from the dropped source (not LOCK.md): ATR length **11**, SL **1.20×ATR**, risk **0.70%** (DJ30) / **0.37%** (NAS100), pyramid **750%** (DJ30) / **1000%** (NAS100), pyramidMinBars 6, session `hour(time,"UTC")` 13–17, DJ30 Tue/Fri + NAS100 Mon/Tue (parametric bools, lock defaults). No delta vs `derive_floors.py::CONFIGS` → **no A5 re-run needed on parameter grounds**; RESULTS_phaseA stands.

## Step 4: sizing/session constructs the futures editions must substitute

- **Strategy (backtest) sizing:** `calcSize = strategy.equity × risk% / stopDist` — **rolling-equity compounding, NO $/pt divisor** (the $1/pt Pepperstone US30/NAS100 CFD spec is baked in implicitly). Futures editions must add the $/pt term: `qty = floor(risk$ / (sl_pts × $/pt))`.
- **Indicator (live-alert) sizing:** `calcLots = accountSize × risk% / (slDist × contractValue)` — **STATIC accountSize** input (manually bumped), contractValue=10. The live execution path is static-balance.
- **C5 pre-reg propagation (the plan's explicit B0 question):** the Pine has BOTH constructs — backtest compounds off equity; the executed/live path (indicator → bridge JSON) sizes off static account. The C4 orchestrator's static-tier-balance panel rescale matches the **live** construct. C5 keeps static-balance sizing (matching what will actually execute), documented here as the B0 answer.
- **Pyramid add:** `initialSize × pyr%` from the **SAVED base size** (indicator warns explicitly: never recalc at current ATR). Integer flooring applies to base AND add in the futures editions.
- **DJ30 day-stop:** strategy Pine default **−2%** with live TV override −1.15%; indicator default −1.15 (`enableDayStop=false`); `maxDailyDD` input native 1.15. Confirms B2's promoted-default task (−1.15 becomes the MYM edition's default).
- NAS100 maxHold **15 bars** (vs DJ30 55) — explains its 0% force-flat boundary crossings.

## Deploy-time floors (B0-owed recompute; NQ/YM bars re-parsed from the 2026-07-01 exports, roll-seam-masked, RMA ATR(11))

Reproduction check: full-median floors **$15,322 (MNQ) / $3,300 (MYM)** = Phase A's numbers exactly.

| | ATR(11) pts | SL pts | floor_balance |
|---|---|---|---|
| **MNQ** panel-median | 23.62 | 28.3 | $15,322 |
| **MNQ recent-90d** | **44.92** | **53.9** | **$29,137** |
| **MYM** panel-median | 38.50 | 46.2 | $3,300 |
| **MYM recent-90d** | **49.73** | **59.7** | **$4,262** |

Integer contracts per Bulenox tier at **recent-90d (deploy)** ATR — base (rounding loss) / pyramid add:

| Tier | MNQ base | MNQ add (1000%) | MYM base | MYM add (750%) |
|---|---|---|---|---|
| $25K | **0 (−100%) — NAS100 CANNOT TRADE** | 0 | 5 (−15%) | 37 |
| $50K | 1 (−42%) | 10 | 11 (−6%) | 82 |
| $100K | 3 (−13%) | 30 | 23 (−2%) | 172 |
| $150K | **5 (−3%)** | 50 | **35 (−1%)** | 262 |
| $250K | 8 (−7%) | 80 | 58 (−1%) | 435 |

## Findings

1. **$25K tier is DEAD for the 2-leg book** at deploy vol — MNQ base floors to 0, so NAS100 never enters; the C4 25K gate number (99.60%) silently assumed a book that can't exist there. 25K would be DJ30-only (a different, un-MC'd book).
2. **$50K NAS100 runs 42% under-risked** — C4's 50K number assumed full %-risk; integer reality deflates the NAS leg materially (lengthens pass, breaks the locked leg ratio).
3. **$150K is the granularity sweet spot** (MNQ −3% / MYM −1%) and carries C4's 91.46% gate pass — the tier where the %-equity approximation is most honest.
4. **NEW COLLISION FLAG — per-account contract caps vs the pyramid add.** At $150K the DJ30 position peaks at base 35 + add 262 = **297 MYM micros** ($250K: 493). Futures-prop firms cap per-account contracts (micro caps are typically ~10× the mini cap). If Bulenox's 150K micro cap is anywhere near typical (~150), **the DJ30 pyramid — the structural edge — is clipped by the CAP, not by granularity.** This moves "confirm per-account contract caps" from housekeeping to a **load-bearing Bulenox primary-source confirm** alongside target%/inactivity/flat-time. NAS100 is safe (add ≤ 80).

## Still owed

- Bulenox per-tier **contract caps** (now load-bearing, finding 4), target%, inactivity, exact flat time, per-SKU DD mode.
- B1/B2 edition authoring (unblocked by this gate) → TV-native compile + per-candle parity = operator-side.
- C5 integer-contract MC on the C4 panel using these floors (runnable next; futures-native panels from C3 refine it later).

**Repro:** `scripts/parse_bar_export.py --symbol {NQ,YM} --in <Downloads BAR_EXPORT v0.2 ...NQ1!/YM1!...>`; driver = session scratchpad `deploy_floors_b0.py` (roll-seam-masked RMA ATR(11), recent-90d = median of ATR series over last 90 calendar days).
