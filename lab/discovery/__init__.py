"""Discovery package — K-ledger (``register_search``) + Stage-2/4 campaign runner.

Canonical entry points (research venv, ``PYTHONPATH=lab``):

* ``python -m discovery.register_search`` — bind/close search-space K
* ``python -m discovery.stage24_runner`` — Mine → Bind-K → Score → emit-matrix
"""
